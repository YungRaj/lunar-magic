# Lunar Magic 3.63 feature-parity ledger

This ledger is the product-level completion boundary for the Rust reimplementation. It complements
`REIMPLEMENTATION_TEST_MATRIX.md`, whose rows primarily describe format and subsystem evidence.
Lunar Magic feature parity is not complete until every in-scope workflow below is `Pass`.

## Evidence and status rules

The original Lunar Magic 3.63 executable is the enumeration and behavior authority. The current
baseline contains 270 PE resources, including 107 dialog resources. Ghidra on port 8089 provides
the named implementation surface; Wine provides observable UI, file, and ROM-transition behavior.

Each workflow has five independent gates:

- **Model**: lossless decode/encode of every owned and opaque field.
- **Tx**: checked mutation, checksum repair, unchanged-range preservation, reopen, undo, and redo.
- **GUI**: the complete user workflow is reachable and usable in `lm-native`.
- **Oracle**: a retained Wine fixture or automated behavioral comparison covers success and
  rejection behavior.
- **Variants**: the supported ROM/format variants are explicitly enumerated and tested.

Gate values are `P` (proved), `~` (partial or indirect evidence), and `-` (missing). A row is
`Pass` only when all required gates are `P`. `N/A` is allowed only when the original workflow has
no corresponding gate, with the reason recorded in evidence.

Broad format support does not promote a row. For example, decoding MWL files does not prove the
interactive level import workflow, and a CLI transaction does not prove its native dialog.

Current LMSW-row addendum: the concrete, separately unsafe-isolated `lm-libretro` backend and its
localized native control window now implement ROM load, bounded RGBA frames, pause/resume,
single-frame stepping, viewport cropping, keyboard joypad input, process teardown, runtime SMW state
observation, and capability negotiation through `LMEMU001`. It automatically advances an untouched
vanilla ROM to a valid transition point and injects the selected 9-bit level; an active session can
switch levels through the same `$0F..$14` game-mode sequence. The retained
`tools/lm-libretro-smw-oracle.py` run against the official ARM64 Snes9x core proves vanilla level
`$105`, live switch to `$106`, distinct nonuniform 256×224 RGBA frames, hard pause/one-step, and
clean teardown. Portable packages contain `lm-native`, `lm-cli`, and the required sibling
`lm-libretro` executable. Active editor-level changes use `LoadLevel`, while committed revision
changes send the exact new in-memory ROM and re-enter the selected level without closing the live
window; the oracle proves an identical revision reload reproduces level `$105` and its exact frame.
When **Game pixels** and the SNES viewport are enabled, the running core's RGBA frame is now
composited into the level-editor canvas using the same centered cover geometry as the deterministic
game-pixel view. It recomputes from the current canvas dimensions on horizontal, vertical, and
full-screen resize while preserving the canvas response used for entity hit testing. The default-on
**Selection over game** option reproduces the recovered `$004530A0` compositor
ordering by repainting only selected Layer 1/Layer 2 object tiles and selected sprite artwork after
the live frame, including transparent pixels and selection outlines; it can be disabled without
affecting canvas hit testing. Audio callbacks now preserve bounded interleaved stereo samples in the
same atomic runtime-frame response. The native frontend plays them through a two-second bounded
queue, linearly resamples the core's actual rate to the device rate, exposes mute, and clears queued
audio on pause, mute, reload, stop, and teardown. The retained official-Snes9x oracle requires
nonuniform 32,040-Hz audio at `$105` and `$106`; identical ROM reload reproduces the exact `$105`
audio hash, while the native hardware-output smoke gate opens and closes the actual output device.
Sprite-only built-in-editor commits now reproduce `RefreshLmswSpritesAfterEdit`: Rust classifies the
commit only when sprites changed and Layer 1/2 did not, serializes the native stream after its
one-byte header, and sends it with the accumulated ROM revision. The isolated backend mirrors that
stream into temporary save RAM, redirects SMW's native loader, clears `$14C8..$14D3` and
`$1938..$19B7`, and preserves the running level/core. Every displaced save byte is restored before
stop or transition, and the accumulated ROM snapshot is installed before a later level switch.
The official-Snes9x oracle changes real sprite records, queries the core's exact `$14C8`, `$009E`,
and `$1938` tables, and requires SMW to instantiate the edited Goomba without a `$14`
mode/sublevel transition. It then proves direct switching still reaches a distinct level frame; the
complete accumulated ROM remains ready for the next ordinary revision reload. The row remains
`Partial` only because broader core/runtime/platform variants are still
missing. This addendum
supersedes only the older clauses that list the now-proved backend, native control, selected-level,
input, audio, and compatible-core runtime boundaries as missing.

The release matrix now explicitly compiles the sibling `lm-libretro` executable for Linux x86-64,
Windows x86-64, Apple Silicon macOS, and Intel macOS. The ordinary three-OS CI matrix also performs
the same complete runtime build and invokes the strict deterministic packager, which rejects an
absent or incorrectly suffixed backend. This proves portable-package presence across the declared
targets; it does not substitute for the remaining additional-core runtime oracles.

An independent official buildbot Snes9x 2010 core now passes the same complete `$1FF` runtime
oracle against the identical vanilla ROM: selected `$105` injection, in-place edited-sprite
consumption, `$106` switching, exact reload reproduction, RGBA, stereo audio, and live WRAM sprite
tables. Full-path cores are no longer rejected categorically: their exact immutable ROM revision is
held in a private lifetime-bound `.smc` snapshot. A core must expose exact 128-KiB SMW WRAM after
at most one bootstrap frame or initialization rejects before claiming live-level support. The
remaining platform-runtime boundary is described after the independent bsnes result below.

The independent bsnes 2014 Accuracy core now closes the emulator-family variant: the unchanged
oracle passes selected-level injection, in-place sprite replacement, runtime sprite-table queries,
level switching, deterministic whole-ROM reload, video, and audio. Its valid 512×224 doubled-width
output is accepted alongside Snes9x's 256×224 geometry while preserving exact per-core reload hashes
and distinct-level hashes. Rust also consumes libretro's bounded standard memory-map descriptors as
a fallback for cores that do not implement memory ID 2, accepting only a mutable exact `$7E:0000`
128-KiB WRAM mapping. Cross-platform runtime status follows the Windows execution below.

The optimized `x86_64-pc-windows-gnu` backend now passes that same complete oracle as a real PE32+
process under Wine 11.13 with the official Windows Snes9x 2010 DLL. Its `$105`/`$106` frame and
audio hashes exactly equal the native ARM64 Snes9x 2010 results, including live edited-sprite state,
whole-ROM reload, and clean process teardown. The driver accepts one optional direct process runner
without invoking a shell. A separate hosted Ubuntu job builds the real Linux backend, dynamically
loads a strict deterministic libretro core, and exercises every advertised `$1FF` capability:
immutable ROM load, WRAM/SRAM, RGBA, audio, direct levels, sprite hot reload and runtime tables,
pause/step, deterministic reload, and teardown. The mock supplies only the platform/ABI axis; real
SMW semantics remain independently proven by the macOS and Windows vanilla-ROM runs. Linux,
Windows, Apple Silicon macOS, and Intel macOS packages all build the sibling backend, while native
macOS, Windows, and hosted Linux execution cover every declared runtime family. The LMSW row is
therefore `Pass`.

The native live window now also applies the recovered focus-only soft pause and main-window `$20`
hard pause from egui's OS focus/minimize state. The same aggregate model preserves manual-hard-pause
precedence and resumes only when all corresponding states clear;
`viewport_focus_and_minimize_map_to_original_pause_inputs` binds the frontend observation.
Collapsing the live viewport now drives hard-pause reason `$04`. Opening an egui menu/popup drives
input reason `$08` and retains Lunar Magic's recovered 100-millisecond post-menu grace before
resuming; `menu_input_pause_retains_the_recovered_hundred_millisecond_grace` covers the boundary.
Port-8089 recovery of `HandleLevelEditorCommand` resolves editor-mode reason `$40` exactly: it is
set when the shared editor animation clock is paused and cleared when playback resumes. Rust now
wires that same `VanillaLevelEditor::animation_playing` state into the aggregate, completing all six
hard reasons plus focus soft pause in the native session.

Current Map16 bitmap variant evidence additionally covers the 12-byte `BITMAPCOREHEADER`: its
unsigned dimensions, bottom-up DWORD-padded rows, RGBTRIPLE palettes, indexed 1-/4-/8-bit pixels,
and 24-bit BGR pixels reach the installed native preview. Malformed core headers, palettes, and
pixel planes reject before preview construction. This is covered by
`map16_bitmap_import::tests::bmp_decoder_handles_core_header_rgb_triples_and_bottom_up_rows`,
`map16_bitmap_import::tests::bmp_decoder_rejects_malformed_core_headers_palettes_and_pixels`, and
`rom_map16_editor::bitmap_import::tests::core_header_bmp_reaches_the_native_preview_with_rgb_channels_intact`.

Current bitmap-option oracle evidence covers Popularity Method 1, Method 2, neither neighborhood
method, Method 1 with Maintain Detail, and the existing-colors-only route with “Allow modifying
palette colors that aren't marked on right” disabled. All five match Lunar Magic's active palette
and complete `$000–$2FF` graphics workspace byte-for-byte. The Maintain Detail breakpoint proves
the leading zero-color sentinel participates in distinct-source assignment; a low-color exact
capture further proves that this is an opaque candidate rather than transparency. The fifth gate
proves retained colors can seed one row and be rearranged into a later row, equal-error selection
prefers exact movable assignments over merely retained words, and equal existing-color entries use
the later index. The live differential now also compares all 524,288 Map16 definition bytes.
Default deduplication at `$8200`, non-deduplicated 2×2 placement, and priority-enabled allocation
from `$83A5` match exactly. The sequential case proves Lunar Magic preserves source rows at a
`$10`-tile destination stride and corrected Rust's formerly flattened placement; the exact
`$8000`/next-`$1000` allocation boundary is now applied. A boundary capture beginning at `$8FFF`
also proves exhaustion is prefix-preserving: Lunar Magic commits the one available definition,
retains palette/graphics conversion, and reports its exact partial-import warning.

Three additional low-color captures close an ambiguity that the larger fixtures could not expose.
Opaque black is not automatically transparent: an exact black already present in a usable nonzero
palette cell bypasses the generated-color limit, stays at its original row/index, and materializes
as a real graphics tile. This remains true with a one-color limit and with Maintain Detail enabled;
Rust now matches all three complete palette, graphics, and Map16 workspaces exactly.
A fourth near-black/red Maintain Detail capture distinguishes exact black from the sentinel's
nearest-source assignment. Lunar Magic routes opaque near-black through row 0/index `$D` and emits
graphics tile `$200`; Rust now preserves that opaque zero-color candidate through row allocation
instead of collapsing it to transparent index zero. Its complete palette, graphics, and Map16
workspaces also match exactly. A pristine-ROM integration gate now crosses transparent source with
all four blank-8×8/blank-Map16 switch combinations and proves the two products are independent:
`$0F8` bypasses graphics allocation, while `$8000` bypasses definition `$8200`. Independently
crossed original-editor captures remain open.

Current Layer 1 import evidence also distinguishes extent from ordering. MWL import recomputes the
five-bit stored last-screen field from the highest visible Layer 1 object or sprite, but preserves
raw backward screen-jump order. `lunar_magic_recomputes_extent_but_preserves_raw_layer1_order`
proves both rules against Lunar Magic 3.63, while
`mwl_import_recomputes_last_screen_without_sorting_raw_layer1_order` covers the installed Rust
import boundary.

Current native-renderer integration evidence covers a complete level rather than a single fixed
viewport. `pristine_full_level_render` composes the stored full extent with authenticated Map16,
background, object, sprite, entrance, orientation, palette, and animation state. The responsive
viewport fits exactly one 256-by-224 SNES screen into the available canvas and recomputes that fit
for horizontal, vertical, and full-screen window changes. The current fixture-backed `lm-native`
gate has 933 passing tests and 12 explicitly ignored external/hardware-fixture tests (the audio
hardware gate was also rerun explicitly and passed);
`every_pristine_level_materializes_its_builtin_render_assets` traverses all
512 pristine level slots, while `pristine_full_render_matches_native_dimensions_and_empty_outcomes`
checks full-render dimensions and empty outcomes.

The native Help menu now includes an offline, searchable Help Topics browser covering every
primary editor family and the shared save, undo, import/export, recovery, and compatibility
boundaries. `help_dialog::tests` proves topic-family coverage, case-insensitive title/body search,
and idempotent reopening without losing the current query or topic. Exact CHM topic enumeration
and original Lunar Magic navigation behavior remain oracle gaps.

The ROM-backed `LMNATED1` aggregate workflow now includes `map16=` and `entrances=` domains alongside level,
Layer 2, palette, ExAnimation, expanded settings/ExGFX, and sprite-spawn edits. Level assets are
materialized privately before Map16 planning so the Map16 allocator observes every newly claimed
block. Main, Layer 2-scroll, and installed separate-midway entrance edits are then prepared from
that staged result; all prepared results collapse into one revision-checked ROM mutation, checksum
repair, history entry, reopen boundary, and undo. The expanded
`terminal_native_assets_spec_commits_all_domains_as_one_undoable_operation` gate proves the
custom block, entrance, and level changes reopen together, undo byte-exactly, and remain
failure-atomic when a late acts-like cycle or unavailable midway entrance rejects after all earlier
domains have been prepared.

The 2026-08-05 retained original-editor run in `ORACLE_RUN_LOG.md` executes 27 opt-in Lunar Magic
3.63/Wine gates with exact executable and pristine-ROM SHA-256 identities. All passed, including
the complete 18-test object/sprite/entrance/screen-exit growth and canonicalization suite plus
legacy headers, expanded level mode, ExAnimation feature options, and support-patch-B/custom-time
preservation. These results strengthen the affected Oracle gates but do not promote variant or
original-dialog coverage that those tests do not exercise.

The level-header Wine target now edits and reopens every legacy five-byte-header field, including
the Layer 1 vertical-scroll selector, plus the sprite header. A second exhaustive gate imports all
twelve reserved modes `$12–$1D` with varying adjacent background-color bits and requires Lunar
Magic to re-export mode `$00` while preserving the color exactly. This confirms the recovered
`LoadLevelModeConfiguration` fallback across the full reserved range rather than one representative
value; separate scroll/spawn dialogs and broader ROM variants remain open in the aggregate row.

Lunar Magic 3.63 has fixed per-editor keyboard dispatch and shortcut fields inside external-tool
definitions, but no global keyboard-shortcut configuration workflow. The complete named Ghidra
surface includes `DispatchLevelEditorKeyboardShortcut`, editor-specific key handlers, and
`DispatchExternalToolKeyboardShortcut`; the authenticated executable contains no shortcut-dialog
command or text, and its live top-level window exposes no such menu action. Rust's portable global
`LMSHORT1` editor is a useful extension and remains tested, but it is excluded from the parity
denominator. Original fixed-key behavior remains evidence within the affected Level, Map16,
Graphics, and Overworld workflow rows; external-tool shortcuts remain under Configuration.

The original-compatible user toolbar now routes `LM_VIEW_LAYER_1`, `LM_VIEW_LAYER_2`,
`LM_VIEW_LAYER_3`, `LM_VIEW_SPRITES`, and `LM_VIEW_SPECIAL_WORLD` through the authoritative canvas
visibility and Special World state, invalidating both level-preview paths after each accepted
toggle. Four recovered zoom names also drive the native canvas's exact 100–5000 percent range,
100-point adjustment, default reset, and remembered nondefault toggle. `$2440` now opens the
original nine-choice 100–800 percent popup with in/out and filter controls, while `$2444` defaults
enabled, toggles the native check state, and invalidates the preview. Exact filtered presentation
remains open because it must operate on the final composited surface rather than packed source
atlases.
The `$2404/$2403/$240E` animation commands now pause/resume the shared live preview clock, step one
60 ms timer tick, and reload preview graphics without falsely resetting the retained frame state.
This state is consumed consistently by the canvas and all three sprite-list/catalog surfaces. The
four default-on switch view commands now independently select the recovered green, yellow, blue,
and red conditional Map16 forms on the live Layer 1/Layer 2 canvas and in both object catalogs.
Ghidra-backed renderer tests cover all eight page-zero/page-one tile choices. The
default-off `$2405` Silver POW view command now applies the recovered `$115` substitution to every
conditional standard-sprite handler on the canvas and both standard-sprite picker surfaces, and
selects its authenticated vanilla animation bank. The default-off `$2406` Blue POW command now
selects the recovered trigger-zero animation groups from the pristine mode/trigger tables while
retaining the original default-on Invisible POW Objects and On/Off states. Both POW states are part
of the preview cache key, so toggling either rebuilds every foreground/background animation phase. The
adjacent default-on `$2400/$2401/$2402/$2409` conditional-view controls are also live: invisible
POW tiles half-blend until Blue POW is active; other invisible tiles use the exact `$113-$115`
remaps and four embedded 16×16 overlays; On/Off changes its two trigger groups; and conditional
Direct Map16 selects the `+$100` definition bank for records carrying both recovered conditional
bits on Layer 1 and object-backed Layer 2. The
`$2407` 512-height background command now changes the game-preview vertical wrapping from 27 to 32
Map16 rows in both composed-plane and direct-Map16 presentation without mutating level data. The
`$2415` translucent command now half-blends every active editor overlay while leaving level artwork
opaque and preserving hit testing. The
`$2408` tile-grid name
now controls the original default-off Map16 canvas grid instead of the grid being painted
unconditionally. The distinct `$23F6` Screen Grid overlay now draws the original orientation-aware
Top/Bottom or Left/Right screen regions and hexadecimal labels. `$23F5` now renders mutually
exclusive screen-exit regions and resolves direct, midway, secondary-level, and overworld labels
through the detected pristine/installed secondary-exit table. `$23F7` completes the mutually
exclusive overlay group with the original mode-dependent 256×232, 352×232, and 448×224 boundary
guide anchored to the level camera. The independent `$2410` Surface Outline and `$2411` Line Guide
Outline names now toggle the exact renderer flags recovered in `HandleLevelEditorCommand`. Rust
composites PE resource 500/524's 113-glyph atlas over object-backed and tilemap-backed Map16 cells,
resolves custom Acts Like chains to vanilla roots, uses the recovered 512-entry surface table with
object-tileset substitutions, and applies the pristine line-guide `$76..$99` lookup. This
strengthens the Configuration toolbar GUI evidence but does not promote that workflow while the
remaining internal command table and toolbar variants are incomplete.

Current Map16 transfer evidence also covers F9's original three-way dispatch: unmodified commit,
Ctrl+F9 ROM-sibling `.m16` export, and Ctrl+Shift+F9 ROM-sibling `.s16` export. The native path
snapshots a matching live sidecar document or the ROM-associated buffer, preserves the exact
`0x2000` M16 shape and last-nonzero/`0x800`-rounded S16 shape, confirms before writing, and permits
only atomic creation or replacement of regular files. The focused
`rom_map16_editor::sidecar_export::tests` bind the shortcut, path, buffer, shape, and target gates.
This strengthens the Map16 transfer GUI evidence but does not promote its row while complete
original prompt gestures and Wine interaction remain incomplete.

Current Graphics evidence additionally recovers the complete `$4000`-tile internal decoded-cache
layout behind Ctrl+Shift+Page Down. The pristine native editor materializes the current FG/BG and
sprite banks, GFX33, GFX32, and eight Layer 3 banks at their exact recovered destinations; absent
auxiliary animation, ExAnimation, and external-sprite-file banks retain Lunar Magic's zero-filled
initialization. The focused shortcut raises the visible limit from page `$05` to `$3F` and reports
the original unlock status. `pristine_internal_graphics_cache_materializes_every_recovered_owned_bank`,
`pristine_editor_loads_the_complete_diagnostic_cache_but_keeps_it_locked`, and
`focused_grid_routes_native_modified_page_keys_to_palette_rows` bind the layout, lifecycle, and
modifier split. The pristine editor now refreshes that cache across active-level and Special World
changes without discarding staged ordinary-file edits; its diagnostic pixels and transforms stage
only Lunar Magic's private selected-tile buffer until an eligible right-paste commits backing,
while right-paste retains the recovered current-level, bypass, animation, and Special World
guards, and F9 encodes the exact six
FG/BG/four sprite working slots. The profile-backed installed editor materializes that same level-dependent
cache, including profile-resolved bypass graphics and Layer 3, the four reserved GFX60–GFX63
ExAnimation source banks, the selected expanded-header or last legacy `$25/$26` auxiliary file,
and all present bounded ROM-sibling `ExSpriteGFX00–07.bin` files. Diagnostic tiles accept the
original's staged pixel/transform edits independently of backing paste eligibility; F9 publishes only the six current
FG/BG and four sprite working slots, skips `$7F`, and resolves duplicate assignments in native
write order. `pristine_diagnostic_cache_edits_are_transient_and_bounded_like_original`,
`pristine_diagnostic_clipboard_paste_stops_after_current_level_cache`,
`pristine_diagnostic_f9_publishes_exact_cache_slots`, and
`pristine_cache_refresh_follows_special_world_without_losing_staged_file_edits` bind the pristine
route. The retained Wine `graphics-pixel-buffer/oracle.tsv` proves transforms and painting mutate
the private 64-byte edit buffer while decoded and planar backing remain unchanged. The retained
Wine `graphics-cache-paste/oracle.tsv` additionally proves ordinary `$002` and
boundary `$5FF` mutation plus exact rejection at fixed-animation `$041`, unused non-bypass `$300`,
and out-of-range `$600`; its automated binding exhausts every recovered predicate range, and both
installed clipboard entry routes share that predicate so direct paste events cannot bypass it. Broader
original interaction/write fixtures remain incomplete, so the Graphics
workflow is not promoted.

Current Graphics shortcut evidence also corrects the older off-by-one function-key wording in the
ledger row below. `HandleGraphicsEditorWindowMessage` forwards raw `WM_KEYDOWN` virtual keys:
F8 toggles the tile grid and Ctrl+Alt+F8 changes its color; unmodified F9 confirms and publishes the
current level's working FG/BG/sprite buffers. F9 requires the complete ROM-sibling standard set
`Graphics/GFX00.bin` through `GFX33.bin` before output, and the Special World view omits the normal
SP2 assignment. Rust now replaces the selected decoded `$1000`-byte files as one recoverable group,
substituting a staged active file when applicable; standard files resolve under `Graphics`, while
expanded selectors use canonical names under sibling `ExGraphics` and join the same complete
preflight. ROM commits remain explicit Rust buttons. The
portable document retains its independent F9 save. `original_level_graphics_publication_shortcut_is_unmodified_f9`,
`extracted_graphics_paths_use_the_rom_sibling_directory_and_complete_standard_set`,
`replacement_batch_requires_the_complete_declared_set_and_replaces_atomically`, and the focused
grid tests bind this correction. Joined mode is now persisted through the recovered `$24BD` option;
its exact `$36D00` AllGFX shape, 52-entry range table, `$7F` omission, selected-range replacement,
and grouped ExGFX publication are covered by `joined_paths_require_allgfx_and_only_selected_extended_files`,
`joined_replacement_patches_declared_ranges_and_preserves_every_other_byte`, and the preference
round trip. The retained Lunar Magic 3.63 level-105 Wine oracle now proves the exact eight-file
separate mutation set, zero-mutation missing-`GFX33.bin` preflight rejection, `$24BD` mode toggle,
and byte-exact joined range mutation. `retained_lunar_magic_f9_oracle_binds_separate_and_joined_publication`
binds that evidence. Installed cache population now covers every recovered level-dependent,
GFX60–GFX63 ExAnimation, selected auxiliary-animation, and external-sprite bank for which the owning
source exists. The diagnostic cache is transiently editable and exact current-level cache slots
feed F9 publication; broader original interaction/write fixtures remain incomplete, so the
Graphics workflow remains Partial.

The Map16 bitmap Oracle evidence now additionally includes Median Cut maximum-color settings
1, 2, 4, 8, 9, 16, and 32 plus maximum-one Popularity. These retained captures cover SNES-lattice
source normalization, BGR equal-cut priority, x87/binary32 scoring, reusable-entry exclusion,
substitution-aware capacity retries, native palette-row allocation, and high-color
per-tile capacity reduction with four-border weights. All nine captures match Lunar Magic's first
32 active palette words and complete `$000–$2FF`
graphics workspace byte-for-byte. The transparent-source blank-option product now also crosses all
four blank-8×8/blank-Map16 combinations through complete headerless/headered Rust commits, semantic
reopen, checksum verification, copier-prefix preservation, exact physical Undo/Redo, and logical
header equivalence. Static original-program evidence closes the option truth table: the retained
`004EE470` graphics branch and `004EF2D0` Map16 branch prove the switches independently control
their respective allocations, including the intervening zero-occupancy recomputation at
`004EEE40`. The complete control inventory below supersedes the former open option-product note;
the aggregate workflow's installed-ROM transaction product is now complete. The
formerly hardcoded installed graphics lookup is now corrected: Ghidra's ROM-open instruction
window proves active layout-descriptor field `+0x94` supplies the 64-byte, 16-by-4 object-tileset
graphics-assignment table. `RevisionProfile` carries that logical offset explicitly, protects and
audits the complete table as metadata, and refuses profiles that omit it rather than borrowing the
SMW-US-v1 offset. `object_tileset_graphics_lookup_covers_every_supported_identity_mapper_header_and_checksum_form`
binds the lookup to all 48 authenticated identity/map-mode/copier/checksum forms, while
`resolved_loader_uses_the_profile_assignment_row_instead_of_the_us_v1_offset` proves the bitmap
workspace consumes a relocated row.
`installed_bitmap_import_commits_reopens_and_undoes_across_all_supported_variants` completes that
48-case product with nonuniform source graphics: every case mutates and semantically reopens real
palette, GFX, and Map16 data, repairs the authenticated checksum, preserves the exact optional
copier prefix, produces byte-identical logical output across header forms, and traverses exact
physical Undo/Redo.

The original bitmap-dialog control inventory is now complete rather than inferred from sampled
captures. Resources `$418/$419` and procedures `004F15E0`/`004F1FA0` bind all 20 editable
semantics, defaults, and bounds to the native Rust model. The retained Wine family covers accepted
color and other-option changes, Cancel snapshot restoration, exact palette/graphics/complete-Map16
results, non-aligned geometry, sequential/deduplicated placement, and the exact partial-exhaustion
warning. This promotes the Oracle gate. The row remains `Partial` only because complete installed
ROM identity/mapper variant coverage is still missing.

Toolbar shortcut evidence update (2026-08-09): the original-compatible parser and portable
`LMSHORT1` schema now preserve all five mouse buttons independently. Named `VK_LBUTTON`,
`VK_RBUTTON`, `VK_MBUTTON`, `VK_XBUTTON1`, and `VK_XBUTTON2` tokens and raw virtual-key values
`$01/$02/$04/$05/$06` map to append-only logical key kinds; egui primary, secondary, middle, and
both extra-button press events enter the same shortcut stream and therefore reach both configured
toolbar actions and original-compatible user-toolbar buttons. Keyboard kind numbers remain
unchanged, and encode/decode plus editor-text tests cover all five appended values. This supersedes
the toolbar row's older closing phrase for mouse shortcuts; Pause and numpad-operator physical-key
distinctions remain open because the current frontend input abstraction does not expose them.

Windows publication-safety update (2026-08-09): grouped, paired, replacement, and rollback cleanup
no longer depend on Rust's unstable Windows `MetadataExt` file-identity methods. The safe
`lm-windows::file_identity` boundary queries `GetFileInformationByHandle`, combines the volume
serial with the complete 64-bit file index, and immediately closes the temporary handle. Every
publication path captures that identity before rename/link operations and removes a rollback or
staging path only when its current identity still matches, retaining the existing protection
against deleting a racing unrelated replacement. The 20-test persistence family covers canonical
aliases, distinct hard links, identity-guarded cleanup, rollback, collision, and grouped/paired
publication; `cargo check -p lm-native --target x86_64-pc-windows-gnu` now completes successfully.

## Workflow ledger

| Area | Original Lunar Magic workflow | Model | Tx | GUI | Oracle | Variants | Status | Primary evidence / next gap |
| --- | --- | :---: | :---: | :---: | :---: | :---: | --- | --- |

Graphics mapper evidence update (2026-08-08): the ExLoROM portion of “Extract/insert GFX and
ExGFX” is now verified. LoROM split-plane and shared-bank graphics pointers use Lunar Magic's
`$00..$7F` mirror, while ExLoROM and SA-1 retain mapper-significant high banks. The live gate
converts a complete standard-GFX plus `ExGFX80` project to 8 MiB ExLoROM, migrates the relocated
ExAnimation runtime, inserts `ExGFX81`, and requires Lunar Magic 3.63 to re-export all 52 standard
files plus both ExGFX files exactly. The row's older closing phrase that lists ExLoROM as open is
superseded by this evidence. SA-1 standard GFX is now verified separately against an authentic
SA-1 Pack v1.40 source: Rust preserves the pack's `$7B` graphics operands and existing RATS
owners, relocates the 32-byte DMA helper through an SA-1 pointer fixup, and Lunar Magic 3.63
re-exports all 52 files exactly from the checksum-valid Rust result. All three SA-1 first-install
ExGFX domains are now verified: Rust reproduces Lunar Magic's distinct `ExGFX60`, `ExGFX80`, and
`ExGFX100` fixed-marker transitions byte-for-byte, including raw reserved ownership, ordinary LZ2
storage, and the extended pointer table inside the expanded-settings owner. The extended route
resolves that owner's authenticated SA-1 runtime operand instead of assuming `$088000`, so it also
survives first-fit relocation after Rust's denser standard-GFX allocation. Lunar Magic 3.63 reopens
each independently generated Rust result and re-exports all three source files exactly. The row
also covers all four multi-domain first-import combinations. Their seven total domain masks have
distinct fixed-marker lengths/terminal bytes and two allocation orders: without reserved files,
extended storage precedes ordinary storage; with reserved files, reserved and ordinary payloads
use the lower first-fit gap before the extended allocation. Every complete Rust ROM is byte-exact
to its Lunar Magic oracle, and the original editor re-exports the three-domain result exactly. The
native directory route now also matches Lunar Magic's subsequent full-set synchronization. It
authenticates and reclaims every referenced ExGFX owner, republishes present files, clears pointers
for omitted files, preserves the sparse API's intentional retain-omitted behavior, and folds the
whole operation into one revision and one Undo step. Retained replacement-all and only-`ExGFX80`
SA-1 transitions are complete-ROM byte matches, and Lunar Magic re-exports each surviving Rust file
exactly. The row remains `Partial` only for the other graphics option and interaction variants
recorded below.
Compression mapper evidence update (2026-08-09): installed Fast-LoROM map mode `$30` now has an
original-editor-crossed LZ2-Orig-to-LZ3 gate across both copier-header forms. Rust preserves the
Fast-LoROM byte, produces identical logical results from headered and headerless inputs, repairs the
checksum, reopens all 52 standard graphics files byte-for-byte against Lunar Magic's own LZ3
conversion, and undoes to each exact physical source. Lunar Magic recognizes both Rust results as
already LZ3 without changing logical bytes and re-exports every file exactly. SA-1, ExLoROM, and
historical runtime-generation variants remain open for the compression row.
| Map16 | Import SNES graphics set and screen tile map | P | P | P | P | P | Pass | Ghidra recovers `ImportMap16PageFromSnesFiles` at `$004E5390`, `LoadPaletteRowFromFile` at `$004765E0`, and `PasteMap16IndexGridIntoLevelLayer` at `$00519010`. The bounded Rust core zero-pads up to `$8000` graphics bytes, requires the complete `$0800`-byte 32×32 screen map and optional `$20`-byte palette row, applies the active 1,024-entry graphics remap plus wrapped offsets and selected color map with native last-write alias behavior, preserves every screen-word attribute bit, and assembles all 256 definitions. Page application reproduces direct replacement with preserved Acts Like and failure-atomic page-local deduplication, returning the global 256-entry index grid. The installed editor exposes the native three-dialog sequence, palette/optimization/remap/filter controls, bounded revision-bound preview, and conflicting-work/close gates. Apply commits Map16, only referenced tiles across the request level's four legacy or six Super-GFX-bypass FG/BG slots in exact VRAM order, the optional palette row, and optimized background-page index-grid paste into tilemap-backed Layer 2 as one checksum-repaired mutation; the unavailable `$300..$3FF` workspace page has no false sprite-file ownership, and duplicate backing files coalesce or reject impossible conflicts. Installed-bypass and pristine-ROM tests reopen every changed domain and prove one Undo restores the byte-exact ROM. A retained Wine run drives the hidden original command through its native option and two file dialogs, confirms optimization defaults on, selects direct placement, and records 5,491 changed graphics bytes plus 1,819 changed bytes on Map16 page `$00` with exact before/after hashes. The native dialog starts Optimize enabled and retains every importer choice across Map16 close/reopen at the original process-lifetime boundary. `optimized_palette_apply_matches_across_copier_header_variants` covers the formerly missing combined permutation: optimized blank-definition deduplication and palette-row import commit graphics, background Map16, palette, and the Layer 2 index grid together on headered and headerless pristine ROMs, preserve exact physical framing, repair checksums, reopen every domain, produce byte-identical logical ROMs, and undo to each exact original. The original can mutate graphics before an insufficient-blank failure; Rust intentionally provides the stronger failure-atomic cross-domain operation. |
| Lifecycle | Open ROM, recent ROM, close, quit, dirty confirmation | P | P | P | P | P | Pass | `lm-app` lifecycle and native menus pass. The retained isolated-Wine Lunar Magic 3.63 dirty-close oracle proves the exact `Save level to ROM?` Yes/No/Cancel contract: Cancel retains the frame and modified byte, No closes, and Ghidra proves Yes dispatches save command `$2392`. The native confirmation now exposes localized Save/Discard/Cancel; Save persists asynchronously and performs the requested close, quit, or replacement open only after exact snapshot acknowledgement. Destination cancellation, worker-start failure, I/O failure, and stale acknowledgement clear the deferred action while retaining the dirty project. `retained_lm363_dirty_close_prompt_has_save_discard_cancel_contract`, `confirmed_save_closes_only_after_successful_persistence`, and `failed_confirmed_save_keeps_dirty_project_open` bind the original prompt and native completion boundary. `confirmed_save_close_covers_all_forty_eight_supported_identity_variants` executes actual asynchronous persistence and close across three accepted game/region identities, LoROM/Fast-LoROM/SA-1/ExLoROM map modes, present/absent copier headers, and valid/damaged checksums; every physical prefix is exact. Together with `every_supported_identity_variant_is_explicitly_enumerated`, this completes the supported lifecycle variant product |
| Lifecycle | Save and Save As with atomic replacement | P | P | P | P | P | Pass | Exact application snapshot acknowledgement passes. Save As now distinguishes an absent destination from an existing regular file after the platform chooser's overwrite approval: the former publishes create-new, while the latter uses the same-directory atomic replacement path and is acknowledged as `ReplaceAs` so the new path is adopted, enters recent documents, and becomes the target of future Saves. Directories and symlinks reject before worker launch; collision races retain create-new semantics; cancellation, worker failure, and stale acknowledgement preserve the dirty source. `save_as_overwrite_replaces_approved_file_and_adopts_destination`, `save_as_routes_absent_to_create_and_approved_existing_file_to_replace`, and `save_as_rejects_non_file_destinations_before_starting_a_worker` cover that boundary. `save_as_create_and_overwrite_cover_all_supported_identity_variants` performs 96 actual publications across the complete 48-identity product in both create and approved-overwrite modes, preserving exact physical bytes and subsequent Save routing. The retained isolated-Wine `rom-save` oracle completes Lunar Magic's actual success boundary: Yes at `Save level to ROM?` opens `Save Level to ROM as (in hex)`; default expansion plus OK transforms the exact 524,800-byte pristine input into a 1,049,088-byte, 1-MiB logical LoROM with its copier header present, 13 RATS blocks, valid checksum, exact SHA-256, and a closed process. `retained_lm363_rom_save_completes_exact_expansion_and_checksum_boundary` binds that fixture, while the retained dirty-close Cancel case supplies rejection. Ghidra `OpenRomBackingStream` (`004A69E0`), `WriteRomStream` (`004A6C10`), and `CloseRomBackingStream` (`004A6AA0`) prove the original mutates only the selected writable backing stream and has no whole-ROM Save As/collision gesture; Rust's stronger transactional Save As collision/failure matrix is therefore the corresponding native boundary |
| Lifecycle | Full-ROM restore points and associated-file restore | P | P | P | P | P | Pass | Exact `.lrp` header, linked `DIRL` records, record extents, stored/decoded checksums, raw-DEFLATE payloads, compact fill/raw commands, logical-ROM CRC-32, and bounded reconstruction pass; an LM 3.63 two-record archive restores the 2,097,664-byte target ROM byte-exactly from `smwOrig.smc`. All thirteen Ghidra-confirmed sidecar slots decode with incremental inheritance, per-record compression, and the pre-3.21 large-file offset rule. Native creation supports initial/appended full checkpoints, deltas, successful/failed reversion markers, consecutive-reversion replacement, reciprocal link/header updates, complete timestamp snapshots, and compressed/uncompressed ROM/sidecar data. Reconstruction follows the non-linear reversion graph. Native automatic-policy controls expose interval and daily full checkpoints while preserving continuity-break priority, and persist those choices across launches. The restore dialog atomically publishes the selected ROM, sidecars, and successful reversion marker, or records the native failed form. Open projects can create a new full archive or append manual full, delta, and automatic points. Native round-trip fixtures cover nonempty sidecars and timestamp-selected incremental sidecar storage. A retained compact Lunar Magic 3.63 archive created through commands `$24CE`/`$23B8` contains every `.msc` through `.osc` slot under compression; `authentic_lunar_magic_archive_restores_all_thirteen_associated_files` decodes the original archive and compares all thirteen restored byte vectors directly with their tracked source fixtures. A second retained isolated-Wine command `$23B9` oracle covers the complete original restore-dialog gesture: one owner-drawn record, initially unselected, auxiliary restoration enabled by default, deterministic row selection, the exact destructive `WARNING!` prompt, No preserving the selected dialog plus byte-identical ROM/archive, and Yes closing the open ROM, restoring the byte-exact ROM and all thirteen capture-hashed sidecars, then growing the archive from 3,060 to 3,343 bytes with a successful reversion record. `dialog-oracle.tsv`, `tools/wine-restore-dialog-oracle.c`, and the fixture-bound test retain the controls, choices, full hashes, lengths, and outcomes. |
| Lifecycle | Undo/redo with configured history limit | P | P | P | P | P | Pass | Ghidra recovers the shared `UndoMain` preference, inclusive 0–51 range, default 33 snapshots, baseline-inclusive counting, 0/1 disabled boundary, oldest-node pruning, and disabled-boundary rebuilding from `ConfigureLevelUndoHistoryLimit`, `CaptureLevelUndoSnapshot`, and `SetOverworldUndoHistoryLimit`. The Rust application applies the resulting 0–50-operation bound to open and future projects, prunes oldest undo entries, clears both directions across the disabled boundary, and persists the setting through a native Tools dialog. The authenticated isolated-Wine gate `original_lunar_magic_shares_and_clamps_every_undo_history_boundary` proves the fresh-prefix default and configured 0, 1, 2, 9, 33, 51, and above-maximum 52 values against both live editor globals; the retained TSV records the same complete boundary set. Focused model and preference-codec tests cover retention, pruning, disabled capture, invalid-setting atomicity, and cross-project application. |
| ROM | Detect identity, mapper, copier header, checksum | P | P | P | P | P | Pass | `every_supported_identity_variant_is_explicitly_enumerated` covers the complete accepted cross-product: SMW Japan, SMW North America, and All-Stars+World North America; LoROM `$20`, Fast LoROM `$30`, SA-1 `$23`, and ExLoROM `$32`; headered/headerless physical forms; and matching/damaged checksum states. It also proves the unsupported All-Stars/Japan combination rejects. Mapper-boundary tests cover complete-bank requirements through 8 MiB SA-1 and 8 MiB ExLoROM, complementary competing internal headers, mapper-explicit small images, WRAM gaps, and oversized/partial tails. The opt-in `retained_lunar_magic_modified_rom_corpus_has_stable_identity` gate passed on 2026-08-06 across twelve local Lunar Magic 3.63 outputs containing level, overworld, ExAnimation, palette, title, credits, Layer 3, and optional-asset changes, plus derived headerless and checksum-damaged forms. Exact SA-1 6/8 MiB and ExLoROM conversion tests separately require detected identity and checksum validity after save/reopen and undo/redo. |
| ROM | Expand ROM and mapper conversion rules | P | P | P | P | P | Pass | Ghidra `$0047D350`/`$004A7390`, command call sites, retained prompts, and live Wine transitions prove ordinary fixed 2/3/4 MiB targets, SA-1-only 6/8 MiB targets, the separate 8 MiB ExLoROM conversion, and `$00` growth. The native dialog exposes the same mapper-gated fixed commands, warnings, shrink prevention, and advanced ordinary route. ExLoROM conversion relocates the first 3.5 MiB, preserves the low first/final banks, installs both inaccessible-bank locks, writes the recovered metadata/runtime/compensation bytes, and crosses undo/redo as one mapper-aware transaction; pristine output matches Lunar Magic 3.63 byte-for-byte and a patterned 4 MiB fixture proves both retained regions. SA-1 addressing now matches the recovered three physical windows (`$00–$3F`, `$80–$BF`, `$C0–$FF`). The 6 MiB transition installs the three exact ZSNES lock allocations around a fourth mirrored-internal-header allocation; 6→8 MiB removes only the locks and retains that mirror. Both SA-1 outputs match live Lunar Magic byte-for-byte. App tests prove revision checks, copier-header preservation, checksum identity, save/reopen, and independent undo/redo for every mapper transition. |
| ROM | Handle, synthesize, add, and remove copier headers | P | P | P | P | P | Pass | Ghidra proves the original has no standalone copier-header conversion dialog: `AddCopierHeaderToRomFile` at `$0044E300` performs the headerless-open transition, while `ToggleSnesCopierHeader` at `$0041E450` is an internal IPS-normalization helper. Both synthesize the same complete 512-byte structure: the little-endian size word is `(logical_len >> 17) << 4`, odd map modes set bytes 2-3 to `$30,$80`, bytes 8-10 are `$AA,$BB,$04`, and all remaining bytes are zero. Rust now applies that exact rule from the open ROM's authenticated size and map mode, including expanded LoROM and 6/8 MiB SA-1 forms. The native dialog adds, replaces, or removes the complete prefix in one revision while preserving every logical byte; stale, pending-save, missing-identity, no-op, and malformed forms reject without history, and undo/redo is byte-exact. Generic-fill and exact SMW-US create-new CLI modes remain backward compatible. The live `rust_direct_rom_main_entrance_edit_is_exported_by_lunar_magic` gate proves Lunar Magic adds the exact synthesized header to a headerless ROM and preserves the production canonical header byte-for-byte; it passed again on 2026-08-06. |
| ROM | Create IPS patch | P | P | P | P | P | Pass | Ghidra `CreateIpsPatch` at `$0041F0B0` plus command `$23BA` recover the selection order, bounded normal/RLE encoder, reserved-`EOF` handling, and canonical headered coordinate space. The native non-blocking original/modified/output workflow preserves exact present prefixes, synthesizes Lunar Magic's complete 512-byte prefix for headerless supported ROMs, rejects aliases, and publishes atomically. `tools/lunar-magic-ips-audit.sh` runs an isolated Lunar Magic 3.63 process and changes logical `$001000` plus the checksum pair: both programs emit the same 23 bytes (`51aecf767b41f6e158d96d21a723da067c81f79b1d5e6b6fb329790961729b32`), whose first record begins at physical `$001200`. Focused native tests prove headered/headerless normalization and exact reconstruction. |
| ROM | Apply IPS patch | P | P | P | P | P | Pass | Ghidra `ApplyIpsPatch` at `$0041FEE0` plus command `$23BB` recover physical headered offsets, raw/RLE records, optional truncation, malformed-input errors, and the modified-ROM confirmation. The native preview converts that temporary physical result into the shared logical transaction, thereby retaining the open project's exact present/absent copier-header state, stable identity, revision checks, and one-step undo/redo. The isolated audit applies the shared 23-byte patch in Lunar Magic and obtains the exact 524,800-byte Rust target (`2e5c017a8edda1ce89aed5e198fa4c7e1b6551a209818cfd527fcb773f7c09f5`), then proves Cancel at the modified-ROM warning and a malformed patch both leave their complete targets byte-identical. |
| ROM | Scan/reclaim owned RATS blocks | P | P | P | N/A | P | Pass | Rust intentionally exposes reclamation only across the stronger explicit `LMRATS01` ownership-proof boundary; Lunar Magic has no equivalent safe user-facing oracle operation. Model, create-new CLI, revision-bound application transaction, and native preview validate every complete owned/retained descriptor, reject stale/duplicate/overlapping/forged claims and internal-header intersections, erase only proven-dead full blocks with the requested fill, repair the active internal-header checksum, and preserve retained tags. `reclamation_reopens_and_undoes_across_every_supported_mapper_family` now runs the complete command through ordinary LoROM, 8-MiB ExLoROM, and 6-MiB SA-1 fixtures, including an exact ExLoROM copier prefix; every result detects with its original mapper and valid checksum, save/reopens, retains the live block, and undo/redo traverses byte-exact physical images. Existing tests cover no-op manifests, late failure atomicity, CLI publication, and stale native workspaces. |
| Level | Navigate all 512 slots and preserve viewport history | P | P | P | P | P | Pass | 512/512 rendering ledger plus application navigation tests |
| Level | Edit level header, mode, music, time, scroll, sprite header | P | P | P | P | P | Pass | Forms and ROM transactions pass. The recovered three-bit default-music and two-bit original time-limit selectors have lossless setters that preserve their adjacent graphics/palette fields and unrelated bits, plus portable/installed native form controls, `level-header music`/`time`, and `LMLEDIT1 header music`/`time` routes. Custom time bypass is modeled as Lunar Magic's three-byte object-stream control command `$28`: the typed value preserves all 12 timer bits plus force-reset bit 15, emits the alternate low-nibble order for vertical modes, collapses duplicates only on explicit edit, rejects the non-persistable enabled-zero-without-force state, and is staged through the native header form and atomic ROM controller. Strict `LMLEDIT1` `custom-time VALUE FORCE_RESET`/`custom-time disabled` automation now shares that setter, with horizontal/vertical raw encoding, checksum/reopen, rejection atomicity, and exact ROM-undo evidence. Sprite-header control tracing proves buoyancy 1 owns `$80` and buoyancy 2 owns `$40`; both are independent checkboxes. The adjacent vertical spawn range and Smart Spawn controls now have lossless semantic MWL and authenticated installed-Lfix3 forms plus a strict semantic `LMSPAWN1` automation child with shared aggregate history, checksum repair, exact reopen, and ROM undo, while the beyond-boundary air choice shares that semantic automation path and has an installed expanded-settings control that preserves its shared GFX word. `lunar_magic_imports_and_reexports_every_sprite_spawn_setting` reciprocally proves all eight range/Smart Spawn combinations against Lunar Magic 3.63, preserves all five shared flags, and finishes with a checksum-valid ROM. Layer 1's four scroll modes have a typed lossless setter over legacy-header byte 4 bits 4-5, native form/controller routes, and strict `level-header layer1-scroll` plus `LMLEDIT1` automation with adjacent-bit preservation, checksum/reopen, and ROM-undo evidence. Layer 2 models both the original 16 paired entrance-table presets and Lunar Magic's independent 5-bit horizontal/vertical selectors, including the split vertical high bit and preserved unrelated extension flag; the MWL form/controller and pristine-ROM entrance form expose the appropriate modes. `ChangeLevelModeDialogProc` now also governs native mode changes across Layer 2 storage classes: ordinary batches reject the destructive transition atomically, the built-in editor presents an explicit confirmation, entering a tilemap-backed mode clears exactly `$800` bytes, switching back before save restores the dormant object workspace, and installed descriptors apply the recovered low-byte `(old & $FA) | $1A` and `old & $E0` masks. Undo/redo includes both active and dormant state, and an approved change saves/reopens with the matching representation instead of failing late. `level_mode_storage_change_requires_approval_resets_and_reopens_atomically`, `aggregate_level_mode_storage_change_is_explicit_failure_atomic_and_resettable`, and `aggregate_mode_storage_changes_apply_recovered_installed_descriptor_masks` cover the boundary. `LoadLevelModeConfiguration` at `$00469540` proves reserved modes `$12–$1D` are not unsupported: Lunar Magic clears them to mode `$00`, preserves the background-color bits, marks the level dirty, and only then loads layout/layer tables. Both native controllers now stage that exact repair on decode, retain the source mode for a user-visible yellow compatibility warning in both native workspaces, and semantic header edits canonicalize identically while retaining out-of-range rejection; the profile renderer plus five-slot painter dispatcher use mode-zero semantics. `reserved_modes_canonicalize_to_zero_without_changing_background_color`, `reserved_editor_modes_use_lunar_magics_mode_zero_fallback`, `reserved_level_mode_is_staged_as_lunar_magics_mode_zero_and_undoes`, and `aggregate_decode_stages_reserved_level_mode_fallback_without_losing_background_color` cover the boundary. The matched support-patch-B oracle is recovered as five fixed call operands plus the exact 48-byte `$0D:F160` runtime; Rust authenticates and installs it as one checksum-repaired, undoable native transaction, while `smw-support-patch-b-install` provides the identity-checked create-new CLI route. Every authenticated byte rejects corruption in both states, and a forced final precondition failure proves earlier staged hooks remain atomic. Headered and headerless profile/native/CLI tests preserve the complete physical copier-prefix state while producing the same authenticated logical runtime. Focused byte, atomic-controller, built-process, ROM-reopen, opt-in Lunar Magic 3.63, and generated-ROM Snes9x initialization tests cover these boundaries. `level_header_edits_reopen_and_undo_across_every_supported_identity_mapper_header_and_storage_variant` closes the supported persistence product across SMW-J, SMW-NA, and All-Stars+World-NA; map modes `$20`/`$30`/`$23`/`$32`; copier-prefix absence/presence; and two allocation placements. Every case edits all legacy header fields, the custom `$28` timer, and sprite header, then proves checksum validity, semantic reopen, exact physical framing, byte-exact Undo/Redo, and identical logical output across header variants. `rust_custom_time_and_support_patch_b_are_applied_in_snes9x_gameplay` now boots the edited ROM through an official Snes9x libretro core, uses controller input to enter a level, requires game mode `$14`, verifies live custom timer digits `4/5/6` at WRAM `$0F31..$0F33`, and retains a bounded nonuniform gameplay frame. `rust_standard_time_music_and_sprite_headers_are_applied_in_snes9x_gameplay` independently removes command `$28`, sets ordinary time selector 3, music selector 7, sprite memory `$0B`, clears both buoyancy controls, and selects Layer 1 scroll mode 3. It requires live digits `4/0/0`, active song `$12` at WRAM `$0DDA`, sprite memory `$0B` at `$1692`, buoyancy flags `$00` at `$190E`, and scroll runtime `$00/$00` at `$1411/$1412` through the same input/render/state boundary; the paired mode-2 observation is `$01/$02`, proving those bytes distinguish the selected scroll behavior. This tuple differs from the prior live `$03/$12/$C0` observation, proving the edited level was entered rather than accepting coincidental vanilla defaults. The retained Lunar Magic 3.63 Wine suite independently edits and reopens every legacy header field and exhausts all twelve reserved modes with adjacent-color preservation; the renderer covers mode/palette/tileset consequences, while the discriminating Snes9x state covers timer/music/sprite/scroll runtime. Together these close the aggregate Oracle gate. |
| Level | Edit primary/midway entrances | P | P | P | P | P | Pass | Native form and separate-midway runtime exist. The direct-ROM Wine gate independently edits and reopens the four pristine main-entrance planes through Lunar Magic 3.63 for headerless and authentic canonical-header physical images, proves Rust preserves either input prefix exactly, proves Lunar Magic preserves its canonical header and adds that exact 512-byte header when opening the headerless form, requires a valid checksum in both cases, and compares every exported main-entrance field plus the coupled original Layer 2 scroll index. Packed MWL and installed/first-install separate-midway Wine gates remain passing. `main_and_separate_midway_edits_match_every_supported_rom_and_storage_variant` completes the supported 48-case product across SMW-J, SMW-NA, and All-Stars+World-NA identities; LoROM `$20`/`$30`, SA-1 `$23`, and ExLoROM `$32`; copier-prefix absence/presence; and default/relocated main-table plus installed midway-helper/table placements. Every case updates all four main planes, the coupled Layer 2 scroll nibble, and all four separate-midway planes, semantically reopens both stores, repairs the checksum, preserves the exact prefix, restores/reapplies exact physical bytes with Undo/Redo, and produces identical logical output across header variants. This matrix also exposed and guards the shared installed-pointer decoder: only LoROM may discard address bit 23 as a mirror; ExLoROM and SA-1 retain mapper-significant high banks. `original_main_midway_dialog_applies_reopens_and_cancels_losslessly` closes the original-dialog gate: it drives all main and separate-midway groups through recovered command `$2524`, retains the configured live dialog, proves Cancel leaves the complete pristine ROM byte-identical, applies with OK, validates the exact seven main bytes and four midway bytes through Lunar Magic's own MWL exporter, checks the repaired ROM checksum, reopens the dialog, authenticates its persisted checkbox/combo state, and proves the final Cancel is mutation-free. |
| Level | Edit screen exits | P | P | P | P | P | Pass | Typed fields and canvas route exist. The recovered command-zero setter preserves the unrelated advance-screen bit, always sets Lunar Magic's required packed `$0400` flag, and canonically chooses the four-byte parameter-0 or five-byte parameter-2 representation from the resulting high nibble. The native semantic form now reloads the committed record immediately, so a requested `$0000` visibly becomes canonical `$0400` and compact/extended shape changes update the raw record instead of leaving stale controls; out-of-range source screens reject without changing the selected record. `vanilla_level_editor::tests::applied_screen_exit_form_reloads_canonical_flag_and_encoding_shape` covers the interaction boundary. A real Lunar Magic 3.63 four-cycle import/re-export gate covers source screens `$00/$1F`, requested destinations `$0000/$0FFF/$1000/$FFFF`, both shapes, and exact full-stream equality. `level_save::tests::screen_exit_edits_reopen_and_undo_across_every_mapper_and_copier_variant` covers LoROM/Fast-LoROM's shared pointer mapping, SA-1, and ExLoROM with both copier-header forms and both compact and extended encodings; every result semantically reopens, preserves the exact physical prefix, and undoes byte-exactly. Together with the retained four-cycle Lunar Magic oracle, this completes the supported variant matrix |
| Level | Edit secondary exits and clear operations | P | P | P | P | P | Pass | The complete 8,192-entry backend and native GUI expose staged edit, clear-current, and confirmation-gated clear-all operations. Clear-current requires the selected index to be loaded and changes exactly that entry; clear-all replaces the complete staged table with native zero records, reloads the selected form, and rejects stale confirmation delivery. Neither operation touches the ROM before the existing revision-checked atomic commit, and dirty close remains guarded. Compact pristine installation uses the detected source fixed planes as transactional preconditions, so clearing or importing changes within those planes now installs and semantically reopens instead of rejecting the intended replacement. Ghidra's `ImportBinaryMwlLevelFile` proves the original canonicalization boundary: process at most 8,192 records, skip `$2000+` indices, let the last duplicate valid index win, and ignore byte 7. Standalone Rust MWL decode/encode remains lossless; installed import applies those exact rules. The live `lunar_magic_imports_reexports_and_clears_secondary_exit_boundaries` gate imports `$0000` and `$1FFF` with minimum/maximum packed fields plus a duplicate, nonzero reserved bytes, and an invalid `$2000` record, requires the exact canonical Lunar Magic re-export and direct installed-table reopen, then imports an empty set and requires both records to clear with a valid checksum. `binary_mwl_secondary_exit_overlay_skips_bad_keys_and_uses_last_duplicate`, `binary_mwl_secondary_exit_overlay_obeys_original_record_count_cap`, `rom_secondary_exit_editor::tests::clear_entry_requires_loaded_selection_and_changes_only_that_entry`, `rom_secondary_exit_editor::tests::clear_all_stages_one_complete_zero_table_and_reloads_selection`, and `secondary_exit_install::tests::compact_install_can_replace_pristine_fixed_planes` cover the native routes. `edit_and_clear_match_every_supported_mapper_header_and_storage_variant` completes the 48-case product across three supported identities, LoROM `$20`/`$30`, SA-1 `$23`, ExLoROM `$32`, copier-prefix absence/presence, and both installed compact fixed-prefix and all-tagged storage. Each case edits a bounded record, grows/repackages when required, clears all 8,192 entries as a second transaction, semantically reopens both states, repairs the checksum, preserves framing, and restores/reapplies the exact original/intermediate/final physical images through two Undo/Redo steps. Mapper round-trip tests additionally prove installed pointer publication no longer discards ExLoROM/SA-1 high-bank state. `original_secondary_exit_dialog_applies_clears_rejects_and_cancels_losslessly` closes the original-dialog boundary through command `$2525`: it retains the configured `$1FFE` dialog, applies and reopens every packed field, verifies the exact six-plane record and ROM checksum, proves Cancel restores the temporary table and leaves the complete ROM byte-identical, drives Clear Slot, retains the original “Really clear all slots?” warning, proves No preserves the saved ROM, and proves Yes clears all 8,192 entries. The live clear-all result also exposed Lunar Magic's zero-length compact representation; Rust now accepts and emits four fixed zero planes plus two null reader pointers without inventing one-byte RATS owners. |
| Level | Insert/edit/delete/move standard and extended objects | P | P | P | P | P | Pass | Authenticated object canvas works. Command-zero parameters `$04+` are positioned extended objects, not controls: the shared stream insertion/relocation model and both Layer 1 and object-backed Layer 2 canvases now accept them while `$00–$03` controls remain rejected. `positioned_command_zero_extended_objects_insert_and_relocate`, `canvas_accepts_positioned_extended_objects_but_rejects_command_zero_controls`, and the expanded `primary_canvas_places_and_drags_object_backed_layer2` cover that boundary. Expanded-ROM allocation recognizes Lunar Magic's zero-filled free space while preserving `$FF`-only pristine policy; `expanded_zero_fill_is_available_for_layer1_growth` requires RATS-tagged publication. The ignored `external_lunar_magic_rom_object_insertion_grows_only_layer1_and_undoes` gate inserts both a standard object and a command-zero extended object in authentic modified level `$102`, semantically edits the extended selector, requires isolated Layer 1 repointing with exact sprite/neighbor pointer preservation, reopens in Rust, and requires Lunar Magic 3.63 to export exactly the baseline-canonicalized stream plus both insertions; checksum and byte-exact undo pass. Its companion `external_lunar_magic_rom_object_move_and_delete_round_trip_exactly` relocates an existing object through the native canvas, deletes another through the native action path, requires RATS-owned isolated Layer 1 publication with exact Layer 2/sprite/neighbor preservation, reopens the complete Rust value, and requires Lunar Magic to reproduce the same canonical relocation and removal; checksum and byte-exact undo also pass. Native command-zero screen jumps retain their recovered low-first or high-first encoding and expose the two actual bounded encoded components (five-bit first, four-bit second). Live horizontal and vertical imports prove the low-first horizontal `$1B0`/`$200` layout-stride mapping and the equal-stride high-first vertical mapping; placement, extent, relocation, rendering, and canvas sizing now share that interpretation while exact packed editing remains lossless. The maximum-component plus advance oracle additionally proves five-bit primary-cursor wrapping, screen `$11` visibility, and byte-exact opaque-segment retention. Lunar Magic's shared `$3800`-word render cache now has the exact recovered secondary-layer partition: ordinary modes begin at `$1B00`, while mixed/vertical modes `$05`–`$08`, `$0A`, and `$0D` begin at `$1C00`; the live-cache diagnostic consequently includes object-backed vertical Layer 2 instead of suppressing it. `screen_jump_components_bound_every_valid_value_and_preserve_encoding_order`, `screen_jump_components_follow_native_layout_strides_and_storage_orders`, `applied_screen_jump_form_reloads_the_original_encoding_and_exact_target`, and `recovered_orientation_and_auxiliary_flags_cover_every_mode` cover these boundaries. Ghidra `HandleObjectModeRightButtonDown` (`0048AB00`) and `DuplicateSelectionOrCreateObjectAtPosition` (`00438FB0`) prove the original unmodified right-click clone-and-relocate gesture. The canvas now applies that source-preserving gesture to the active Layer 1 or object-backed Layer 2 selection; `right_click_duplication_repositions_objects_and_sprites_without_removing_sources` binds insertion, target cell, and selected-clone state. Recovered `$245D` and `$245B` establish Select All and selected-group Delete, while `DispatchLevelEditorKeyboardShortcut` proves bare Insert routes through `$245A` to place one active template at the cursor and Ctrl+Delete routes through the distinct destructive `$245C` command. Ctrl+A selects every positioned Layer 1, object-backed Layer 2, or sprite record while excluding controls; Delete is atomic across the aggregate, and Insert now places one active template at the current canvas pointer. `object_select_all_shortcut_excludes_controls_and_group_delete_is_atomic`, `insert_shortcut_places_one_active_object_template_at_the_pointer`, and the existing group transaction tests cover Rust. The ignored `lunar_magic_select_all_deletes_every_positioned_object_and_preserves_controls` gate proves aggregate selection/delete through Lunar Magic's own MWL exports. `lunar_magic_ctrl_selection_right_duplicates_and_drags_the_complete_object_group` additionally authenticates two Ctrl-selected level-$105 objects through live selected-count memory, right-duplicates and drags the aggregate, and requires Lunar Magic's complete before/after stream to equal Rust exactly. That comparison exposed and fixed clone serialization: duplication now preserves every pre-existing screen-jump and advance byte instead of canonicalizing unrelated transitions. Original and end-to-end gesture coverage are complete. `object_edits_reopen_and_undo_across_every_supported_identity_mapper_header_and_storage_variant` closes the supported 48-case product across SMW-J, SMW-NA, and All-Stars+World-NA; LoROM `$20`/`$30`, SA-1 `$23`, and ExLoROM `$32`; copier-prefix absence/presence; and two independent tagged storage placements. Every case inserts standard and command-zero extended objects, edits the extended selector, relocates and deletes objects, preserves the sprite pointer and complete sprite stream, repairs the checksum, semantically reopens the exact level, preserves physical framing, and restores/reapplies the exact physical image through Undo/Redo. Headered and headerless cases produce byte-identical logical ROMs |
| Level | Insert/edit/delete/move standard and custom sprites | P | P | P | P | P | Pass | Standard dispatch/rendering and declared custom-record widths are verified. Every accepted raw or semantic replacement reloads the selected token from staged controller state. Legacy semantic publication always applies Lunar Magic's stable screen sort, preserves within-screen priority, and leaves malformed record indexes intact for typed errors. `lunar_magic_sorts_raw_legacy_sprite_records_on_export` proves with a direct out-of-order ROM write that this is a load/export invariant even without an editor gesture; `semantic_legacy_sprite_position_edit_sorts_and_tracks_the_selected_record` covers selection tracking after an edit. Expanded semantic publication now applies the exact orientation-aware comparator across ROM, MWL, legacy-MWL, native-level, controller, and import surfaces: screen then resolved upper-Y band, plus the vertical low-Y-nibble tie-breaker, with stable same-key priority and minimum transitions. `lunar_magic_sorts_raw_expanded_sprite_records_on_export` proves this is likewise a load/export invariant using a directly injected out-of-order expanded ROM; raw decode/encode remains lossless. Installed-canvas expanded drag and placement use that same model, preserving record identity/extensions while selection follows reordered records and removed controls. Semantic saves reconstruct upper-Y state from records, removing leading `$FF 00`, repeated transitions, ignored `$FF 80..FD`, and trailing state; the live `lunar_magic_canonicalizes_expanded_sprite_controls` oracle covers all four shapes together. `expanded_sprite_canvas_edits_rebuild_controls_commit_reopen_and_undo` proves both canvas routes through ROM reopen and exact undo. The built-in editor resolves Lunar Magic's installed per-level sprite-bank table instead of combining the selected level with a stale pristine shared-bank stream; `builtin_editor_resolves_lunar_magic_per_level_sprite_banks` proves the installed route. Nonshared commits now serialize only changed domains; `sprite_only_nonshared_commit_preserves_layer1_pointer_and_payload` proves sprite growth cannot needlessly relocate Layer 1. The ignored `external_lunar_magic_rom_sprite_edit_saves_reopens_and_undoes` gate moves and inserts sprites in authentic modified level `$102`, requires growth to relocate only that level's split sprite pointer while its Layer 1 pointer and neighboring level remain exact, reopens Rust's complete sorted stream, and requires Lunar Magic 3.63's before/after exports to reproduce the complete grown sprite aggregate; checksum repair and byte-exact logical undo pass. `lunar_magic_exports_rust_installed_expanded_sprite_framing` moves an authentic level-`$105` record to screen `$10` and upper-Y band 2 with an escaped `$FF` byte, proves Lunar Magic retains sprite-header bit `$20`, emits `$FF 02`/`$FF FF` and expanded termination while its binary MWL container flags stay zero, matches Rust's complete comparator-derived order, and reopens Rust's exact checksum-valid ROM stream. `lunar_magic_matches_vertical_expanded_sprite_ordering` dynamically discovers an authentic vertical level, places two same-screen records in upper band 2, forces a low-Y-nibble reorder, and requires Lunar Magic's complete stream to match Rust exactly. The authenticated level-$102 comparison renders all three `$47` previews, and its retained eight-phase gate selects phase 2; after declaring exactly 3,824 pixels occupied by Lunar Magic-only screen-number and entrance overlays, all 290,064 compared artwork pixels are within one channel step (2,011 one-step differences, zero above one, MAE 0.002311). `compare-level-render-audit.test.mjs` proves the bounded overlay-mask parser, and `raw_sprite_replacement_reloads_committed_semantic_fields` covers raw replacement; complete dialog parity remains |
| Level | Layer 2 object-backed editing | P | P | P | P | P | Pass | Shared history/canvas tests pass. The SMW-US profile classifies all four recovered Layer 2 runtime generations: exact `LM $0103` selects the current descriptor layout, while hook opcodes `$A9`/`$4B`/`$5C` identify legacy formats `$100`/`$101`/`$102`. Unknown `LM` versions reject instead of being written through the wrong layout. All three legacy formats now migrate transactionally through the application and native built-in-runtime UI: the complete Layer 1 table is retained as a precondition, all 512 Layer 2 pointers and descriptors follow the recovered migration loops (including the shared `$100`/`$101` descriptor-flag normalization), the exact 64-byte `$103` hook is installed, checksum/history commit once, late changes reject atomically, and undo restores the source. The `$100` hook is authenticated in full from its recovered hard-coded `LDA #$05 / PHA / PLB` data-bank sequence and shared `$101` tail; the `$102` result matches the authentic Lunar Magic 3.01→3.63 oracle byte-for-byte; and the `$101` hook is independently identical in Lunar Magic 1.31–1.65 binaries, an official 2001 Demo World patch, and a period community patch. The opt-in `layer2_wine::lunar_magic_reexports_rust_object_backed_layer2_edit` gate discovers a real object-backed level in the installed fixture, changes an ordinary object's coordinate through the typed Rust model, saves/reopens it with a repaired checksum, and requires Lunar Magic 3.63 to export the byte-exact expected Layer 2 payload. The ignored `external_lunar_magic_rom_layer2_object_insertion_is_isolated_and_undoes` gate discovers object-backed Layer 2 in an authentic current-runtime modified ROM, inserts through the native canvas, requires only that level's Layer 2 pointer to move while its Layer 1, sprites, and neighboring Layer 2 pointer remain exact, reopens the complete staged stream in Rust, and requires Lunar Magic 3.63 to export exactly the baseline-canonicalized stream plus the insertion; checksum repair and byte-exact undo pass. Runtime migration routes are covered by `layer2_runtime_install::tests::format_100_hook_and_descriptor_flags_migrate_transactionally`, `layer2_runtime_install::tests::format_101_hook_and_descriptor_flags_migrate_transactionally`, `layer2_runtime_install::tests::external_lunar_magic_301_migration_matches_363_tables_and_hook_exactly`, and `built_in_runtime_installer::tests::layer2_runtime_selection_migrates_authentic_format_102_and_undoes_exactly`; `every_legacy_generation_migrates_across_both_copier_header_variants` explicitly covers formats `$100`, `$101`, and `$102` through exact `$103` migration under absent and byte-preserved present copier headers, including generation-specific descriptor conversion, complete physical-image Undo, and Redo; the retained current `$103` editor oracle completes the supported SMW-US LoROM generation/header cross-product |
| Level | Layer 2 background tile editor, rectangle tools, undo | P | P | P | P | P | Pass | Ghidra-proved two-plane mapping is shared by the installed canvas, clipboard, renderer, and ROM transaction. `complete_canvas_gesture_sequence_composes_every_rectangle_tool` drives click/Shift-click selection through one live editor state, then composes full-word rectangle fill, visual row-major copy/paste, move, edge resize, cut, captured-pattern flood, ordinary flood, endpoint updates, and selection reset while asserting Lunar Magic's distinct 12-bit normalization boundaries for resize and flood. Focused tests cover every edge rejection, selection-scoped remap, and all binary topologies; controller gates prove one-history-step edits, checksum-valid save/reopen, failure atomicity, and exact undo. The retained real-process `lunar_magic_reexports_rust_checksum_atomic_layer2_tilemap_edit` gate passed on 2026-08-06, requiring Lunar Magic 3.63 to re-export the Rust-edited installed tilemap exactly. Renderer regressions cover native 32×32 plane wrapping and installed composition. |
| Level | Layer 3 settings, graphics, tilemap, remap commands | P | P | P | P | P | Pass | Wine import/re-export evidence exists. Shared Layer 3 visibility governs the primary canvas, installed preview, and image exports; authenticated SMW-US planes preserve priority, start offsets, repetition, special between-layer order, and additive composition. Custom installed tilemaps materialize the recovered all-`$38FC` workspace, packed GFX/ExGFX range, and `$07F` sentinel through the active profile. Both installed level workflows now expose semantic enable, GFX/ExGFX file, length-selector, destination-selector, and exact 32-bit expanded-mode controls. The portable MWL native editor exposes that complete setting through the same typed transaction, and its backward-compatible `LMMWLL31` specification optionally carries `expanded-mode`. The standalone exact-record native editor now also exposes the complete proven Layer 3 fields, all ten Super GFX Bypass files, and sprite-boundary interaction without requiring raw-word edits. The focused installed-record window now exposes that identical semantic surface with independent stale-revision rejection, canonical staging reload, checksum-repaired commit, reopen, and exact ROM undo. `LMXSETED1` exposes validated tilemap, `layer3-mode`, and `super-gfx` automation alongside raw-word compatibility. Exact bit-mask ownership permits their disjoint fields to compose across shared words while rejecting mode/boundary and intersecting raw collisions before mutation. Aggregate edits preserve every unowned expanded-settings byte and share history, checksum repair, reopen, and ROM undo. The expanded record resolves the exact mode value's signed editor-row offset, level/tileset gates, twelve-row bias, and row-30 clamp. Installed rendering applies that state: expanded offsets supersede the vanilla editor position and clamped modes repeat row 30's two 8x8 source rows for every later 16-pixel target cell. The complete 32-entry primary-source, alternate-source, and composition tables were captured from live Lunar Magic 3.63 memory. The typed dispatcher reproduces its five painter slots, packed-bit 30/31 overrides, and legacy-header low/high Layer 3 priority split; installed rendering consumes that order plus exact Layer 3 additive and source-half-color state. Layer 1/2 slot addition now saturates ordinary pixels and halves averaged sources before addition while composing only the final overwritten Map16 cache cell. A retained 200-case live-process oracle covers all valid modes, both legacy priority states, and all four expanded bit-30/31 combinations byte-for-byte. `rom_level_assets_editor::tests::installed_layer3_raster_honors_priority_addition_and_plane_wrapping`, `installed_layer3_raster_halves_source_before_composition`, `installed_layer3_priority_split_uses_the_recovered_painter_slots`, `native_level_raster::tests::whole_layer_addition_saturates_and_halves_the_averaged_source`, `whole_layer_addition_composes_only_the_final_overwriting_cell`, `level_layer_slots::tests::every_valid_mode_and_expanded_route_matches_the_retained_live_slot_arrays`, and `retained_lunar_magic_custom_layer3_descriptor_loads_its_rom_graphics_file` cover the semantic and rendering boundaries. `layer3_edit_reopens_and_undoes_across_every_supported_identity_and_table_variant` executes the complete supported identity product—SMW Japan, SMW North America, and All-Stars+World North America; LoROM, Fast LoROM, SA-1, and ExLoROM map modes; present/absent copier headers; and default/relocated profile-described expanded-settings tables. Every one of the 48 combinations commits Layer 3 tilemap and expanded-mode settings, semantically reopens them, validates the checksum, preserves the exact physical prefix, undoes to the byte-exact source, redoes to the byte-exact result, and produces identical logical output across copier-header forms. Legacy expanded-settings generations are edit-compatible after their separately authenticated atomic migrations to the current table layout; unsupported profile shapes reject during layout validation rather than borrowing SMW-US offsets. Together with the exhaustive live mode oracle, this closes the supported Layer 3 variant product. |
| Level | Per-level palette editing and import/export | P | P | P | P | P | Pass | The installed native-level-assets editor boundedly imports and create-new exports its complete staged palette as portable `.lmpal`, exact 257-word raw, version-2 TPL, or RGB24 without blocking the UI. Raw/TPL/RGB imports automatically read an optional same-basename `.palmask`; raw uses the authenticated 257-word operation, while TPL/RGB use the retained installed `[0],[2..256]` to supported `[0..255]` mapping and preserve installed backdrop word 1. Selected row-zero entries receive the recovered clearing behavior. Detected RGB expansion is retained for reciprocal export, and full exports recoverably remove a stale mask sidecar. Every import routes the resulting complete palette through the aggregate controller's immutable ownership map, so fixed and ExAnimation-owned changes reject atomically while source-level provenance permits intentional cross-level copies. Loading gates aggregate edits, commit, and close; export snapshots staged colors and file persistence gates commit and close. `native_level_assets_controller::tests::complete_palette_replacement_is_slot_agnostic_shape_checked_and_atomic`, `rom_level_assets_editor::palette_transfer::tests::native_level_palette_helpers_round_trip_exact_words_and_provenance`, `rom_level_assets_editor::palette_transfer::tests::supported_transfer_uses_exact_installed_mapping_and_selected_row_zero_clearing`, and `rom_level_assets_editor::palette_transfer::tests::authentic_import_helpers_are_shape_checked_and_failure_atomic` cover the native route. `per_level_palette_edits_reopen_and_undo_across_every_supported_identity_mapper_header_and_storage_variant` closes the ROM portability product across SMW-J, SMW-NA, and All-Stars+World-NA; LoROM `$20`/`$30`, SA-1 `$23`, and ExLoROM `$32`; absent/present copier prefixes; and two independent tagged palette placements. All 48 cases edit boundary and cross-row colors in the complete 256-color aggregate, repair the checksum, semantically reopen exactly, preserve physical framing, restore/reapply exact physical images through Undo/Redo, and produce byte-identical logical images across header forms. The existing format tests separately cover `.lmpal`, 257-word raw, TPL, RGB24, and `.palmask` permutations. The retained isolated-Wine gate `original_lunar_magic_level_palette_transfer_formats_masks_and_auto_enable_match` drives recovered level-editor commands `$239F/$23A0` against the SHA-verified Lunar Magic 3.63 executable and pristine headered SMW-US ROM. It proves exact 768-byte RGB, `TPL` version-2, and 514-byte raw exports; row-zero substitution; automatic optional 257-byte `.palmask` discovery and reciprocal publication; selected-entry mutation with every unselected color preserved; automatic custom-palette enablement; and malformed-TPL rejection. The rejection oracle also records the original's transient selector reset while colors remain unchanged; Rust intentionally provides the stronger failure-atomic selector boundary. Ghidra's `PromptAndSavePaletteFile`, `PromptAndLoadPaletteFile`, `SavePaletteToSupportedFile`, and `LoadPaletteFromSupportedFile` close command, prompt, extension, legacy-import, conversion, and selector control flow, while immutable fixed/ExAnimation ownership remains covered by Rust's atomic controller gates. |
| Level | Per-level and global ExAnimation editing | P | P | P | P | P | Pass | Semantic Wine MWL fixture exists. Installed preview now executes ROM-global records before staged per-level graphics, palette, rotation, and fixed-color transfers against one recovered mutable cache with absolute frame progression and independent trigger-bank state. Ghidra's `ResolveGraphicsRuntimeDataOffset` (`0045C790`) proves the global pointer is split: its bank byte resides at runtime `+$5C` and its low word at `+$65`. The global provider combines those operands, distinguishes absent/empty/present storage, and boundedly decodes the compact allocation. That same split operand pair is a checked copy-on-write save target with compact canonicalization, allocation limits, optional exact-owner reclamation, checksum repair, reopen, and byte-exact undo; allocator policy protects both noncontiguous components and refuses to invent an absent runtime or accept a layout without the authenticated locator. `retained_lunar_magic_runtime_uses_split_empty_global_pointer_and_accepts_save` opens the actual Lunar Magic 3.63 installation fixture, resolves its `$600` per-level table, recognizes the real split zero pointer as empty, allocates the first global animation, semantically reopens it, and restores the exact physical ROM with one Undo. The installed ROM editor switches explicitly between the selected level domain and the global 32-record domain, exposes the same setting/header/trigger/record/frame/clipboard controls for both, retains exact prior ownership for checked reclamation, and prevents target switching while the active domain has uncommitted edits. Composite scheduling enforces the 32-slot global limit and Lunar Magic's 64-to-32 level-slot reduction when globals are active. `level_and_global_edits_match_across_every_supported_identity_and_runtime_variant` proves the complete supported 48-case product: SMW-J, SMW-NA, and All-Stars+World-NA identities; LoROM `$20`/`$30`, SA-1 `$23`, and ExLoROM `$32` map modes; exact copier-prefix absence/presence; and both default and relocated installed runtime/pointer-table placements. Every case edits and semantically reopens both the selected per-level set and the installed global set, validates the repaired checksum, preserves the physical prefix exactly, produces identical logical output across header variants, restores the exact original physical ROM with two Undo operations, and reapplies the two exact intermediate/final states with Redo. Legacy runtime inputs are covered by authenticated migration into this supported compact installed representation or rejected as unsupported profiles rather than being silently reinterpreted. |
| Level | Change bypassed GFX/ExGFX and animation options | P | P | P | P | P | Pass | Ghidra-proved expanded-header mapping now has a lossless typed model and controls for six FG/BG plus four SP 12-bit files in both the portable and installed native-level-assets editors. Installed edits use the checked undoable aggregate transaction and preserve unknown bits. A profile-backed resolver decodes all ten selected files at native 2/3/4bpp depths and materializes fixed VRAM slots. With bypass disabled or expanded settings absent, the installed preview instead resolves SMW-US revision-0's active four-file object and four-file sprite tileset tables and pads the two unused FG/BG slots; unsupported profile families reject rather than borrowing those fixed tables. Installed tables with entries `$32/$33` also provide relocated player/animation and animated-display sources, while pristine tables use the recovered fixed startup pointers; a retained equivalence gate covers both routes. The live installed-assets workspace rasterizes tilemap Layer 2 plus recovered-family Layer 1 and object-backed Layer 2 streams from either graphics source together with profile Map16 definitions and the staged palette, at mode-specific editor dimensions; object streams supply sparse per-object final-cell writes, omit initialized blanks, and retain cross-object same-cell overwrites in painter order. Each placement carries opaque, exact destination-average, or source-only half-color composition: tileset-4 Map16 `$027`–`$02A` selects Lunar Magic's channel-floor average for object and tilemap sources, while recovered modes `$0C`/`$0D` half-color Layer 2 background pixels independently of the destination. Its typed per-layer palette routing reproduces tileset-3 object-backed Layer 2's low-row-plus-four rule without changing tilemap Layer 2 or Layer 1. Every accepted aggregate edit regenerates staged state, and the shared exact viewport adapter supplies a bounded draggable 512×448 camera at 50–400% rational zoom with center-anchored combo changes, pointer-anchored Ctrl/Command-wheel steps, a world-aligned optional Map16 grid, and click-selected Map16 cells with a shared marching overlay. The selected cell inspector preserves all staged Layer 2/Layer 1 placements in painter order, exposes words, composition, and bounded acts-like chains with typed failures, and decodes Map16 visual/source quadrants plus tile/encoded-palette/effective-CGRAM/priority/composed-flip fields. It also lists overlapping authenticated sprite parts with full provenance plus decoded tile/page/palette/priority/flip fields. Its shared 60 ms clock drives the recovered eight-phase GFX32/GFX33 tile interpreter and eight-color Dragon Coin palette cycle independently according to the two staged per-level switches; selection-only animation is isolated from both asset switches. Retained-ROM evidence proves adjacent tile phases differ, and focused tests prove palette mutation is confined to CGRAM `$64`. Authenticated standard-sprite previews are composited from the four selected ordinary SP slots, the separate GFX33 animated display page, and staged sprite palette rows. The ten-file bypass now also has a strict semantic `super-gfx` automation and first-class aggregate-controller route with exact shared-bit composition, installed/portable persistence, checksum/reopen, and undo evidence. The four inverted animation-feature bits now have named lossless semantics, MWL checkbox/script routing, strict `LMEXFT1` aggregate automation with a real profile-qualified mixed-shell gate that preserves the unrelated low nibble, exact installed-ROM migration, runtime-relative table location, strict profile/allocation routing, installed checkbox/aggregate-save integration with checksum/reopen/undo coverage, and a live Lunar Magic export that recovers Rust-written byte `$5B` exactly. Installed global and per-level ExAnimation materialize all recovered graphics, palette, rotation, and fixed-color transfer families in the live preview while honoring their independent feature switches. `super_graphics_bypass_wine::lunar_magic_exports_all_rust_super_gfx_bypass_fields_in_both_states` validates the owned `$6E00` expanded-settings allocation before mutation, writes ten distinct available FG/BG/SP selectors with bypass both disabled and enabled, and requires Lunar Magic 3.63 to export every selector and the enable state exactly. The same live gate covers authentic headered and headerless ROMs, checksum repair, semantic reopen, byte-exact Undo/Redo, preserved copier metadata, and identical logical output across physical variants; model tests retain the complete 12-bit range and invalid-resource rejection boundary. |
| Level | Custom object library (`.mw0`/`.mw0t`/`.osc`) | P | P | P | P | P | Pass | Lunar Magic 3.63's extracted `info_custom_object_collection.htm` corrected the binary boundary: `.mw0` begins with five reserved bytes, then stores one-or-more-record collection entries separated by the first record's orientation-dependent new-screen bit. Ghidra-recovered `GetEncodedCustomObjectRecordSpan` proves the original tests mask `$90`, so horizontal `$80` and vertical `$10` both start a later group. Rust retains that header, decodes either boundary byte-exactly, removes only the horizontal separator from logical records, reconstructs the native orientation form canonically after insert/replace/remove/reorder, and rejects a programmatic later group member whose vertical bit would make Lunar Magic split it. The native form edits complete semicolon-separated groups, `LMCUSED1` accepts comma-separated groups, typed clipboard replacement preserves the remaining group members, and semantic observations expose the header, group counts, primary compatibility field, and every record. `native_group_boundaries_create_multi_object_entries_and_round_trip`, `either_native_orientation_screen_bit_starts_a_group_and_round_trips`, and `programmatic_group_rejects_an_ambiguous_vertical_boundary_member` bind the recovered format. `every_native_text_framing_variant_round_trips_exactly` proves the complete BOM absent/present × LF/CRLF × trailing-line-ending absent/present product. `every_selector_and_directive_mode_is_lossless_and_resolved` exhaustively covers all object types `$00`–`$3F`, both special index families, generic all-variant and individually selected variants zero through four, parameter endpoints, dimension and record-length boundaries, ordinary/alternate-linear selection, and all four OSC directives. The ignored `rust_multi_object_collection_reloads_renders_and_places_in_lunar_magic` gate authenticates the original executable and pristine ROM, reloads a Rust-authored grouped collection, selects its exact live description, synchronizes a window-specific compositor preview capture, places the group, and requires Lunar Magic's own before/after MWL exports to differ by exactly `[06 06 10]`, `[07 0E 10]` while every other semantic domain and the repaired checksum remain equal. The unoccluded 520×520 RGBA artifact is retained and hash-bound at `docs/oracle-work/lm363/pristine-us/custom-object-collection/preview.png`. Ghidra `PopulateCustomObjectTemplateList` proves the original publishes a description only after consuming LF; `lunar_magic_hides_a_custom_description_without_its_final_newline` requires the live picker to contain zero entries and the ROM to remain byte-identical, while `original_picker_hides_only_the_unterminated_final_description` binds Rust's lossless retained model to that original picker boundary. |
| Level | Custom sprite library (`.mw2`/`.mwt`/`.ssc`) | P | P | P | P | P | Pass | The size-table-bound paired `.mw2`/`.mwt` editor and lossless `.ssc` document/controller pass revision, persistence, undo/redo, malformed-input, and source-preservation gates. `every_selector_display_palette_and_remap_mode_is_lossless_and_resolved` exhaustively covers sprite numbers `$00/$FF`; ordinary/alternate and local/global selectors; all four extra-bit/index banks; zero/maximum width and height; absent, under-minimum, exact, maximum, and over-maximum record-length declarations; description, multi-tile display, and palette directives; signed coordinates; masked tile words; all four global tile-remap source modes/biases; and palette remapping. Every accepted source round-trips byte-exactly and resolves to the same selector/metadata/remap model. `rust_multi_sprite_collection_reloads_renders_and_places_in_lunar_magic` now loads a Rust-authored two-sprite `.mw2`/`.mwt` group plus explicit four-tile `.ssc` display metadata in authenticated Lunar Magic 3.63, selects the exact live custom row, retains and structure-binds the rendered `WindowSpriteViewx` preview, enters sprite mode through recovered command `$2459`, performs the picker-documented Ctrl+right-click, and proves Lunar Magic's own MWL exporter adds exactly `60 60 00` and `60 70 0F` while every other level domain remains equal and the saved ROM checksum remains valid. `lunar_magic_hides_a_custom_sprite_description_without_its_final_newline` binds Ghidra's LF-only list publication to the original empty custom picker and byte-identical ROM rejection boundary. Port-8089 functions `$005766B0`, `$005767F0`, `$00578EA0`, `$00579010`, `$0057C830`, and `$00498FA0` independently recover loading, category selection, preview, mode, and gesture semantics. |
| Level | Import/export one binary MWL level | P | P | P | P | P | Pass | Complete controller, shell, native level-assets actions, and live Rust export → Lunar Magic import/re-export oracle pass. MWL Layer 1 crosses the same `LoadLevelModeConfiguration` compatibility boundary as direct ROM opening: reserved modes `$12–$1D` stage as mode `$00` before orientation-dependent sprite/control canonicalization, preserve background color, and commit/reopen canonically through both single and directory-batch imports because both share `replace_modeled_assets_from_mwl`. The post-fallback Layer 2 storage contract remains enforced rather than writing an object stream through mode `$00`'s tilemap path. Native single-file import now blocks staged panel edits, undo/redo, Layer 2 reset confirmation, and commit while its worker is active and during the exact completion frame in which its ROM command has been prepared; this prevents a late UI edit from being silently omitted or stranded in a stale workspace. `level_import_workers_and_completion_frame_block_staged_edits` covers both timing boundaries. `installed_binary_mwl_import_is_identical_across_copier_header_variants` imports an authentic Lunar Magic export with a real semantic edit through profile-derived allocation, requires a non-empty revision-bound mutation, preserves the physical copier header exactly, repairs the checksum, and produces byte-identical logical installed ROMs from headered and headerless inputs. Together with `installed_worker_exports_all_512_levels_across_copier_header_variants`, this proves the installed import/export physical-ROM cross-product; `reserved_mode_mwl_import_canonicalizes_before_tilemap_storage_commit` covers compatibility ingress. |
| Level | Import/export legacy multi-file levels | P | P | P | P | P | Pass | Native two-stage bounded import and rollback-safe grouped export match the exact live level-105 `.mwl`/`.mw0`/`.mw1`/`.mw2` fixture and Ghidra `00477940`/`004796c0`. A second isolated Wine capture from the authenticated custom-palette ROM retains the complete five-file level-000 bundle, including the exact `0x202`-byte `.mw3` and 7,923 demonstrated secondary-exit rows through `$1FFF`; `authentic_custom_palette_legacy_bundle_matches_binary_level_semantics` proves its Layer 1, Layer 2, sprites, and palette agree with Lunar Magic's binary export and that destination palette state cannot leak into import. The capture corrected the current-format exit bound from `$0FFF` to Lunar Magic's demonstrated 13-bit namespace. Live prompt capture further proves a missing required `.mw0` rejects, while a declared-but-missing `.mw3` displays `File Missing!`, continues with the destination shared palette, clears the custom-palette flag, and emits no `.mw3` on re-export. A present short `.mw3` instead keeps the flag and overlays its bytes—down to an odd half-word—onto the destination palette; a 515th byte is ignored. Rust now makes only the palette worker read optional, exposes the original warning text, and reproduces missing, partial, empty, complete, and trailing-data semantics with a bounded read. `missing_declared_palette_falls_back_to_shared_palette_and_clears_custom_flag`, `present_partial_palette_overlays_destination_bytes_and_keeps_custom_flag`, `present_empty_and_trailing_palette_payloads_match_lunar_magic_reads`, and `missing_legacy_palette_surfaces_lunar_magic_fallback_warning` cover these layers. Both manifest and sidecar worker phases share the binary-import mutation gate: staged panel edits, undo/redo, Layer 2 reset confirmation, and commits remain disabled through the command-preparation frame, so neither phase can overwrite or strand a concurrent edit. `level_import_workers_and_completion_frame_block_staged_edits` covers the shared timing boundary. `custom_palette_publication_adds_exact_mw3_and_keeps_group_atomic` proves collision rollback and successful five-file publication. Port-8089 revalidation additionally proves fixed-position signature/version recovery, the `$0132` header/exit-width boundary, `$0341` Layer 2 normalization, level-number clamping, ignored malformed exit rows, and last-valid-duplicate behavior. Four retained full-window captures now cover the complete required-sidecar rejection family (`.mw0`, `.mw1`, `.mw2`) and the distinct optional-`.mw3` fallback. The capture process enumerated each live modal title, child-static message, and button before publication; `retained_legacy_import_dialog_captures_are_hash_and_structure_bound` binds all four PNGs to exact SHA-256 digests, dimensions, chunk order, CRCs, and RGBA framing |
| Level | Insert multiple levels from a directory | P | P | P | P | P | Pass | Shell and native progress dialog auto-target visible MWLs, skip hidden files, continue after failures, commit independently, and support Escape cancellation. Every native file read uses the shared bounded background loader instead of blocking the UI; completion is bound to the ROM revision captured at dispatch, and stale or post-cancellation results cannot prepare a commit. `installed_directory_import_variants_continue_after_failure_and_match_logically` sequentially imports authentic edited level `$000` and `$001` MWLs with a malformed file between them into installed headered and headerless ROMs. It proves two independent commits, failure isolation and continuation, exact physical copier-header preservation, repaired checksums, and byte-identical final logical ROMs. `next_file_starts_a_revision_bound_nonblocking_read`, `stale_or_cancelled_worker_completion_never_prepares_a_commit`, and `dispatch_acknowledgements_advance_exactly_one_file` cover the worker and coordinator boundaries. |
| Level | Export all levels to a directory | P | P | P | P | P | Pass | Shell and non-blocking native actions implement all/modified modes, exact `%03X` naming, grouped publication, Ghidra predicate, Wine-backed selection fixture, and cooperative Escape/button cancellation between levels with a final pre-publication cancellation gate. Untouched SMW-US ROMs use a dedicated built-in exporter instead of requiring installed palette/Lfix3/expanded-settings tables. Its complete 512-level output is semantically equal to Lunar Magic 3.63 for every header, Layer 1/2 stream, sprite stream, palette, secondary exit, optional section, and source field from both canonical headered and headerless ROMs; pristine modified-only output is empty. Installed-ROM all-level export now composes untouched slots through that proven vanilla path and modified slots through the installed aggregate. Format-$103 Layer 2 decoding accepts Lunar Magic's retained pre-migration `$361`-byte padded legacy backgrounds when an installed descriptor makes the original shared-background pointer direct. `installed_worker_exports_all_512_levels_across_copier_header_variants` proves all 512 installed outputs are byte-identical across headered/headerless inputs; `installed_worker_exports_only_lunar_magic_modified_selection` proves the exact one-level Wine-backed modified selection across both forms; and `worker_cancellation_never_publishes_partial_output` proves cancellation leaves no partial group. Together with `builtin_worker_exports_all_512_vanilla_levels`, `vanilla_header_variants_match_all_512_lunar_magic_mwl_semantics`, `vanilla_modified_mode_and_cancellation_match_lunar_magic_batch_boundaries`, and `installed_descriptor_accepts_pre_migration_legacy_padding_byte`, this covers the complete supported vanilla/installed × headered/headerless × all/modified cross-product. |
| Level | Export one/all level images as PNG/BMP | P | P | P | P | P | Pass | The installed native-assets editor exports the current staged level as create-new PNG or bottom-up padded 24-bit BMP using the full pre-viewport software canvas. Stored sizing reads the serialized five-bit last-screen header exactly, retaining deliberate empty tail screens; auto sizing independently recomputes the final visible object or sprite. The shared View menu independently controls Layer 1, Layer 2, Layer 3, and sprites in the primary editor, installed preview, and exported raster. Single and batch jobs snapshot those toggles together with the active animation phase and Special World view before background rendering. The non-blocking multiple-level actions match Lunar Magic 3.63's retained help and Wine behavior: a Unicode filename template produces `base %03X.ext`, modified-only defaults on and uses the authenticated expanded-ROM Layer 1 predicate, auto-set screen count defaults off, unrenderable levels are counted and skipped, and Escape/button cancellation occurs before all-or-rollback publication. Installed profiles now compose vanilla palette and empty ExAnimation state when an untouched slot retains the runtime's intentional empty pointers, while present custom assets remain installed-path data; SMW rendering consumes the same complete native Map16 model as the Map16 editor. `installed_full_level_variants_encode_identically_without_copier_header` covers PNG and BMP, stored and automatic sizing, modified level `$000`, padded-background level `$01C`, and vertical level `$109` across headered/headerless installed ROMs. `installed_modified_batch_publishes_only_the_wine_selected_level` proves exact modified-only selection and publication; `cancelled_batch_never_publishes_a_partial_image_group` proves the cancellation boundary. Together with the authenticated pristine 488-image corpus, full-level render audit, visibility/crop/Special World/animation tests, and `installed_untouched_level_composes_empty_palette_and_exanimation_slots`, this covers the complete supported vanilla/installed and physical-ROM variant boundary. |
| Level | Level usage analysis | P | N/A | P | P | = | Pass | Native background scan/progress/cancel and exact `LevelAnalysis.txt` framing pass; all four usage domains match Lunar Magic's authenticated 512-level working-ROM report with zero Map16 count deltas, and the same report is verified from canonical headered and headerless modified-ROM variants; unused-only filters and disabled sprite/music domains reproduce the corresponding filtered oracle-backed report (read-only workflow, so Tx is N/A) |
| Level | Restrict level access | P | N/A | P | P | P | Pass | Exact authenticated LM 3.63 LoROM and 8 MiB ExLoROM mutations, randomized protection material, native warning/title dialog, checksum preservation, and atomic rejection/undo pass. Live Wine plus port-8089 revalidation corrected the hidden dispatcher to command `$23A4` and proves mapper restriction is a substantive bulk migration, not a shifted fixed layout: descriptor indexes `$E0/$E1/$13B` own the per-save family, `$147/$146` own the bulk-save family, and `$112` anchors integrity metadata; standard, optional ExAnimation, and optional ExGFX pointers are all rewritten. The ExLoROM transaction now exactly reproduces all 314 changed original bytes: descriptor-routed hooks and bank `$81` helper, both base and `$400000` marker/title/version copies, 50 split pointers plus GFX32/GFX33, integrity records, two dynamic lower-window RATS relocations, three rebased references, allocator cursor, old-owner erasure, copier-header flag, and checksum compensation. `exlorom_restriction_matches_authenticated_bulk_save_and_undoes_exactly`, its headerless companion, dynamic collision test, and corrupt-owner atomicity gate cover exact output, framing, checksum, reopen, and Undo/Redo. Ghidra `HandleRestrictLevelAccessCommand` at `$00485050` and strings at `$005BA538`/`$005BA59C` recover the exact post-success order and copy: conditionally append a full restore point, offer standard command `$23BA` IPS creation against the already-written locked ROM, wait for that workflow, show completion, then close the ROM. `DecodeRomFeatureOptionFlags` at `$004AD740` and its sole caller `SynchronizeApplicationSettingsRegistry` prove the restore gate is packed application-settings bit 0, not ROM state. The native state machine persists that independent preference, publishes the locked ROM before capturing its timestamped full `.lrp` record, blocks IPS creation until the checkpoint succeeds, retains retries on persistence failure, and requires final save acknowledgement before close. `level-access-restriction-workflow-363.md` retains the address-level evidence and focused workflow tests cover restore, IPS completion/cancellation, and persistence ordering. The SA-1 route now matches an authentic SA-1 Pack v1.40 Lunar Magic 3.63 output byte-for-byte, including seven guarded bulk-resave prerequisite regions, base-descriptor hooks and protected data, non-mirrored metadata, checksum compensation, and exact Undo/Redo; `level-access-restriction-sa1-363.md` retains provenance and the opt-in complete-ROM gate. Ghidra `BuildRestoreArchiveFilename`/`OpenOrCreateRestoreArchive` correct the final assumption: the archive is deterministically `sysLMRestore/<ROM stem>.lrp`, while `OrigROMn` is only a later original-ROM fallback. Rust now creates or atomically appends that exact associated archive, selects `smwOrig.smc`, `smwjOrig.smc`, or `AllWorldOrig.smc` by supported identity, validates and reuses the project-local original without a chooser, and publishes a missing user-selected original under the canonical name. `associated_paths_match_lunar_magics_restore_directory_and_variant_names` and `restriction_restore_uses_and_reuses_the_automatic_associated_archive` cover all names, initial creation, repeated full append, exact reconstruction, and no-chooser reuse. |
| Level | LMSW/emulator level testing integration | P | N/A | P | P | P | Pass | The native Tools → Test ROM in Emulator workflow snapshots the exact current in-memory physical ROM revision, including unsaved committed edits and its copier header, into a private create-new workspace and passes that path directly to either a chosen emulator or a persisted shell-free `LMTOOLS1` tool. Configured tools may also consume the selected level through `{level_hex}` or `{level_dec}`. The approval dialog exposes the executable, working directory, and every independent argument before launch. Execution is non-blocking; Stop, frontend shutdown, process-start failure, nonzero exit, signal, denial, and normal exit terminate/reap owned processes where applicable and remove the staged ROM. The backend-independent `EmulatorSessionState` now exactly reproduces Ghidra-recovered Lunar Magic 3.63 pause aggregation: level-transition `$01`, manual `$02`, viewport `$04`, input `$08`, main-window `$20`, and editor-mode `$40` reasons independently select hard pause mode 2; focus-only state selects soft mode 1; no reasons select running mode 0. It suppresses inactive/duplicate updates, clears all state on stop, and establishes manual hard pause before the first single-frame step. The versioned `LMEMU001` process transport losslessly and boundedly carries immutable ROM revision, selected level, sprite stream, flags, pause/step/stop commands, signed viewport state, capabilities, active state, RGBA frames, and diagnostics. Nine focused tests bind masks, precedence, lifecycle, action ordering, every command/event round trip, every truncation, trailing bytes, unknown tags, invalid scalar values, UTF-8, geometry/raster consistency, and variable-input bounds. `state::tests::tools::emulator_test_captures_current_revision_level_context_and_physical_rom`, `state::tests::tools::emulator_test_requires_a_level_and_explicit_rom_argument`, `external_tool_launcher::tests::emulator_request_stages_exact_rom_and_denial_removes_workspace`, and `external_tool_launcher::tests::stopping_emulator_reaps_process_and_removes_workspace` cover the application/native boundaries; existing opt-in Snes9x smoke tests cover generated-ROM initialization. This read-only workflow has no ROM transaction gate. A concrete backend process, direct selected-level injection, native control wiring, live refresh triggers, viewport overlay, input-driven gameplay assertions, and broader emulator/platform oracles remain missing |
| Map16 | Browse/edit subtiles, attributes, Acts Like, undo | P | P | P | P | P | Pass | Native editor loads and atomically commits all 256 foreground/background pages. Both the profile-backed and pristine/native installed-ROM controllers now route replacement, subtile, and Acts-Like edits through the same bounded complete-graph validator: dangling targets, multi-node cycles, duplicate replacements, malformed sets, and exhausted caller traversal limits reject failure-atomically, while background replacements canonicalize their non-semantic Acts-Like field to zero. Its installed-ROM workspace has a bounded 100-snapshot staged undo/redo history around manual fields, typed clipboard paste, legacy page-pair import, and complete structured import. Each accepted mutation records one exact complete-set predecessor, no-ops record nothing, undo/redo restore through the same validated controller, monotonic staged revisions invalidate pending clipboard delivery, and divergent edits clear redo; ROM commit remains one application-level undo transaction. Typed single-tile clipboard paste across the installed-ROM, standalone-page, and complete-set editors captures the exact requested tile/address and controller revision before asking the platform clipboard; delivery targets that immutable address even if selection moves, rejects a stale portable revision, ignores unsolicited paste events, and clears pending targets when workspaces open or close. On Windows, all three editors additionally publish and consume the original registered `Lunar Magic 16x16 Tile` clipboard format as four little-endian subtile words in top-left/top-right/bottom-left/bottom-right order followed by the little-endian Acts Like word. Paste accepts larger global allocations while consuming only the first ten bytes, and the portable Unicode envelope remains available as the cross-platform fallback. A retained isolated-Wine fixture binds the original copy/paste entry points to that cross-process contract: vanilla tile `$000` publishes the exact ten-byte `701C721C711C731C0000` record, and an asymmetric `23016745AB89EFCD5713` record pastes and copies back byte-for-byte. `smw_map16_controller::tests::native_edits_reject_acts_like_cycles_atomically_and_normalize_background_behavior`, `smw_map16_controller::tests::native_edits_enforce_the_caller_resolution_limit`, `rom_map16_editor::tests::staged_map16_history_restores_exact_sets_and_invalidates_divergent_redo`, `rom_map16_editor::tests::staged_map16_history_is_bounded_to_one_hundred_snapshots`, `map16_editor::tests::clipboard_delivery_uses_requested_tile_and_rejects_a_stale_revision`, `map16_set_editor::clipboard::tests::clipboard_delivery_uses_requested_address_and_rejects_a_stale_revision`, `rom_map16_editor::tests::rom_clipboard_delivery_uses_the_requested_map16_address`, `native_clipboard::tests::lunar_magic_native_map16_tile_is_four_words_then_acts_like`, and `native_clipboard::tests::retained_lunar_magic_map16_clipboard_oracle_binds_copy_and_roundtrip` cover these routes. `map16_file_imports_and_subtile_attribute_edits_reopen_across_every_supported_variant` closes the supported 48-case ROM product across SMW-J, SMW-NA, and All-Stars+World-NA; LoROM `$20`/`$30`, SA-1 `$23`, and ExLoROM `$32`; absent/present copier prefixes; and two independent tagged graphics/Acts-Like placements. Each case edits both page endpoints and an interior tile across complete 16-bit subtile words (tile, palette, priority, and flips) plus minimum/maximum Acts-Like boundaries, repairs the checksum, reopens the exact page, preserves physical framing, and restores/reapplies exact physical images with Undo/Redo. Headered and headerless runs produce byte-identical logical images. The retained isolated-Wine `map16-editor-interaction` oracle opens command `$232F`, scrolls to page `$02`, selects the visible preceding-row tile `$1F0` through the actual drag gesture, changes all four subtiles, Acts Like, palette, priority, X flip, and Y flip as nine independent GUI edits, then proves nine Undo steps restore every initial control value and nine Redo steps restore every modified value exactly. Together with the named Ghidra handlers and retained native clipboard round trip, this completes the original core interaction boundary |
| Map16 | Import/export current page and complete sets | P | P | P | P | P | Pass | File models/CLI pass; Ghidra `ImportCurrentMap16PageFiles`/`ExportCurrentMap16PageFiles` at `$004E5A60/$004E5B60` prove the corrected legacy plane binding: the native ROM editor boundedly imports and atomically create-new exports `Map16Page.bin` (`0x800` definition bytes) plus `Map16PageG.bin` (`0x200` Acts-Like bytes) for editable foreground pages `$02–$7F`. Import captures the selected page and ROM revision, replaces all 256 definitions through one complete-set controller edit, and rejects protected `$00–$01` or background targets; paired publication is all-or-nothing and never overwrites. The adjacent Ghidra functions at `$004E5C60–$004E5EB0` establish complete legacy transfer: `Map16FG.bin` carries `0x40000` foreground definition bytes, `Map16FGG.bin` carries `0x10000` foreground Acts-Like bytes, and `Map16BG.bin` carries `0x40000` background definition bytes. The native editor now boundedly imports and create-new exports all three exact planes, atomically publishes the foreground pair, captures the initiating revision, preserves protected definition words, and canonicalizes background Acts-Like values to zero; `legacy_complete_planes_round_trip_exact_original_namespaces_and_lengths` and `foreground_import_preserves_protected_definition_words_but_imports_acts_like` cover their full shapes and namespace semantics. Original fixed-size reads accept short files, ignore trailing suffixes, and apply a definition prefix even when the `G` companion is missing; Rust's bounded prefix worker overlays only present bytes onto the current planes and reproduces that final state in one atomic edit. `prefix_reads_accept_short_and_trailing_files_and_optional_missing_siblings` and `legacy_prefix_reads_overlay_short_planes_and_missing_g_companions` cover the complete file-shape family. The editor also has bounded complete `.map16` import and create-new export across all 256 pages, binds completion to the initiating ROM revision, preserves imported auxiliary/editor sections, and byte-exactly re-exports a genuine LM 3.63 fixture. Modern selected-range `.map16` is now modeled and exposed natively from the exact recovered `WriteSelectedMap16ExportFile`/`ReadMap16ImportFile` boundary: compact containers retain header width, height, column, band-relative row, namespace-disambiguation flags, row-major definitions, and Acts-Like words. Export accepts a hexadecimal rectangle anchored at the selected tile; import either restores the file origin or captures the selected destination before background loading. Both paths reject row wrapping, namespace overflow, complete-container substitution, stale revisions, and malformed compact section shapes; protected `$0000–$01FF` graphics and background Acts-Like semantics remain canonical. Active legacy/selected/complete/bitmap file work gates edits, ROM commit, and close. The GUI gate is proved by the live `RomMap16Editor::contents` wiring for all ten transfer actions and the 32-test `rom_map16_editor` family, including genuine-file byte equality, revision binding, protected-page rejection, worker close gates, controller commit, and semantic reopen. Ghidra's modeless dispatcher at `$00501550` additionally proves commands `$2266/$2267` are selected export/import and `$2268/$2269` are complete export/import; `HandleMap16RenderWindow` at `$005008A0` proves the key table receives `virtual_key - $1B`, so `$004FFEF0` maps unmodified F2 to selected export and F3 with any modifier state to selected import. Rust now consumes those exact fixed shortcuts once and routes them through the same blocked/busy and revision-bound actions; `original_selected_file_shortcuts_match_f2_export_and_f3_import_modifiers` covers the full modifier boundary. `lm16_map16_file::tests::selected_range_band_flags_restore_all_ambiguous_namespaces`, `rom_map16_editor::selected_file::tests::selected_helpers_round_trip_a_rectangle_across_page_rows`, `rom_map16_editor::selected_file::tests::selected_import_can_retarget_and_preserves_protected_or_background_semantics`, `rom_map16_editor::selected_file::tests::selected_import_is_bound_to_the_revision_that_started_loading`, and `persistence_worker::tests::create_pair_is_all_or_nothing_and_never_replaces` cover the added routes; `map16_file_imports_and_subtile_attribute_edits_reopen_across_every_supported_variant` combines every supported file shape with the full 48-case ROM publication product: legacy `Map16Page.bin`/`Map16PageG.bin` planes, the portable page container, a complete-set container, and a compact selected-range LM16 rectangle all decode and round-trip before publication across three identities, four map modes, both copier-prefix forms, and two tagged storage placements. The selected range retains its 2×2 origin/band geometry and all four tile words plus Acts Like values. Every case repairs the checksum, semantically reopens the exact imported page, preserves physical framing, undoes/redoes exact images, and produces byte-identical logical output across header forms. The retained isolated-Wine `map16-transfer-interaction` oracle closes the original GUI boundary: actual buttons `$2266–$2269`, standard Open/Save As dialogs, modern filter 0, raw filter 1, exact one-tile and complete LM16 files, exact page/FG/BG legacy file pairs, byte-exact selected/complete/FG/BG import-reexport, immediate ROM save for the blue complete import, and the exact missing-headered-restore prompt are all bound by fixture and test. |
| Map16 | Import bitmap with quantization, deduplication, previews | P | P | P | P | P | Pass | Native PNG, Windows/OS/2 BMP, and bounded background system-clipboard workflows load real level graphics/palette context, show synchronized pan/zoom previews, expose optimization controls and both recovered reduction choices, and model all 128 free/reusable/reserved palette states. File loading format-detects bounded PNG plus indexed 1-/2-/4-/8-bit, default RGB555, 24-bit BGR, 32-bit BGRX, 16-/32-bit `BI_BITFIELDS`, 32-bit `BI_ALPHABITFIELDS`, `BI_RLE4`, `BI_RLE8`, OS/2 Huffman 1D/RLE24, `BI_JPEG`, and `BI_PNG` BMP. Windows/OS/2 v2 2-bit rows decode four MSB-first indices per byte with DWORD padding and family-correct orientation, while legacy core headers continue to reject that unsupported depth; the public BMP Suite `pal2.bmp` fixture (SHA-256 `bac6eec4100831e635fcd34a9e0e34a8a9082abdec132ac327aa1bfc7137d40f`) decodes byte-exactly to its published 127×64 PNG reference. Both the 12-byte OS/2 core header with RGBTRIPLE palettes and 64-byte OS/2 2.x header with RGB2 palettes, uncompressed indexed/24-bit pixels, compatible indexed RLE, Huffman 1D, and 24-bit RLE are decoded through distinct bounded paths. Huffman input requires exactly two palette entries, exact declared rows, monotonic in-width transitions, Group-3 EOL/RTC termination, and bottom-up output; the public BMP Suite `pal1huffmsb.bmp` fixture (SHA-256 `cad3a788ec02c3920e867b4937cdde98427ca1d0a9b1b3946f4e57c17eaf873e`) decodes byte-exactly to its published 127×64 PNG reference. The RLE24 path handles encoded/absolute runs, word padding, delta/EOL/EOB commands, and bottom-up coordinates; the public BMP Suite `rgb24rle24.bmp` fixture (SHA-256 `351d358824671a79dc63147a78fc555d46cbee357661674e80c898e133e0b5c5`) decodes byte-exactly to its published 127×64 PNG reference. External 40-byte alpha masks and embedded V3/V4/V5 alpha masks are validated as bounded, contiguous, and disjoint from RGB before their transparency reaches the native preview and import planner; an absent optional `BI_BITFIELDS` alpha mask retains opaque compatibility. Embedded image payloads require exact declared framing and header dimensions, share bounded decoding, retain PNG alpha, and normalize grayscale/RGB/CMYK JPEG output to RGBA. The decoder handles packed pixels, palette tables, validated channel masks, RLE encoded/absolute/delta/EOL/EOB commands and word padding, padded rows, and top-down/bottom-up orientation where the format permits it; malformed headers, dimensions, planes, compression, depths, palettes, masks, embedded payloads, fax transitions/termination, RLE coordinates/termination, truncation, and unknown signatures reject before preview construction. Each accepted background request captures its ROM revision, level, first Map16 tile, two optional graphics slots, and palette row before loading; completion rejects stale revisions, and active loading or preview gates conflicting Map16 edits, transfers, bitmap requests, and ROM commit. Loading blocks close, while an open preview requires discard confirmation. The weighted set/subset allocator assigns one row per 8×8 tile and commits palette/GFX/Map16/runtime changes atomically. Median Cut matches the recovered moment/variance/average/SNES-rounding semantics, generated entries receive Lunar Magic's integer HSL row ordering before pixel remapping, and the low-color/Popularity paths match an exact Wine-observed four-color fixture. `map16_bitmap_import::tests::bmp_decoder_handles_os2_v2_huffman1d_rows_palette_and_orientation`, `map16_bitmap_import::tests::bmp_decoder_handles_packed_indexed_depths_and_palette_padding`, `map16_bitmap_import::tests::bmp_decoder_rejects_invalid_palette_shape_and_indices`, `map16_bitmap_import::tests::bmp_decoder_rejects_malformed_core_headers_palettes_and_pixels`, `map16_bitmap_import::tests::bmp_decoder_handles_os2_v2_rle24_runs_absolute_delta_and_padding`, `map16_bitmap_import::tests::bmp_decoder_handles_os2_v2_uncompressed_and_indexed_rle_images`, `map16_bitmap_import::tests::bmp_decoder_rejects_malformed_and_incompatible_os2_v2_images`, `map16_bitmap_import::tests::bmp_decoder_preserves_valid_v3_v4_v5_and_external_alpha_bitfields`, `map16_bitmap_import::tests::bmp_decoder_rejects_missing_overlapping_and_noncontiguous_alpha_masks`, `map16_bitmap_import::tests::bmp_decoder_handles_embedded_png_and_jpeg_payloads`, `map16_bitmap_import::tests::bmp_decoder_rejects_embedded_shape_and_payload_framing_errors`, `map16_bitmap_import::tests::bmp_decoder_handles_rle8_runs_absolute_delta_and_padding`, `map16_bitmap_import::tests::bmp_decoder_handles_rle4_alternating_and_absolute_nibbles`, `map16_bitmap_import::tests::bmp_decoder_rejects_malformed_rle_commands_and_orientation`, `rom_map16_editor::bitmap_import::tests::os2_huffman1d_bmp_reaches_native_preview_with_palette_and_orientation_intact`, `rom_map16_editor::bitmap_import::tests::packed_2bpp_bmp_reaches_the_native_preview_with_palette_rgb_intact`, `rom_map16_editor::bitmap_import::tests::os2_v2_rle24_bmp_reaches_the_native_preview_with_rgb_channels_intact`, `rom_map16_editor::bitmap_import::tests::os2_v2_bmp_reaches_the_native_preview_with_rgb_channels_intact`, `rom_map16_editor::bitmap_import::tests::alpha_bitfield_bmp_reaches_the_native_preview_without_losing_transparency`, and `rom_map16_editor::bitmap_import::tests::bitmap_completion_is_bound_to_the_revision_that_started_loading` cover the decoding and asynchronous boundaries. The native option surface now wraps its complete allocation, optional GFX-slot, and color-reduction groups at narrow widths, keeping the rightmost Map16/reserved-tile, slot-5, priority, and reduction controls reachable. Sixteen native-session tests, 27 decoder/planner tests, and seven installed-session transaction tests pass across request capture, persisted options, synchronized RGBA previews, every supported BMP family, quantization/allocation, pristine foreground/background runtime installation, semantic reopen, and undo. |
| Map16 | Clipboard bitmap and rectangle conversion | P | P | P | P | P | Pass | The authenticated `Lunar Magic 16x16 Tiles` rectangle now has an exact bounded model for its 0xA0-byte header, definition/Acts-Like/source-index section offsets, selected count, width/height, source origin, row-stride index grid, and legacy alternate subtile word-order flag. The installed-ROM Map16 editor copies its hexadecimal selected rectangle through that exact representation and pastes at the request-captured destination through one ownership-aware staged edit; stale ROM/editor revisions, malformed framing, row wrap, protected built-in graphics, background Acts-Like values, and namespace overflow retain their existing atomic guards. Its rendered page now supports direct click-drag rectangle selection in all four directions: reverse drags normalize to the top-left origin, the complete rectangle receives one visible outline, and hexadecimal width/height feed the same exact clipboard and selected-file pipeline without manual entry; an ordinary click restores a one-tile range. The installed level editor additionally converts that native rectangle into Lunar Magic's exact seven-byte Direct Map16 `$27`/`$29` object, choosing the command at tile `$4000`, retaining its source-pattern and output dimensions, enforcing the `$8000` namespace boundary, binding clipboard delivery to both application and level-controller revisions, and staging the object for one ordinary canvas-placement transaction. Both complete-stream and isolated-object rendering now bypass family dispatch for these commands, expand the physical row-major source pattern at its native 16-tile stride, repeat it to the independent output dimensions, clip through the normal cache boundary, and preserve X/Y semantics in horizontal and vertical levels. Preview, reopen, and Undo coverage proves the resulting edit is visible, persistent, and reversible. `clipboard::domains::map16::native_tests::native_rectangle_uses_exact_header_sections_and_row_stride`, `native_clipboard::tests::native_map16_rectangle_adapter_retains_exact_shape_origin_and_sections`, `rom_map16_editor::tests::rendered_page_drag_normalizes_rectangle_in_every_direction`, `rom_map16_editor::tests::rendered_page_outlines_only_rectangles_visible_on_the_current_page`, `rom_map16_editor::tests::rom_rectangle_clipboard_targets_captured_origin_and_rejects_stale_delivery`, `object_fields::tests::direct_map16_rectangles_encode_both_native_commands_exactly`, `standard_object_renderer::tests::direct_map16_commands_render_physical_repeating_patterns_before_family_dispatch`, and `vanilla_level_editor::tests::direct_map16_clipboard_rectangle_places_reopens_and_undoes_in_pristine_rom` cover the native routes. Disposable-process Wine automation now captures the original bitmap dialog plus pre/post palette, graphics, and complete 65,536-definition Map16 workspace. Default deduplication, non-deduplicated spatial placement, priority-enabled/nondefault-cursor, non-aligned synthetic-edge, and `$8FFF` exhaustion captures match byte-for-byte; the sequential capture corrected flattened Rust placement to Lunar Magic's `$10`-tile row stride, while the exhaustion capture proves one available definition is retained before the exact native warning. The transparent-source blank-reuse truth table is additionally bound to the original `004EE470`/`004EEE40`/`004EF2D0` branch chain and all eight Rust persistence variants. The retained `00500850` mouse dispatcher through `004F9340` marquee chain closes the original rectangle gesture, including reverse-drag normalization, inclusive dimensions, and the one-source-pixel white/black/black/white border phase |
| Graphics | 8×8 tile viewer/editor with selection, flips, copy/paste | P | P | P | P | P | Pass | Portable, pristine-layout, and profile-backed installed graphics editors expose selected-tile horizontal and vertical flips through their existing typed controller mutation paths. Portable flips are individually revisioned and undoable; installed flips retain fixed/ExAnimation ownership and stale/file-worker guards before joining the staged ROM commit. Typed single-tile copy/paste now spans all three surfaces through buttons and the authenticated tile-sheet gestures: Ctrl+left copies and selects the pointed tile, unmodified right-click pastes the active edited tile without changing its selection, and Ctrl+right requests a system-clipboard tile and selects the target only after successful validation. On Windows, copy publishes Lunar Magic 3.63's registered `Lunar Magic 8x8 Tile` format as the exact 64 indexed pixels together with the portable Unicode envelope; paste prefers that native record, accepts larger allocations while consuming only the first 64 bytes, and retains text fallback on other platforms. Installed paste retains the ownership, stale, and file-worker guards. Each surface displays only the active 256-tile page as the native seamless 16×16 array of 16-pixel cells. F8 toggles its initially hidden grid; Ctrl+Alt+F8 switches the retained white/black grid color and reports the exact native status. Up/Down retain page navigation for every modifier form except editable Shift without Ctrl/Alt; Left/Right wrap whenever Shift is held and the tile is editable. Page Up steps palettes with any modifiers, while Page Down does so except for the reserved Ctrl+Shift diagnostic unlock. The default selection renders the exact sixteen-color RGBQUAD table recovered from the executable. Unmodified F9 confirms and publishes the current level's working FG/BG/sprite buffers to the selected separate-file or joined-AllGFX destination without committing the ROM; the original's complete-set preflight and selected-range mutation are retained in the level-105 Wine oracle. The recovered `WM_CHAR` dispatcher routes lowercase and Shift-uppercase D/M/R/X/Y to apply color map, edit color maps, rotate 90° clockwise, flip horizontally, and flip vertically across all three surfaces; Ctrl/Alt chords stay outside it. Those actions, color mapping, and pixel painting mutate Lunar Magic's private selected-tile edit buffer rather than source backing; only an eligible right-paste publishes that buffer to a sheet target. The retained Wine pixel-buffer oracle proves this split on diagnostic tile `$600`, including unchanged decoded and planar backing, and native tests prove source preservation plus eligible-target publication. The recovered sixteen-by-sixteen color-map model, identity reset, source/mapped previews, transactional OK/Cancel dialog, and selected-tile application now span all three surfaces through those same guards. They also share event-driven status for tile/address and palette hover, tile/foreground/background selection, copy/paste results, rendered/default palettes, viewed pages, and page boundaries. Left/right palette clicks select FG/BG, left/right pixel gestures paint them, and Ctrl+left/right sample them; native defaults FG 1 and BG 0. Installed sampling remains available on read-only tiles without bypassing mutation ownership. Installed Ctrl+Shift hover additionally reports exact OrigAnim, level ExAnimation, and global ExAnimation slots when supplied by canonical v2 ownership evidence; legacy v1 generic-record evidence remains readable. The authenticated Ctrl+Shift+PageDown diagnostic now unlocks the complete `$4000`-tile working-cache address space in both pristine and profile-backed installed editors. Both follow active-level and Special World changes without discarding staged ordinary-file edits; the installed route additionally resolves legacy or six-slot Super GFX Bypass assignments through the active revision profile, uses installed GFX32/GFX33 pointers when present, and fills Layer 3 from the profile-described expanded-settings record. A pristine/profile equivalence test binds the recovered sprite, special, and Layer 3 banks; reserved GFX60–GFX63 ExAnimation sources, selected auxiliary animation, and present external-sprite banks are also populated at their recovered bases. Both right-paste routes retain the original `$5FF` ceiling plus unused-bypass, fixed-animation, and Special World guards. The retained Wine cache-paste oracle proves successful `$002/$5FF` mutation and unchanged `$041/$300/$600` rejection, while the pure predicate test exhausts every recovered range. F9 publishes only exact current-level FG/BG/SP slots with `$7F` omission and last-assignment-wins duplicate handling, so an unpasted edit buffer is excluded. Transient hover outlines and the fixed 256×256 logical selected-tile canvas share exact render/hit-test geometry; toolkit DPI scaling supplies the native presentation scaling without an invented user zoom control. The isolated `graphics_editor_wine` gate now opens the original `Window8x8` from an authenticated installed level-105 ROM, reproduces the retained pixel-buffer and guarded sheet-paste TSVs exactly through real flip, paint, and right-paste window gestures, proves native 64-byte copy plus asymmetric paste/copy round trip, exercises ordinary `$002` and boundary `$5FF` writes plus fixed-animation `$041`, unused `$300`, and out-of-range `$600` rejection, and proves the transient workflow leaves the complete ROM byte-identical. Ghidra binds the remaining keyboard/button dispatch to the same actions. Twenty-nine focused tests cover portable, pristine, and profile-backed installed editors, including controller history, ownership/stale/file-worker guards, full diagnostic cache population, Special World refresh, F9 slot routing, every recovered paste range, and native/portable clipboard routes. |
| Graphics | Extract/insert GFX and ExGFX | P | P | P | P | P | Pass | CLI/project operations pass. The installed graphics editor non-blockingly extracts its staged slot as Save-As raw 4bpp `GFX%02X.bin`/`ExGFX%02X.bin` and boundedly inserts an exact-size raw file through the same ownership-aware staged transaction. Its standard-GFX directory actions use fixed `GFX%02X.bin` names, bounded background I/O, per-file progress, and Escape/button cancellation. Extraction decodes the profile-declared standard range from one immutable ROM image and publishes the complete set all-or-rollback. Insertion requires the complete exact-shape set, precompresses every file, then allocates, repoints, semantically reopens, checksum-repairs, and commits the ROM as one revision or leaves it unchanged. Equivalent replace-or-create `AllGFX.bin` extraction concatenates those raw slots in table order; bounded joined insertion derives exact split offsets from the current ROM and enters the same atomic transaction. Canonical profiles represent contiguous or three-plane graphics pointers, validate and audit their physical components, and protect every plane during allocation. SMW-US special extraction resolves the authenticated live non-contiguous startup operands while retaining public order `GFX33`, then `GFX32`. Its atomic insertion searches one bounded LoROM bank for both tagged payloads, writes both low words and their shared bank operand, repairs the checksum, and requires byte-exact semantic reopen; synthetic failure-atomic tests and an authentic 2 MiB Lunar Magic-created ROM gate pass. Audited expanded tables require every standard `$000..$033` pointer and treat later zero pointers as unused auxiliary/ExGFX sentinels. Native ExGFX extraction enumerates every nonzero reserved `$060..$063` and ordinary `$080..$FFF` entry and preserves raw 2/3/4bpp bytes under canonical `ExGFX%02X.bin` names; insertion strictly enumerates those same canonical directory files, supports both replacement and new native-depth slots, and commits the sparse set atomically without touching omitted pointers. First-time SMW-US insertion supports both pristine 3bpp and authenticated 4bpp states, installs the zero-filled expanded-settings owner plus the recovered current ExAnimation/graphics/shared-palette runtime family, protects both runtime allocations and `$088000..$08ACFF` from standard-file allocation, initializes both compressed ExGFX pointer tables to canonical zero sentinels, publishes the recovered `$002A47 = EA EA` expanded-format marker, routes raw reserved `$60..$63`, ordinary `$80..$FF`, and extended `$100..$FFF` through their three recovered pointer domains, and publishes mixed directories as one reopen-verified mutation. Runtime authentication accepts populated `$60..$63` entries only when every nonzero pointer resolves to a bounded RATS-owned payload; padding and empty entries remain zero. Retained Lunar Magic ROMs prove byte-exact first `$60` and `$80` installs; foreign layouts remain disabled. A vanilla first import now installs the recovered 32-byte graphics data block, packs `$80..$DFF` to native 3bpp while preserving `$E00..$FFF` as-is, follows the relocated extended pointer table, reopens and re-exports both domains, and restores the byte-exact vanilla ROM through Undo. The complete native file-action surface now uses a wrapping layout, so raw, current-level, standard-directory, special GFX32/GFX33, sparse ExGFX, and joined AllGFX extraction/insertion controls remain reachable instead of being clipped at narrower editor widths. Ghidra now binds the exact ordinary/quiet and separate/joined handler flags plus the legacy 3bpp-ExGFX format-change branch. The native directory and `AllGFX.bin` routes detect that state only from an authenticated ExGFX runtime plus the incomplete two-marker 4bpp prerequisite, display the original warning text before file I/O, retain the already-selected request on Yes, and discard it without mutation on No/Escape; expanded, partially marked, and foreign-hook false positives are covered. Yes executes an authenticated modified-runtime migration: it preserves ROM size and all three ExGFX pointer tables, reinstalls the 4bpp standard-GFX files, repairs the checksum, and semantically reopens both every standard file and a retained `ExGFX80` payload. Native ExGFX insertion also coordinates both authenticated legacy ExAnimation generations instead of requiring a separate command: legacy pointer-hook fragments advance in place while existing ExGFX survives, and the legacy global table migrates every record before the requested ExGFX file is installed, all inside one unpublished staging project. The live Wine gate first proves Lunar Magic's own `-ImportGFX` → `-ImportExGFX` → `-ExportExGFX` control, then executes the complete Rust standard-GFX → ExGFX80 → legacy-marker → standard-GFX migration from canonical headered, headerless, and Fast-LoROM inputs. It requires exact preservation of the authentic 512-byte prefix or continued prefix absence, byte-identical logical header variants, retained `$30` Fast-LoROM identity, current expanded-ExAnimation authentication, exact Lunar Magic shared hooks/helpers, and original Lunar Magic re-export of all 52 GFX plus `ExGFX80` from every result. ExLoROM is verified through conversion, retained `ExGFX80`, new `ExGFX81`, and exact Lunar Magic re-export. Authentic SA-1 Pack v1.40 verifies all seven nonempty first-import domain masks across `ExGFX60`, `ExGFX80`, and `ExGFX100`: every native result is byte-identical to Lunar Magic's full transition, including mask-specific marker, allocation order, expanded settings, helpers/tables, pointers, encodings, and checksum. The extended pointer follows the authenticated expanded-settings runtime operand when prior graphics allocations relocate its owner. Subsequent SA-1 directory synchronization authenticates and reclaims every existing owner, replaces present files, clears omitted entries in all three pointer domains, and commits as one revision and Undo step. Retained replacement-all and only-`ExGFX80` transitions are complete-ROM byte matches, and original Lunar Magic re-exports every surviving Rust file exactly while leaving omitted files absent. The retained write-publication oracle binds both original extraction functions to the shared `wb` mode and fixed directory/joined naming; repeated separate, joined, ExGFX, and raw Save As publication now replaces approved regular files atomically while rejecting invalid late targets without partial output. Nineteen installed-editor tests, ten active batch-export tests, both import-worker tests, nine active application batch-import tests, the mixed first-time ExGFX transaction test, both coordinated legacy-generation tests, focused profile tests, `lunar_magic_reexports_rust_standard_and_exgfx_across_legacy_migration`, all three `authentic_sa1_first_exgfx*_install_is_byte_exact` gates, `authentic_sa1_first_mixed_exgfx_domains_are_byte_exact`, `sa1_extended_pointer_follows_the_authenticated_settings_owner`, `authentic_sa1_directory_sync_reclaims_replaces_and_removes_omitted_files`, `directory_sync_reclaims_omitted_owners_and_is_one_undoable_commit`, `lunar_magic_reexports_rust_sa1_standard_graphics_install`, and the 233-test renderer gate cover the verified boundaries. |
| Graphics | Change compression and migrate dependent tables | P | P | P | ~ | ~ | Partial | The low-level complete-slot recompression transaction, checksum repair, semantic reopen, undo/redo, CLI copy-on-write boundary, and native dialog pass on explicitly payload-only layouts. Fresh Lunar Magic 3.63 `-ChangeCompression` oracles authenticate all three real SMW-US modes from the metadata nibble, fixed hook, exact RATS ownership, complete immutable-runtime checksum, and trailer. The native tools expose direct installed-SMW transitions from `LZ2 Orig/Speed` to `LZ3` and from `LZ3` to either `LZ2 Orig` or `LZ2 Speed`; payload-compatible `LZ2 Orig` → `LZ2 Speed` remains runtime-only. Codec-changing transactions migrate all 50 split-plane files and shared-bank GFX33/GFX32, every populated compressed `$80..$FFF` ExAnimation/ExGFX slot, and both installed overworld-event streams while switching the authenticated decoder, metadata, ROM-size header, checksum, semantic reopen codec, and Undo/Redo state atomically. LZ3 reversal constructs an explicit ownership manifest from every currently referenced tagged runtime/graphics/event block, skips untagged fixed vanilla streams, validates every owner against the source image, reclaims only those proven obsolete blocks in staging, and allocates the replacement into the reclaimed same-size ROM. The semantic replacement history retains codec-aware Undo/Redo; late reclamation or plan failure remains atomic. Lunar Magic recognizes both same-size `LZ2 Orig` and `LZ2 Speed` Rust results as already using their selected formats and re-exports all 52 GFX files identically; migrated ExGFX files and event streams also reopen under the selected codec. Fast-LoROM `$30` is covered across both copier-header forms. Authentic 8-MiB ExLoROM LZ2/LZ3 captures bind every runtime/table write to the relocated active SMW body, preserve mapper-significant pointer banks, round-trip all 52 standard files plus `ExGFX80`/`ExGFX81`, retain ROM size and checksum validity, restore exact physical bytes through Undo, and reopen in original Lunar Magic for byte-exact export. Authentic SA-1 Pack v1.40 captures separately authenticate its embedded LZ2 decoder, distinct 780-byte LZ3 runtime, mapper-specific standard/ExGFX pointer operands, and relocated overworld-event stream locators. The SA-1 migration round-trips all 52 standard files plus `ExGFX80`, migrates both installed event streams, repairs the checksum, restores exact physical bytes through Undo, and reopens in original Lunar Magic for byte-exact export. `exlorom_codec_replacement_preserves_every_graphics_stream_and_undoes`, `exlorom_lz3_command_is_one_same_size_undoable_revision`, `sa1_codec_migration_preserves_all_standard_graphics_and_undoes`, `sa1_installed_event_streams_authenticate_in_both_compression_modes`, and the extended live Wine gates cover those mapper families. Snes9x survives initialization and the 232-test renderer gate remains green. One patch-derived historical LoROM optimized-LZ2 generation is now authenticated by its exact `$1AF` RATS runtime, `LM 00 01` trailer, and complete checksum; all 52 standard graphics streams decode through that selected mode. Rust now follows the authenticated relocated `$6D00` settings owner for the legacy `$100–$FFF` ExGFX table, accepts the exact earlier event-loader generation, preserves all 54 ExGFX streams including two bounded `$FFF` payloads, and reproduces GFX17's four exact fourth-plane tiles. The complete same-size LZ3 migration matches Lunar Magic's 52 GFX files, 54 ExGFX files, and both event streams; both reciprocal LZ3 → `LZ2 Orig`/`LZ2 Speed` transitions preserve those same 108 semantic domains, checksum, reopen, and exact Undo. Lunar Magic accepts all three Rust results as already using the requested format without changing their hashes and re-exports all 52 GFX plus 54 ExGFX files byte-for-byte against its own three conversion oracles. Other historical runtime generations remain open. |
| Graphics | External graphics editor launch and reload | P | P | P | P | P | Pass | The installed graphics editor exports the current staged slot into a private create-new workspace under its canonical `GFX%02X.bin`/`ExGFX%02X.bin` name. Persisted `LMTOOLS1` entries that reference `{graphics}` or Lunar Magic's recovered `%1` alias in a direct argument appear in a graphics-editor selector and expand that staged path together with ordinary ROM/project/level context only after the private file exists; using the staged file as a working directory is rejected before staging. A direct executable/application chooser remains available. The approval window shows the exact executable or macOS `.app` bundle and every direct argument, launches without a shell on a worker, and waits non-blockingly; macOS bundles route through `/usr/bin/open -W -n -a ... --args` so the newly launched instance participates in the same reload boundary without collapsing argument strings. Only a successful exact-size regular file reloads into the staged controller. Pixel edits, commits, and close/shutdown are gated during the round trip; missing or malformed templates, stale application revisions, failed processes, symlink/non-file replacements, truncation, growth, disconnected workers, and cleanup failures cannot replace staged state. Cancellation, completion, errors, expansion failures, and dropped prompts remove the private workspace. Port-8089 Ghidra revalidation of `ExpandExternalEditorCommandTemplate` at `$004407B0` proves every `%1` occurrence is replaced with the graphics path while other percent sequences remain literal; `LaunchGraphicsFileExternalEditor` at `$00440B10` proves the `Graphics`/`ExGraphics` path and `GFX%02X.bin`/`ExGFX%02X.bin` naming route into that template before process creation. The retained isolated-Wine oracle drives command `$251E` and native `Edit` control `$0185`, records the exact expanded `GFX00.bin` argument while `%2` stays literal, observes the external file mutation, proves the original closes process handles without waiting or reloading, and captures the exact `Couldn't Create Process!` rejection for an invalid executable. `every_graphics_domain_and_command_template_reloads_the_process_output` completes the supported six-case process boundary: canonical `GFX00.bin` and `ExGFX123.bin` workspaces each launch directly, through `{graphics}`, and through Lunar Magic's `%1` alias; every launched process receives the staged path, replaces the exact-size file, reloads the exact output, and leaves no private workspace. Separate process-launch tests retain argument boundaries for ordinary executables and macOS application bundles, with direct Windows command execution covered on that target; rejection tests cover alias discovery, ordinary-percent preservation, staged-path safety, malformed shapes, failed processes, cancellation, and non-publication. |
| Palette | Edit level/shared/overworld palettes | P | P | P | P | P | Pass | Multiple native editors pass. The ownership-qualified ROM palette, installed level-assets palette, portable/installed overworld palettes, and native shared/custom palette all use the typed clipboard for exact single-color and 16-color-row copy/paste, reproducing the documented Ctrl+Alt left/right gestures plus explicit row controls. Rows align to the selected color; incomplete tails reject. Paste is request-targeted, ROM/shared workspaces gate stale or active-import delivery, aggregate and overworld rows use their ownership-atomic controller transactions, and shared/custom rows decode and replace their exact backend once without disturbing auxiliary bytes. `native_clipboard::tests::palette_row_requires_and_round_trips_exactly_sixteen_colors`, `rom_palette_editor::tests::palette_row_paste_targets_the_complete_selected_row_and_rejects_partial_tail`, `native_level_assets_panels::palette::tests::aggregate_row_paste_is_aligned_and_complete`, `overworld_editor_palette::tests::overworld_row_paste_is_aligned_and_complete`, `rom_shared_palette_editor::tests::typed_row_paste_replaces_one_aligned_backend_row_atomically`, `rom_shared_palette_editor::workspace::tests::edits_preserve_backend_shape_and_unselected_bytes`, and `palette_controller::tests::protected_late_edit_rolls_back_and_stale_commit_is_rejected` cover these routes. On Windows every palette surface publishes and prefers Lunar Magic 3.63's exact 12-byte `Lunar Magic Color V2` and 132-byte `Lunar Magic Color Row V2` records while retaining the Unicode typed fallback; larger allocations consume only the proven prefix. Ghidra recovers both publish/read paths, V2 preference, legacy fallback, minimum-size checks, representation flags, RGB/SNES planes, and nearest-replicated RGB quantization. The retained isolated-Wine `palette_clipboard_wine` gate opens the original palette editor, captures exact V2 and legacy color/row publications, proves valid V2 color and row decoding, proves 11-byte/131-byte rejection, and leaves the authenticated ROM byte-identical. Rust fixtures bind the exact records, upward quantization tie, SNES masking, and all native/portable editors; 59 focused palette tests and the 232-test renderer gate pass |
| Palette | Import/export full palette, row, shared palette formats | P | P | P | P | P | Pass | Installed per-level full palette import/export now covers portable `.lmpal` plus exact raw, TPL v2, RGB24, and automatic optional `.palmask` handling with bounded background I/O, exact installed/supported mapping, atomic ownership enforcement, staged-state export, RGB-convention retention, stale-sidecar removal, create-new publication, and revision/busy guards. The installed shared/custom editor boundedly imports and create-new exports exact native `.smwpal` files in both the `$7E2`-byte legacy and `$810`-byte expanded layouts, including all 16 expanded auxiliary bytes; supported legacy-to-expanded installation is allowed and unsafe downgrade rejects atomically. The ownership-qualified ROM palette editor boundedly imports and create-new exports Lunar Magic's exact 257-word raw palette, version-2 TPL, and RGB24 palette. It also imports and create-new exports the selected complete row in `LoadPaletteRowFromFile`'s exact `$20`-byte little-endian SNES format: the initiating 16-color boundary is captured before background loading, malformed lengths and incomplete rows reject, immutable ownership makes protected changes failure-atomic, revision/busy gates cover edits/commit/close, and export snapshots the selected row. Every full import automatically discovers the exact lossless 257-byte same-basename `.palmask` selector proven by the bundled LM 3.63 help and executable strings, preserving unselected words; only a missing sidecar is ignored, while malformed, non-regular, oversized, or unreadable siblings reject the whole load. TPL/RGB routing uses retained LM 3.63 evidence for the exact installed `[0],[2..256]` to supported `[0..255]` mapping, so supported selector indices address that same order while installed backdrop word 1 remains unreachable and retained. Raw import performs its recovered selected-row-zero clearing; TPL/RGB imports clear only selected supported row-zero indices 16 through 240. Masked RGB expansion detection considers only selected entries, and reciprocal export retains the detected high-bit or replicated-bit channel convention. The native mask mode starts fully enabled for each opened workspace, marks disabled colors with `X`, toggles one entry by click or its bounded 16-color row by Alt-click, exposes enable-all/disable-all, and retains imported noncanonical selector bytes until changed. Any disabled entry makes raw/TPL/RGB export atomically create the palette and same-basename `.palmask`; an all-enabled mask creates only the palette while recoverably removing a stale regular sidecar as required by LM 2.42+, and collision/failure restores that sidecar and publishes no palette. Every format submits only actual differences through immutable ownership so a protected mutation rejects the entire batch. Import loading is revision-bound and gates editing; all active file work gates commit and close, while export persists immutable palette and mask snapshots. `document_loader::tests::optional_read_omits_only_a_missing_sibling`, `persistence_worker::tests::create_pair_is_all_or_nothing_and_never_replaces`, `persistence_worker::tests::create_removing_deletes_only_a_stale_regular_sibling`, `palette_controller::tests::raw_import_is_masked_row_zero_canonical_and_ownership_atomic`, `palette_controller::tests::supported_palette_mapping_matches_retained_tpl_oracle_and_is_ownership_atomic`, `palette_controller::tests::supported_palette_mask_uses_file_indices_and_preserves_unselected_rows`, `rom_palette_editor::tests::palette_mask_click_and_alt_row_preserve_unrelated_and_partial_rows`, `rom_palette_editor::transfer::tests::selected_row_transfer_is_exact_little_endian_and_request_targeted`, `rom_palette_editor::transfer::tests::selected_row_transfer_rejects_bad_shape_boundary_and_incomplete_rows`, `rom_palette_editor::transfer::tests::raw_transfer_helper_preserves_exact_words_and_noncanonical_mask_bytes`, `rom_palette_editor::transfer::tests::tpl_and_rgb_helpers_round_trip_supported_order_and_rgb_convention`, `rom_palette_editor::transfer::tests::supported_mask_is_retained_and_limits_rgb_expansion_evidence`, `rom_palette_editor::transfer::tests::import_requests_use_automatic_same_name_palmask_sibling`, `rom_palette_editor::transfer::tests::export_publishes_palmask_only_when_a_color_is_disabled`, `rom_level_assets_editor::palette_transfer::tests::supported_transfer_uses_exact_installed_mapping_and_selected_row_zero_clearing`, `rom_level_assets_editor::palette_transfer::tests::authentic_import_helpers_are_shape_checked_and_failure_atomic`, `rom_shared_palette_editor::workspace::tests::complete_file_replacement_allows_install_upgrade_but_rejects_downgrade_atomically`, `rom_shared_palette_editor::transfer::tests::native_shared_palette_helper_round_trips_both_exact_backends`, and `rom_shared_palette_editor::tests::complete_file_upgrade_dispatches_and_reopens_exact_expanded_backend` cover these native routes. The retained Wine oracle drives original commands `$239D`/`$239E` and palette-editor control `$2264`: legacy shared and full exports are identical at `$7E2` bytes, a changed legacy import reopens byte-for-byte with a valid ROM checksum, and controlled original expanded-backend shared/full exports are identical at `$810` bytes. Fixed SHA-256 fixtures make both backends reproducible. Recovered `ImportSharedPaletteFile` control flow proves the reciprocal expanded read size and save path; Rust then round-trips both exact layouts and tests legacy-to-expanded installation plus failure-atomic unsafe downgrade. The original `$20`-byte selected-row loader and per-level raw/TPL/RGB/`.palmask` success and rejection routes are independently retained. |
| Overworld | Edit Layer 1 tiles, levels, paths, events | P | P | P | P | P | Pass | Complete model and transfer oracle exist. The installed nine-domain editor renders the staged ROM aggregate with profile-decoded Map16 plus native `GFX1C`–`GFX1F`, supports exact canvas selection for either layer, previews a bounded completed-event count, and invalidates the nearest-neighbor raster after every accepted edit. Its live-palette visual Map16 page picker sets an exact 16-bit brush. Brush strokes fill pointer-sampling gaps, rectangle drags normalize either direction from the original press cell, and deterministic four-connected fill is bounded to the exact rectangular layer; all three omit unchanged cells and route ordered typed batches to either layer. Both canvases outline their active cell, while unrenderable non-overworld pages retain hexadecimal selection and property editing. `rom_overworld_editor::canvas_tests` covers stroke, rectangle, fill, and exact edit generation; `multi_cell_layer_gesture_batch_commits_reopens_and_undoes_as_one` proves multi-cell Layer 1/Layer 2 batches across the ROM commit, semantic reopen, undo, and redo boundary. `authentic_lunar_magic_rom_opens_and_renders_profile_free_playable_layer2_workspace` now opens the real Lunar Magic 3.63 transfer result with and without its copier header, proves pixel-identical 1024x512 map renders, applies one four-cell terrain gesture and a route-link edit, semantically reopens both domains, preserves the exact header state, validates the checksum, restores the exact original physical image with two Undo operations, reapplies both operations with Redo, and proves byte-identical logical results across variants. The thirteen-domain Lunar Magic 3.63 transfer observation includes the recovered Layer 3 mode-enabled, packed-mode, alternate-source, and primary-additive fields for all seven expanded-settings records. Refreshing the authentic before/after observations adds exactly 28 paths while preserving every prior value, both ROM digests, all 124 changed ranges, and all 23 installed owners. `built_cli_replays_lunar_magic_overworld_transfer_event_domain` compares complete parsed observations with a bounded diagnostic and asserts canonical text. `interactive_overworld_edits_match_every_supported_identity_and_layout_variant` completes the supported 48-case product across SMW-J, SMW-NA, and All-Stars+World-NA; LoROM `$20`/`$30`, SA-1 `$23`, and ExLoROM `$32`; copier-prefix absence/presence; and default/relocated pointer tables. Each case applies one mixed atomic edit to Layer 1, Layer 2, event reveals, endpoints/routes, messages, sprites, palette, and animation, semantically reopens every domain, validates the repaired checksum, restores/reapplies exact physical bytes with Undo/Redo, preserves the copier prefix, and produces identical logical output across header variants. |
| Overworld | Edit Layer 2 tiles and event tiles | P | P | P | P | P | Pass | Transfer evidence exists. Event-tile selection movement now follows the recovered `ConvertOverworldTileCoordinatesToIndex`, `ConvertOverworldTileIndexToCoordinates`, `ConstrainEventTileSelectionMoveEndpoint`, `SearchValidEventRecordDisplacement`, `RelocateSelectedOverworldEventTiles`, and 6x6 `ApplyMultiCellOverworldEventTile` semantics from Lunar Magic 3.63: all 8,192 seam-aware 64x128 coordinates round-trip bijectively, destinations retain the native doubled-offset representation, and a drag searches X then Y toward zero until every selected footprint fits. Duplicate/out-of-range selections and invalid destinations reject without partial mutation. The installed nine-domain GUI exposes inclusive selection and signed tile deltas; bounded `LMOWEDT1 event-move` exposes the same typed transaction. `selection_move_uses_one_constrained_native_displacement_atomically` proves shared constrained displacement and failure atomicity, while `event_tile_selection_move_commits_reopens_and_restores_as_one` proves controller staging, ROM commit, semantic reopen, one-step Undo restoration, and Redo. `event_movement_round_trips_every_storage_and_copier_header_variant` completes the storage/framing cross-product on canonical vanilla and the retained Lunar Magic 3.63 transfer ROM: real movement saves from fixed and transferred-source storage, reopens as exact expanded ownership, moves and republishes that expanded owner a second time, preserves an absent or exact 512-byte copier prefix, repairs the checksum, restores both prior physical images with Undo, reapplies both with Redo, and produces byte-identical logical results across header variants. `complete_event_workflow_matches_across_copier_header_variants` now uses genuinely distinct headerless and headered inputs rather than duplicating the logical image. |
| Overworld | Edit paths and directional links | P | P | P | P | P | Pass | Standalone and ROM editors pass. The portable path form now distinguishes deliberate one-way edges from field-complete reciprocal pairs: it discovers and loads the exact reverse direction, exposes its independent exit and raw flags, and upserts/removes both stable keys in one failure-atomic revision so strict validation never requires an invalid intermediate graph. `LMOPEDT1 edge reciprocal` and `edge remove-reciprocal` expose the same semantic transaction, clear only the owned one-way bit, preserve independent unowned fields, save/reopen canonically, undo/redo together, and roll back an earlier forward removal when the reverse is missing. `authentic_navigation_edits_match_across_copier_header_variants` edits the installed path runtime together with the authentic warp runtime in a real Lunar Magic 3.63 transfer result, semantically reopens both domains, preserves exact copier-header state, validates checksums, restores the exact original ROM with two Undo operations, reapplies both with Redo, and produces byte-identical logical results across headered and headerless inputs. A retained isolated-Wine Lunar Magic 3.63 oracle now covers the original Submap Exit Tile Settings gesture boundary. Selection mode 2 and valid Layer 1 record `$009D` (tile type `$83`) open command `$2074`; changing native link 4 from Left to Up and its return selector to one-way transforms the exact interleaved record from `a000d8000050015800021405` to `9800e00000ffffffffff1405`, preserves the target and all thirteen other links, and saves a checksum-identified ROM. The paired rejection selects record `$0000` (tile type `$00`), receives the exact `Wrong type of tile!` / `An exit tile must be selected to use this.` error, and leaves the complete ROM SHA-256 unchanged. Ghidra `ValidateSelectedOverworldWarpTile` at `$00538B00` recovers the complete thirteen-type acceptance set. `lunar_magic_exit_tile_predicate_accepts_exact_recovered_set`, `retained_lm363_left_to_up_one_way_route_transition_is_exact`, and the route regression suite bind that fixture to the installed Rust model and GUI. |
| Overworld | Edit exit/warp links | P | P | P | P | P | Pass | Native workflow and authentic installed-ROM variants pass. The recovered current runtime count is a byte length rather than an entry count; decoding rejects zero and odd lengths, while save and legacy migration publish `entries × 2`. Installed 27-link tables are editable instead of being misread as 54 overlapping entries. Saving preserves unowned or non-exclusive source storage, reclaims only exact plane-owning RATS blocks, and atomically republishes one canonical four-plane allocation. `separate_exact_plane_owners_are_reclaimed_and_republished_atomically` covers split ownership, while `authentic_navigation_edits_match_across_copier_header_variants` covers a real Lunar Magic 3.63 untagged installed table through edit, path composition, reopen, checksum, exact Undo/Redo, copier-header preservation, and logical identity. Ghidra separates the original gesture's two predicates: stride-two Layer 1 types `$5A/$5B/$5F/$82` alone enter the two-click star/pipe route through `ValidateSelectedOverworldExitTile` at `$0053A2B0`, while the thirteen Submap Exit Tile Settings types remain disjoint. `lunar_magic_exit_path_predicate_is_distinct_and_exact` exhaustively binds all 256 type values. Instruction-level recovery of `OverworldWarpLinkDialogProc` proves all 258 source/return combo rows: No Setting at 0, one-way at 1, and native records `$00..$FF` at rows 2..257. `OverworldWarpReturnChoice` models that exact bijection and rejects row 258; `return_choice_covers_all_258_original_combo_rows_exactly` binds the complete boundary. The retained isolated-Wine Lunar Magic 3.63 oracle now completes the original success and rejection boundary. Layer 1 selection records `$003A/$00EB` (both raw type `$82`) open `Link Star and Pipe Tiles`; changing combo rows 3/5 to 27/28 and saving moves the selected pair into records `$19/$1A`, clears their prior owners `$01/$03`, preserves all other records, and produces the exact retained canonical `LMOWWR1` transition across all four planes. Selecting record `$0000` (type `$00`) produces `Wrong type of tile!` / `A star, pipe, or exit tile must be selected to use this.` and a following save leaves the complete ROM hash unchanged. `retained_lm363_four_plane_warp_transition_and_rejection_are_exact` decodes both complete compact tables, proves the exact four-record difference, binds rows 27/28 to records 25/26, and checks the rejection fixture |
| Overworld | Edit level settings, names, messages, player starts | P | P | P | P | P | Pass | Individual native workflows and the retained Lunar Magic overworld transfer oracle pass. `complete_metadata_workflow_updates_installed_runtimes_across_header_variants` composes seven real application transactions on canonical headered and headerless SMW-US ROMs: it installs the seven-record expanded overworld settings runtime, installs all 100 level names, installs 200 messages, edits the second player's submap and coordinates, then updates settings, names, and messages through their installed-runtime paths. Every final domain semantically reopens, exact physical copier-prefix state is preserved, checksums remain valid, the two logical ROMs are byte-identical, and seven Undo commands restore each exact original physical image. Existing native forms and bounded import/export tests cover the individual file and interaction boundaries. |
| Overworld | Edit sprites and custom overworld sprite sidecars | P | P | P | P | P | Pass | Models and appearance editor exist. `LMOWAPP1` multi-tile definitions support direct atomic painter-order movement by index/before-index or end across controller, bounded `LMOWAED1` script, and native GUI; canonical save/reopen, one-step undo/redo, late-index rollback, and built-process Unicode-path coverage preserve every part field. The direct-manipulation composition preview shares the renderer's exact 8×8 signed-offset geometry and painter order, retains the origin, labels tile/palette/flip semantics, and targets the topmost overlapping part. Dragging shows pixel-snapped bounded coordinates live but commits exactly one typed replacement on release. Focused arrows move the selected part one pixel and Shift+arrow moves it eight; Alt applies either step to the whole composition through one atomic `TranslateParts` revision. Every group offset is staged before assignment, so one signed overflow blocks the entire move history-neutrally, while Ctrl/Command chords remain untouched. Focused `X`/`Y` toggle only the selected part's horizontal/vertical flip, Page Up/Page Down move it exactly one painter-order step while selection follows, Insert duplicates it immediately above its current painter position, and Delete removes it while retaining a valid neighboring selection. Modified chords, painter-order boundaries, and deletion of the required final part remain untouched. A distinct fixed-width typed clipboard domain preserves tile, palette, signed offsets, and flips for single-part copy, request-captured paste-over/paste-after, and one-revision duplication. It also transfers nonempty complete painter-ordered compositions: replace and append are one first-class part-set replacement, while paste-as-new inserts a stable-ID definition and all parts in one staged revision. Wrong-domain, malformed, empty, duplicate-ID, invalid-palette, excessive-count, and stale deliveries preserve the document. Lunar Magic's actual UTF-8 `.sscov` sidecar now has a bounded semantic decode/encode model for regular/custom IDs, tooltip flags, shadows, point-based Sprite Map16 compositions, text labels, translucency, and external graphics/palette ranges; duplicate sprite lines reproduce last-write behavior. The recovered `.s16ov` codec owns its exact zero-filled `0x4000`-byte custom band (`$400..$BFF`), and the renderer resolves all built-in/custom/internal Map16 quadrants with offsets, palette/priority/flips, shadow and translucency metadata plus positioned labels. The lossless native GUI directly opens `.sscov` with its required `.s16ov`, edits every tooltip/display/range/custom-Map16 field without portable conversion, retains painter order, and atomically saves/reopens both files with shared undo/redo. The built CLI boundedly normalizes the native pair and emits field-complete semantic observations in one collision-safe create-new batch, including exact `.s16ov` prefix identity; its Unicode-path process test retains native-only fields. The installed-overworld preview now composes those appearances with each placement's real submap, exact base/SP/animated cache family, all ten tile bits, active `$00/$80` palette offset, and bounded external `ExSpriteGFX00` plus MW3/RGB palette routes; internal definitions use the authenticated editor cache. Full renderer, external-asset, route-table, and authentic-ROM open tests cover the live path. The native custom-stream application controller now stages insert, replace, delete, and move-before across all seven maps; canonically reopens the complete variable-width stream; reuses only authenticated ownership; repairs the checksum; and passes application Undo and stale-publication gates. Invalid IDs and 25th-per-map placements reject the whole batch atomically. Descriptor-derived stream and record-size routing covers pristine SMW, ExLoROM upper bodies, SA-1, and relocated All-Stars bodies; installed size tables require exact RATS ownership, vanilla empty sentinels open as editable maps, and the installed GUI stages list insert/replace/delete/reorder plus placement at the canvas selection with live native-appearance rendering. Terrain, ordinary records, animation options, and native sprites now allocate against one materialized staging image and publish as one checksum-valid mutation and Undo step; a forced-growth collision gate proves semantic reopen of both domains. A dedicated canvas tool maps main/shared preview planes to map-local coordinates and directly appends or moves the selected native record; cross-plane clicks reject atomically and leaving the panel restores Select mode. Custom records on maps 1–6 now receive the required +512-pixel render offset into the shared submap plane while retaining map-local ROM coordinates; `native_custom_sprite_placements_route_every_map_to_its_canvas_plane` locks all seven routes, and the complete 512-level renderer audit remains green. Canvas point and marquee selection now use the same painter-ordered resolved elements used for drawing: signed-offset 8x8 subtiles, internal text definitions, unresolved 16x16 Map16 footprints, and exactly measured native-font labels. Only sprites without drawable definitions use their 8x8 placement anchor fallback. The active map remains isolated, point selection chooses the later overlapping record, while a marquee selects every intersecting owner. Live Ghidra recovery of `HandleOverworldSpriteLeftButtonDown` `$0055BFB0`, `ApplyOverworldSpriteSelectionRectangle` `$0055AFF0`, `HitTestOverworldSpriteAtGridPoint` `$004D2250`, `ConstrainOverworldSpriteMovePosition` `$0055B690`, and `FindValidOverworldSpriteMoveOffset` `$0055B4C0` proves plain-clear/select, Ctrl-toggle/additive marquee, whole-selection dragging, grid constraint toward the origin, and one finalized Undo gesture. The installed Rust editor retains a map-local selection set, accepts Ctrl or Command toggles, updates an exact-footprint marquee from its baseline, shows selection/marquee overlays, and applies all moved records as one ordered controller batch on release; explicit canvas-cursor placement remains available. `rendered_element_hit_test_uses_exact_regions_and_reverse_painter_order`, `native_custom_sprite_hit_test_uses_rendered_label_geometry_on_the_selected_map`, `native_sprite_canvas_hit_test_selects_topmost_record_on_the_active_plane`, `native_sprite_selection_toggle_and_marquee_rectangle_are_deterministic`, and `native_sprite_group_drag_is_one_ordered_constrained_edit_batch` cover geometry, painter order, fallback, plane isolation, selection semantics, snap/no-op, and group-boundary fallback. Live `HandleOverworldEditorKeyboardShortcut` at `005510D0` and the `2455`/`2456` command branches prove Ctrl+C/Ctrl+V are intentionally tile-only and no-ops in sprite mode; Ctrl+A selects all and unmodified Delete removes the selected eligible sprites. `PasteCustomOverworldSprites` at `0055C350` and `DuplicateCustomOverworldSpritesAtPosition` at `0055C140` prove right-button duplication copies the complete selected custom group, selects the copies, constrains them at the pointer, and enters drag. Rust now focuses the canvas gesture surface, exposes Ctrl/Command+A and Delete, orders group removals descending in one controller batch, and defers right-drag copy plus final positioning into one atomic batch on release. `native_sprite_right_drag_duplicates_the_complete_group_and_delete_is_descending` covers stable copy order, coordinate preservation, plane rejection, capacity rejection, and deletion index stability. `EditCustomOverworldSpriteProperties` at `0055BE60` proves Alt-right-click is a distinct painter-topmost single-sprite property edit and suppresses duplication; accept writes one record while cancel is history-neutral. Rust now opens a dedicated field-complete modal for the hit record, blocks competing mutations while it is open, applies one typed replacement only on acceptance, and retains the existing multi-selection. `native_sprite_selection_toggle_and_marquee_rectangle_are_deterministic` also locks the Alt-hit versus ordinary-duplicate routing. A retained Lunar Magic 3.63/Wine fixture now binds the original Ctrl selection, Alt-right `Modify Custom Sprite Manual (in hex)` modal, cancel path, and source-preserving two-record right-drag duplicate through three hash- and PNG-structure-bound full-frame captures; `retained_lunar_magic_overworld_sprite_gestures_are_hash_and_structure_bound` locks the artifact boundary. `native_custom_sprite_edits_match_every_supported_identity_mapper_header_and_storage_variant` closes the Variants gate with the complete 48-case product: SMW-J, SMW-NA, and All-Stars+World-NA; LoROM `$20`/`$30`, SA-1 `$23`, and ExLoROM `$32`; two independent tagged stream-pointer placements; and copier header absent/present. Every case executes insert, move-before, replace, and remove, semantically reopens through both the project loader and controller, retains a valid checksum, restores/reapplies the exact physical image through Undo/Redo, preserves the copier prefix, and produces byte-identical logical results across header forms. |
| Overworld | Edit event-number map, main/special reveals, tilemaps | P | P | P | P | P | Pass | The Lunar Magic transfer fixture covers physical and semantic changes, while native forms expose event-number, main-reveal, special-reveal, and event-tilemap editing. `complete_event_workflow_matches_across_copier_header_variants` composes those four real application commands on canonical headered and headerless SMW-US ROMs: it changes both halves of the event-number map, installs a 200-entry main reveal table, installs all 24 special reveals with directions, and installs both event tilemap planes. Every domain semantically reopens from the final ROM, exact physical copier-prefix state is preserved, checksums remain valid, the two logical ROMs are byte-identical, and four Undo commands restore each exact original physical image. Existing fixed-to-expanded, growth, second-update, native-file, GUI, and retained oracle tests cover the individual boundaries. |
| Overworld | Palette and ExAnimation editing | P | P | P | ~ | ~ | Partial | The installed editor now previews custom overworld graphics and palette ExAnimation from the staged compact records. It resolves the ordinary-versus-startup GFX33/GFX32 sources, submap AN2 source, and relative source bank; constructs every slot's first frame before display; then advances the native eight-way slot interleave. Play/pause, timer step, reset, Custom 0-F, One Shot 00-1F, Manual Frame 0-F, and independent passed-event controls invalidate the live texture. The preview exposes all four exact rates recovered from `CalculateOverworldAnimationTimerInterval`: 120/60/30/15 ms for 7.5/15/30/60 fps, processing 8/4/2/1 substeps per callback. Built-in tile, built-in palette, lightning, and custom slot materialization now share that substep clock, so equal substep positions render identically at every rate; 15 fps remains the original default. Overworld Event Manual 8-F replaces the level-only meanings, uses each Trigger Init value as its event number, retains an independent cursor for `$0F`, and selects the dual frame bank only when that event is passed. The installed path reads Lunar Magic's exact 67-word built-in overworld animation table through recovered descriptor field `+$5C4`: logical `$020000` for ordinary SMW and SA-1, active-body `$420000` for ExLoROM, and relocated `$1A0000` for All-Stars + World. It rejects a truncated or invalid selected table without probing another mapper's mirror, accepts both original 3bpp `$18` and expanded 4bpp `$20` source strides, materializes tiles `$75-$7F`, preserves the `$B480/$B700` fixed-group exception, reproduces all three seed rotations, and advances the two slow groups on native eight-substep boundaries. It also reads both exact eight-frame level-dot color cycles from the descriptor-backed vanilla shared-palette block, initializes them at native phase two, and writes only CGRAM `$6D/$7D`. The lightning preview resolves descriptor fields `+$904` and `+$90C` across SMW-US, SMW-J, All-Stars + World, SA-1, and ExLoROM without cross-family fallback; `descriptor_fields_route_na_japan_all_stars_sa1_and_exlorom`, `selected_family_is_authenticated_without_cross_identity_fallback`, and `native_lifecycle_loads_every_descriptor_lightning_family_and_rejects_selected_corruption` bind that source boundary. It reproduces the original wrapping wait counter, odd-substep scheduling, self-referential 128-byte selector copied from the game routine, exact delay/initial-color tables, pre-decrement display selector, four/eight-substep fade timing, current submap palette `$29-$2F` sources, CGRAM `$47` target, and vanilla `$F7` mask that enables it only in Valley of Bowser. Installed ROM-global records are resolved through Lunar Magic's authenticated split runtime pointer, limited to the recovered 32-slot global domain, use their own relative source setting and trigger state, obey the per-map global switch, and run after the 32-slot local domain so global destination ownership wins. The per-map options now load through the original `$020087` hook, `$0024E3` marker, runtime `+$4A` table operand and independent `$027709` lightning byte; the unused lightning bit is retained, dynamic storage is allocator-protected, and installed saves are atomic/checksummed/undoable. The native Animation panel exposes all five switches for each of the seven maps, disables only the four runtime-backed controls when that runtime is absent, keeps pristine lightning editable, invalidates the live preview immediately, and merges option changes with the complete nine-payload mutation so neither staged domain can be lost. On an authenticated clean pristine SMW-US LoROM workspace it now installs the dedicated overworld runtime recovered from `InstallOverworldAnimationRuntime` at `$004B2440`: the exact `$C20` executable template, `$15`-byte auxiliary owner, seven-byte mutable options owner, six identity-checked fixed writes, 25 explicit pointer relocations, and 108-entry local-word relocation table are published in one checksum-repaired, revision-bound, undoable mutation. Runtime detection authenticates every immutable runtime byte, all three core RATS owners and lengths, the complete fixed hook network, and all seven mutable auxiliary submap pointers while allowing the seven option bytes to vary; populated pointers must resolve to valid RATS-owned compact animation payloads, and partial or corrupt installations fail closed. The native Animation panel now boundedly imports and create-new exports the active compact domain as `.lmexan` using the profile's exact 256 size modes and record limit. Import accepts intentional cross-slot provenance, canonically reopens before replacing only animation state, preserves all eight other staged overworld domains, and shares the complete-transfer stale-revision, busy-edit, close, and persistence gates; `animation_file_replacement_is_domain_isolated_slot_agnostic_and_atomic` and `native_animation_transfer_round_trips_with_profile_modes_and_limits` cover the controller and file boundaries. Built-ins run before custom records so an owned custom destination wins. `built_in_overworld_tiles_use_rom_table_slow_groups_and_exact_seed_rotations`, `built_in_palette_cycles_level_dots_and_valley_lightning_without_touching_other_submaps`, `lightning_uses_the_exact_wrapping_wait_and_predecrement_color_sequence`, `authentic_pristine_rom_loads_vanilla_overworld_animation_address_table`, `authentic_pristine_rom_loads_exact_overworld_palette_animation_tables`, `pristine_descriptor_offsets_load_exact_vanilla_overworld_animation_options`, `installed_options_save_reopens_checksums_and_undoes_atomically`, `authenticated_pristine_fixture_installs_saves_options_reopens_and_undoes`, `detector_rejects_owned_runtime_or_auxiliary_corruption`, `map_options_merge_with_payload_edit_and_reopen_from_one_mutation`, and `resolved_overworld_animation_option_chain_and_lightning_operand_are_protected` bind the ROM sources, installer, authentication, and persistence boundary. `overworld_event_manual_replaces_level_trigger_slots_eight_through_f`, `overworld_preview_builds_all_first_frames_then_interleaves_graphics_and_palette`, `every_native_timer_rate_materializes_the_same_animation_substep`, `passed_event_state_selects_overworld_event_manual_second_bank`, `enabled_global_overworld_records_run_after_local_records_and_obey_the_map_gate`, and `overworld_animation_clock_uses_the_authenticated_native_preview_cadence` cover the custom-animation and timer boundaries. `descriptor_field_routes_every_supported_identity_to_its_active_body`, `selected_table_corruption_rejects_without_cross_layout_fallback`, and `native_lifecycle_uses_selected_identity_animation_table_without_mirror_fallback` bind mapper routing and corruption rejection. Ownership navigation is derived from the live winning graphics/palette records, and `authentic_lunar_magic_363_runtime_matches_every_owned_and_fixed_byte` binds the complete original-ROM installer differential, exact fixed writes, mutable seven-pointer auxiliary table, and authentic populated compact owner chain. Broader original-editor behavioral and supported mapper/identity variant evidence remains partial. |
| Overworld | Import/export complete overworld | P | P | P | P | P | Pass | The installed-ROM complete-overworld editor exposes bounded non-blocking import and create-new export for the nine-domain `.lmow` aggregate. Import requires the active profile shape, animation limit/modes, compact encoding, and palette ownership before one atomic staged replacement; source-slot provenance does not prevent intentional cross-slot copies. Export snapshots the complete staged aggregate and cannot overwrite. Active file work now gates every canvas, record, palette, animation, direct-mutation, commit, and close route rather than only the transfer buttons, so a completing import cannot overwrite an edit staged while its worker was running. Import also rejects a ROM revision change during loading. `active_complete_transfer_rejects_direct_mutation_entry_point` covers the native race boundary; `complete_file_replacement_is_atomic_validated_and_slot_agnostic` and `native_transfer_helpers_round_trip_all_nine_domains` cover validation and exact file semantics. The retained nine-domain transfer oracle remains passing. `complete_overworld_transfer_matches_every_supported_identity_and_layout_variant` completes the supported 48-case product: SMW-J, SMW-NA, and All-Stars+World-NA identities; LoROM `$20`/`$30`, SA-1 `$23`, and ExLoROM `$32` map modes; exact copier-prefix absence/presence; and default/relocated pointer-table families. Every case encodes and decodes a cross-slot `.lmow`, changes all nine modeled domains, commits the complete aggregate atomically, semantically reopens every domain, repairs the checksum, restores the exact physical source with Undo, reapplies the exact result with Redo, preserves the copier prefix, and produces byte-identical logical results across header variants. |
| Title | Edit/import/export title-screen tilemap | P | P | P | P | P | Pass | Pristine/expanded transaction and Lunar Magic transfer evidence pass. `title_tilemap_install_and_update_match_across_copier_header_variants` drives the application transaction from pristine storage into the owned expanded runtime and then replaces that expanded payload again on canonical headered and headerless SMW-US ROMs. Every step preserves the exact physical copier-prefix state, repairs the checksum, reopens the complete tilemap and authenticated expanded ownership, and produces byte-identical logical ROMs; two undo operations restore each exact original physical image. Native bounded import/create-new export and the retained Lunar Magic `.lmtile` transfer fixture cover the file boundary. |
| Title | Record/import/export/play title movements | P | P | P | P | P | Pass | Native `LMTITL01`, minimal ZSNES V143 SRAM placement, and plain/gzip Snes9x tagged RAM-block formats preserve the exact `$4..=$8000` terminated movement payload. `install_and_update_match_every_supported_mapper_header_and_storage_variant` completes the 48-case ROM product across SMW-J, SMW-NA, and All-Stars+World-NA; LoROM `$20`/`$30`, SA-1 `$23`, and ExLoROM `$32`; copier-prefix absence/presence; and pristine first-install versus owned installed replacement. Every case publishes all three biased recording pointers and the runtime hook, semantically reopens the exact recording, repairs the checksum, preserves physical framing, restores/reapplies exact bytes with Undo/Redo, and produces identical logical output across header variants. `runtime_and_recording_pointers_preserve_mapper_significant_high_banks` additionally guards mapper-correct runtime/data publication: only LoROM uses its equivalent low-bank mirror, while ExLoROM and SA-1 retain significant high-bank state. The native editor now also installs and removes Lunar Magic's temporary level joypad recorder through revision-checked commands with the recovered overworld-RAM warning. A retained PID-scoped Lunar Magic 3.63 oracle proves commands `$1F46/$1F47`, both native confirmation dialogs, two exact hooks, the 178-byte RATS runtime, low-bank relocation, additive `$07EFA3..$07F08D` checksum compensation, Cancel atomicity, and byte-exact reciprocal uninstall. `install_matches_retained_lunar_magic_oracle_and_uninstall_is_reciprocal` requires the complete installed SHA-256 and all 347 changed bytes. A second retained `$1F44` oracle imports deterministic `12 34 56 FF` ZSNES data and proves the exact confirmation text, both cancellation boundaries, recording-before-runtime allocation, fixed zero initialization word, no unnecessary ROM growth, stored-checksum preservation, all 335 changed bytes, and complete original/Rust output SHA-256 equality through `playback_import_matches_retained_lunar_magic_oracle_byte_for_byte`. The authenticated `-ImportTitleMoves`/`-ExportTitleMoves` batch routes additionally prove the valid import ROM hash, byte-identical 134,163-byte ZSNES export, exact truncated-state rejection with unchanged ROM, and absent-playback export rejection with no output. The same oracle against the 524,800-byte vanilla copier-headered ROM proves the affirmative not-enough-room path: exact 1 MiB expansion, ROM-size and metadata initialization, boundary allocation, stored-checksum compensation, unchanged copier prefix, complete SHA-256 equality, semantic reopen, and byte-exact Undo through `vanilla_playback_import_expands_and_matches_lunar_magic_byte_for_byte`. `rust_title_recorder_captures_real_joypad_input_in_snes9x` boots the byte-identical vanilla recorder result in an official Snes9x core, enters level mode through real controller input, and requires the exact recorder-produced B/A/release stream at WRAM `$7F:0000`, its `$0042` marker and bounded length metadata, a nonblank gameplay frame, playback installation, and semantic reopen. |
| Credits | Edit/import/export credits tilemap | P | P | P | P | P | Pass | Legacy/expanded rows and Lunar Magic transfer evidence pass. `credits_tilemap_install_and_update_match_across_copier_header_variants` drives the application transaction from the original 202-row legacy representation into the complete 256-row owned expanded runtime and then replaces that expanded payload again on canonical headered and headerless SMW-US ROMs. Every step preserves the exact physical copier-prefix state, repairs the checksum, reopens the complete tilemap and authenticated expanded ownership, and produces byte-identical logical ROMs; two undo operations restore each exact original physical image. Native bounded `.lmcred` import/create-new export and the retained Lunar Magic transfer fixture cover the file boundary. |
| Patches | Install and migrate expanded ExAnimation runtime | P | P | P | P | P | Pass | Ghidra enumerates all three branches of `EnsureExpandedExAnimationRuntimeInstalled` (`0045FD50`). The strict probe now distinguishes absent, legacy pointer-hook, legacy global-table, and current states using the active SMW-US descriptor: shared pointer/current hook `$02390`, legacy-global hook `$02418`, and core hook `$0283AD`. It authenticates the current core, `$600` table, `$20`/`$30` helpers, sentinel, shared-palette family, and allocator-dependent `JSL core+$170; RTS` hook. Three independently authored Lunar Magic 1.71/1.82/1.91 ROMs additionally authenticate the historical `$C30` `LM 01 01` current family: after canonicalizing only proven mutable/relocation spans all cores share digest `10dbd77a94ddb479cf4ac83360c824ccf97f58dbbc55f61770465369e6cc90e0`. The strict Co-op oracle covers the complete family, LoROM-mirrored shared hook, ownership and corruption rejection; the Karoshi oracle traverses all 512 slots and semantically round-trips every populated compact payload. Provenance is retained in `docs/oracle-work/expanded-exanimation-generation-101.md`. An authentic unlocked Lunar Magic 1.65 legacy-global before/after pair now proves the second coordinator migration branch reciprocally across all 512 slots and four populated animations. It corrected the runtime-owner model (the entry can live inside the `$140` auxiliary owner), authenticated source-snapshotted fixed-hook replacement, and exposed all twelve IRAM words as allocation-relative `Low16` fixups at an irregular `$0A5D51` destination. The global-table detector resolves the runtime prefix, its `+$1A` `$600` table, and the `$02418`-selected auxiliary table while allowing their historically intentional runtime/auxiliary overlap and rejecting unsafe pointer-table overlap. Provenance is retained in `docs/oracle-work/expanded-exanimation-legacy-global-165.md`. An authentic unlocked Lunar Magic 1.70 pointer-hook ROM now closes the remaining coordinator branch: a real dirty level-ExAnimation save changes the two runtime bank bytes from `$00` to `$10` and advances only the marker generation byte from `LM 00 01` to `LM 01 01`, exactly matching Rust's three guarded fragments; the application migration repairs the checksum and undoes to the byte-exact physical source. Its isolated Wine capture, hashes, and scoped 1,295-byte save delta are retained in `docs/oracle-work/expanded-exanimation-legacy-pointer-170.md`. The native command stages exact current-runtime installation, converts all 512 old pointer slots and `$23`-byte records through `migrate_legacy_exanimations`, publishes compact allocations, erases obsolete storage, repairs checksum, and collapses the result into one revision and one byte-exact Undo; live-record reopen, headered/headerless framing, and late malformed-source atomicity pass. Fresh installation now also writes the previously omitted `$02390` hook and matches the retained Lunar Magic output. The optional `$20` mapper suffix, 37+3 IRAM relocation pass, and its two final pointer relocations are modeled. Its ROM-aware selector reproduces ExLoROM declaration/enable bits 1/17, SA-1 bits 2/18, and the legacy `$002D2B > $1FFF` fallback exactly, while rejecting partial metadata. ExLoROM addresses the conversion's active relocated SMW body at logical `+$400000`; SA-1 retains base offsets. The complete mapper plan resolves all eight canonical mapper pointers, 108 local words, the concrete suffix pointer, and `$7FC020`; installs both allocated helpers, the sentinel, canonical shared-palette hooks, adjusted fixed helpers, and `$025E1` NOPs; and independently detects corruption. `mapper_expanded_exanimation_install_reopens_preserves_header_and_undoes_exactly` proves the complete eight-case ExLoROM/SA-1 × `$C30`/`$C50` × headered/headerless application matrix, including strict reopen, checksum validity, duplicate rejection, physical-header preservation, and byte-exact Undo. Lunar Magic 3.63 command `$23B4` plus `-ImportLevel` now supplies an authentic 8 MiB copier-headered ExLoROM before/after Oracle. `authentic_lunar_magic_exlorom_runtime_family_matches_every_owned_byte` reconstructs allocation-dependent addresses and compares every byte of its `$C30` core, live `$600` pointer table, both allocated helpers, hooks, NOPs, sentinel, and shared-palette family against the compact retained fixture. |
| Patches | Install expanded settings, Layer 3, palette runtimes | P | P | P | P | ~ | Partial | Four expanded-settings generations are recovered. Expanded-settings detection distinguishes Lunar Magic's unrelated first-fit `$087FF8` RATS allocation from the owned `$6E00` block by authenticating the complete fixed runtime destination family, while damaged installed ownership remains a hard failure. When that ordinary `$8000` allocation occupies the preferred bank tail, Rust now matches Lunar Magic 3.63's next-bank result exactly: header `$090000`, payload `$090008`, record table `$092D08`, all ten allocation-dependent fixups (including the complete table low word), the `LM 03 01` generation marker at `$07F15C`, and the complete fixed runtime family. The marker was recovered from Ghidra's threshold checks at `004609D0`/`00460A30` and is now an expected-byte-guarded member of the same atomic transaction. Installed loaders and canonical revision-profile controllers derive the table from the authenticated runtime operand instead of assuming first fit. The authenticated Lunar Magic 2.42 `LM 02 01` family now migrates atomically: Rust validates its exact `$6E00` owner and immutable runtime, normalizes the 512 standard records with the recovered reference-only pass, retains all eight trailing records exactly, reclaims the old owner in staging, installs current storage/runtime, repairs/reopens, and undoes byte-exactly. Lunar Magic 2.22's pre-Layer-3 generation 1.01 is also covered at both authentic default and forced-relocated placements: the exact `$6D00` owner, companion `LM 01 01` marker, six live relocation groups, and SHA-256-authenticated immutable runtime/hook family are required; its 512 records are normalized and eight absent special slots initialized during atomic conversion to current `$6E00` storage. Lunar Magic 1.71 generation 1.00 is now authenticated by two independently authored patch-derived ROMs: the older fixed `LM 00 01` marker, exact `$6D00` owner, the same six relocation groups and immutable runtime/hook digests as generation 1.01, 512-record reference normalization, eight-slot initialization, atomic replacement, current-runtime reopen, corruption rejection, and byte-exact Undo all pass. One dense real ROM also forced an irregular destination and exposed the formerly low-byte-only table operand; it is now a complete 16-bit relocation guarded by `irregular_relocation_updates_the_complete_table_low_word`. Ghidra's `AllocateRomSpaceWithExpansion` at `004A8810` proves that the original searches the complete current mapper extent and calls `RequestRomExpansionForAllocation` only after a failed search. The native install route now does the same for pre-expanded/no-settings LoROM sources: it preserves pristine SMW's authenticated one-MiB target, searches all existing space through 4 MiB, then retries failure-atomically one 32-KiB mapper bank at a time. `preexpanded_headered_and_headerless_roms_use_existing_late_space_and_undo_exactly`, `exhausted_preexpanded_rom_grows_one_bank_reopens_and_undoes_atomically`, and `application_installs_settings_into_preexpanded_headered_rom_and_undoes_exactly` cover existing-space placement, minimal growth, both copier-header forms, semantic reopen, checksum repair, one application revision, and exact Undo. `generation_102_migration_authenticates_normalizes_reclaims_and_undoes`, all three external historical-generation gates, `authentic_first_fit_collision_relocates_settings_to_the_next_lunar_magic_bank`, `authentic_first_fit_collision_installs_relocated_settings_across_header_variants`, `canonical_profile_resolves_a_relocated_expanded_settings_table`, the built CLI process collision case, and the live `lunar_magic_and_rust_match_relocated_expanded_settings_owned_bytes` reciprocal oracle cover corruption rejection, exact owned bytes, relocation, both copier-header forms, checksum repair, semantic reopen, and byte-exact Undo/Redo. Generation 1.00 provenance and hashes are retained in `docs/oracle-work/expanded-settings-generation-100.md`; broader mapper/identity variants remain open |
| Patches | Sprite 19 ASM fix and Lfix3 families | P | P | P | P | P | Pass | The native revision-bound built-in runtime dialog exposes both the complete recovered SMW-US-v1 Lfix3 core and Lunar Magic's user-requested sprite `$19` ASM fix as first-class transactions. It reports the exact authenticated Lfix3 generation before migration, distinguishes generation 1's packed-table conversion from generation 2's three-plane preservation, disables authenticated current installs, rejects stale revisions, and closes only after a successful atomic command. The Sprite 19 path was recovered from command `$26AC` and a matched pristine-ROM Lunar Magic 3.63 Wine oracle: it identity-checks and installs the exact six-byte `$00E762` hook, fixed `$20`-byte `$01BCA0` helper, and three-byte `$0020A0` branch removal, repairs the checksum, authenticates the result, commits one revision, rejects duplicate or corrupt forms, and undoes byte-exactly. A retained second Lunar Magic 3.63 command `$26AC` oracle proves that when the authenticated shared helper is already installed, only the final branch removal is fix-specific; the complete original transaction, including first-save metadata, is embedded as an IPS regression fixture. The Lfix3 path allocates the `$510`-byte runtime, applies 107 typed relocations and all helpers/hooks/tables, and strictly authenticates immutable code while allowing mutable per-level tables. Its recovered generation probe authenticates generation 1 from the exact `$02D7CE` JSL and complete `$02DC50` helper, generation 2 from its `$240` RATS runtime and immutable network (including the legitimately retained generation-1 hook), and generation 3 from the complete current `$0111` runtime. Both legacy generations now migrate transactionally. Generation 1 requires every later fixed write and new table destination to retain its pristine precondition, converts the live packed plane into the recovered two-plane form, initializes the third plane, installs the current runtime, authenticates the result, and undoes exactly. Generation 2 reclaims its authenticated owner in staging, relocates the current runtime into the reusable run, preserves all three live tables byte-for-byte, and keeps late failure atomic. Native/application installation routes both migrations. `built_in_runtime_installer::workspace::tests::migration_copy_identifies_every_authenticated_legacy_generation`, `built_in_runtime_installer::tests::lfix3_runtime_selection_installs_reopens_and_undoes_exactly`, `built_in_runtime_installer::tests::sprite19_fix_selection_installs_authenticates_and_undoes_exactly`, `lfix3_install::tests::generation_1_migration_converts_tables_authenticates_fixed_sites_and_undoes_exactly`, `lfix3_install::tests::generation_2_migration_reclaims_runtime_preserves_tables_and_undoes_exactly`, and `relocatable_patch::tests::replacement_late_plan_failure_does_not_escape_staged_reclamation` cover these boundaries. The Sprite 19 pristine/shared-helper source variants and all three authenticated Lfix3 generations are explicitly covered |
| Patches | Map16 staged hooks and compatibility upgrades | P | P | P | P | P | Pass | The revision-bound native built-in runtime dialog exposes the authenticated pristine SMW-US-v1 Map16 runtime transformation recovered from an unchanged complete Map16 import under Lunar Magic 3.63. One application command applies every identity-checked fixed-byte range, relocates the exact `$8000`-byte auxiliary table into the recovered 512-KiB-to-1-MiB window, publishes its typed low-bank operand, repairs the checksum, advances one project revision, semantically reopens the installed secondary Map16 storage, and undoes byte-exactly. Current-runtime detection no longer trusts the single `$22` marker: it authenticates every recovered fixed replacement byte outside the checksum and typed relocation, resolves the relocated low-bank operand, requires exact RATS ownership and length, and compares the complete auxiliary payload. Corrupt or marker-only forms reject, while an authenticated current install disables duplicate native installation without changing history. The generation probe authenticates all four recovered stages: `$0100` and `$0101` from their auxiliary prologue, final hook, and stage-specific compare-hook state; Lunar Magic 3.01's `$0111` network from the complete `$037600` hook base and untouched `$0377A0` destination; and current `$0112` from the complete stage-four helper. The shared transactional migration repairs the stage-one compare discriminator where required, replaces every generation-owned hook with its current form, leaves live Map16 tables and allocations untouched, rejects late authenticated-range changes atomically, repairs the checksum, records one revision, and undoes byte-exactly. The native installer identifies stages one through three as migrations and routes each through the same revision-bound application command. A mandatory embedded LM 3.01-to-3.63 IPS oracle now matches every stage-three upgrade runtime byte on every test run; only Lunar Magic's editor-version stamp and Rust's explicit checksum repair are excluded. The pristine 512-KiB and occupied 1-MiB source shapes both install: the latter expands to 2 MiB and places the bank-aligned auxiliary payload at `$108000`, matching the retained Wine permutation. Canonical-header and headerless application routes preserve physical framing, checksum-valid reopen, and byte-exact undo. `map16_runtime_install::tests::embedded_patch_decodes_and_installs_the_relocated_wine_shape`, `map16_runtime_install::tests::current_detector_rejects_marker_only_fixed_and_payload_modifications`, `map16_runtime_install::tests::stage_one_and_two_runtimes_are_authenticated_migrated_and_undo_exactly`, `map16_runtime_install::tests::early_stage_detectors_reject_owned_hook_corruption`, `map16_runtime_install::tests::early_stage_migration_rejects_changes_after_planning`, `map16_runtime_install::tests::stage_three_runtime_is_authenticated_migrated_and_undoes_exactly`, `map16_runtime_install::tests::stage_three_migration_rejects_staged_hooks_changed_after_planning`, `map16_runtime_install::tests::external_stage_three_oracle_matches_lunar_magic_upgrade_bytes`, `built_in_runtime_installer::workspace::tests::commands_are_bound_to_the_open_revision_and_selected_family`, `built_in_runtime_installer::tests::map16_runtime_selection_installs_auxiliary_hooks_and_undoes_exactly`, and `built_in_runtime_installer::tests::map16_runtime_selection_migrates_stage_three_and_undoes_exactly` cover the recovered and native boundaries. All supported SMW-US LoROM source sizes, copier-header forms, allocation banks, and compatibility generations are explicitly covered |
| Configuration | Main/user toolbar customization | P | P | ~ | P | ~ | Partial | Tools → Customize Toolbar stages the Rust layout, preserves stable IDs and localization keys, exposes all twelve native actions and separators, supports reordering/removal, validates atomically, restores the built-in toolbar explicitly, and persists canonical `LMTBAR01` layouts. The original-compatible startup path boundedly parses the documented UTF-8 `usertoolbar.txt` five-line format, implicit definition termination, internal/external targets, spacers, image/base directives, icon indexes, multiline tooltips, options, shortcut tokens, working directories, global flags, and `LM_NO_TOOLBAR`; a visible parsed toolbar renders as a distinct wrapped second row, mapped internal commands dispatch natively, and external commands expand documented `%1`–`%8` context without a shell before entering the existing permission gate. User-toolbar shortcuts take precedence over built-in bindings, remain active under `LM_NO_TOOLBAR`, and preserve the original's behavior of firing every duplicate assignment. Tokens cover quoted characters, generic/left/right Ctrl/Shift/Alt, F1–F24, insert/delete/home/end/page/arrow/tab/space/enter/escape/backspace, numpad digits, and equivalent raw Windows virtual-key hex values. The portable shortcut schema appends the previously missing navigation/editing key kinds without renumbering existing encodings. Startup boundedly loads each referenced Windows BMP, derives or honors the square icon size, splits horizontal strips, applies the first-pixel transparency key, retains directive-order entry starts, resolves signed indexes relative to `LM_NEWIMAGE`, and implements previous/global base changes at each button definition. Internal and explicit `LM_USEIMAGE_LIST` buttons render the resolved nearest-neighbor texture with tooltip fallback; `LM_USEIMAGE_FORCE` assigns sequential global cells only to external buttons that would extract icons, while `LM_USEIMAGE_FORCE_ALL` assigns every non-spacer button. `LM_NO_BUTTON` suppresses only the visual control while retaining its shortcut. Explicit working directories win; otherwise `LM_DIR_ROM`, `LM_DIR_LM`, and the original default/`LM_DIR_PROGRAM` executable directory are applied before the shell-free permission gate. Malformed geometry, oversized/non-regular/missing files, decode failure, list overflow, or out-of-range indexes cannot partially publish textures. The documented built-in main-toolbar `Lunar Magic.ff4` override is also loaded only at its exact 41-square-cell shape; retained live 3.63 `TBBUTTON` evidence maps native Open/Save/Undo/Redo to original bitmap cells 1/3/5/6, while absent or invalid overrides retain the textual toolbar. A separate live Lunar Magic 3.63/Wine observation proves the original creates a distinct visible two-button `ToolbarWindow32` beside its 52-button built-in toolbar from the same startup fixture. `user_toolbar::tests`, `user_toolbar_images::tests`, `application::toolbar::user_toolbar_tests`, `shortcut::tests`, `shortcut_editor::tests`, `toolbar_editor::tests`, `frontend_ui::tests::english_fallback_covers_every_typed_localization_key`, and `application::preference_tests` cover the native path. Ghidra-authenticated extraction now retains all 318 original table slots: 317 named commands plus the terminal null sentinel, including the duplicate `LM_EDIT_SELECT_ALL` entry and shared-ID aliases. Every named target parses, invented `LM_*` targets reject before native dispatch, authenticated unsupported targets report their original command ID, and 209 table slots route to implemented native commands, view actions, or the same Help/About, level-analysis, restore, IPS, Sprite 19, integrated object/sprite-placement, return-to-level, edit-domain, Select All, Delete, Delete All, Escape, and typed multi-record Cut/Copy/Paste, the four X/Y coordinate-adjustment commands and all six creation-order commands (two legacy one-step plus four rendered-overlap-aware front/back/forward/backward actions) with multi-selection, vertical-axis, position preservation, staged-Undo, ROM-reopen, and application-Undo coverage, and mapper-gated 2/3/4 MiB LoROM, warned 8 MiB ExLoROM, and warned 6/8 MiB SA-1 expansion workflows, plus fixed-directory ordinary and quick standard-GFX/ExGFX extraction with authenticated completion-versus-quiet behavior and quick standard/ExGFX insertion through conditional format warnings and atomic ROM commits, bounded mouse Back/Forward navigation, all-512-level MWL directory export, compression options, external-emulator/tile-editor setup, configured SNES emulator launch, and live-emulator Run, Unload, Pause/Resume, Mute, and Frame Advance through the same backend session state used by its native window. The configured launch rejects GBA and graphics-only profiles for an SMW test and falls back to the direct chooser only when no eligible SNES profile exists. Repeated default external-tool activation now restores/focuses the tracked child window; allow-multiple modes still create independent instances. `user_toolbar_internal_command::tests`, `every_native_internal_route_belongs_to_the_authenticated_original_table`, `original_run_emulator_prefers_configured_snes_profile_over_gba_and_unrelated_tools`, and `toolbar_controls_share_pause_step_mute_and_stop_state_with_the_live_window` bind that inventory. External buttons without bitmap-list or force overrides now extract their requested executable resource icon on Windows through bounded `ExtractIconExW` plus two-background RGBA reconstruction; quoted/relative/`%4` paths, default/indexed resources, override precedence, text fallback, GDI-handle cleanup, Windows cross-compilation, and an isolated Wine `notepad.exe` ABI oracle pass. Other editor-specific `.ff*` strips, native routing for the remaining authenticated commands, and Pause/numpad-operator shortcut distinctions remain incomplete. |
| Configuration | Localization | P | P | ~ | ~ | ~ | Partial | Tools → Language now reports the active locale, non-blockingly loads a standalone bounded `.lmlang`/`LMLOC001` catalog, validates and switches it atomically, persists exact canonical Unicode catalog bytes across launches, and explicitly restores built-in English. `localization::tests::published_encoded_limit_accepts_the_largest_valid_catalog`, `configuration_loader::tests::decodes_standalone_localization_catalog`, `application::preference_tests`, and `state::tests::frontend_configuration_replacement_is_validated_and_atomic` cover bounds, decoding, persistence, and reset. The append-only typed catalog now covers title/status/toolbar text plus the complete application menu surface: all eight top-level menus, every static File/Edit/View/Editors/Profile/Tools/Help action, dynamic language/emulator labels, and all 22 Documents families through reorderable localized templates. Existing 19-key `LMLOC001` catalogs upgrade in memory with English values for new keys and re-encode canonically without renumbering any original key. `localization::tests::legacy_nineteen_key_catalogs_upgrade_with_english_menu_fallbacks`, `frontend_ui::tests::english_fallback_covers_every_typed_localization_key`, and `application_menu_surface_has_no_literal_button_or_menu_labels` cover migration, completeness, and wiring. The same catalog now drives every title, action, status, template, workflow-guide title/body, original-index label, and translated search comparison in Help, About, and Compatibility diagnostics; the immutable compatibility report payload itself remains technical data. `help_dialog::tests::topic_search_uses_the_installed_translation` and `frontend_ui::tests::localized_text_selects_complete_catalog_or_english_fallback` cover translated lookup and fallback routing. The Editors menu's 24 installed-ROM editor open/close pairs now use localized edit/close templates and stable editor-name keys; its three MWL batch actions are localized directly. The source audit includes this generated pair surface, preventing a literal-label regression. Crash recovery, unsaved-change confirmation, global error acknowledgement, and Undo History settings also receive localized titles, explanations, dynamic revision/level/count templates, and actions without translating opaque error/report payloads. The executable-adjacent `sysLMLanguage` discovery route now deterministically scans at most 64 canonical `.lmlang` regular files, skips malformed/oversized/unreadable siblings like the original skips invalid DLLs, retains duplicate locales as distinct choices, and exposes every decoded locale directly in Tools → Language through the existing bounded background loader. With no persisted choice, ordered preferences normalize encoding/modifier/underscore forms, prefer an exact locale, then fall back to the first deterministic primary-language match; explicit and built-in persisted choices win. Windows obtains those preferences from the bounded `GetThreadPreferredUILanguages(0x30, ...)` multi-string used by Lunar Magic 3.63 and falls back to `GetUserDefaultUILanguage` plus `LCIDToLocaleName`; other platforms use ordered `LANGUAGE`/`LC_ALL`/`LC_MESSAGES`/`LANG` values. The unsafe Windows ABI is isolated behind the safe `lm-windows` boundary, which validates the returned count, UTF-16, bounds, and double-NUL termination. `lm_windows::tests`, `configuration_loader::tests::installed_catalog_discovery_is_bounded_filtered_and_locale_sorted`, `installed_catalog_discovery_skips_invalid_and_retains_duplicate_locales`, and `application::preference_tests::installed_language_autodetection_prefers_exact_then_primary_language` cover decoding, discovery, and selection; `cargo check -p lm-windows --target x86_64-pc-windows-gnu` proves the conditional API path compiles. Lunar Magic's CHM plus Ghidra-recovered `BuildSystemLanguageDirectoryPath`, `EnumerateAvailableLanguageModules`, `AutoDetectAndLoadLanguageModule`, `LoadSelectedLanguageModule`, `ValidateLanguageModuleChecksum`, `LanguageSelectionDialogProc`, `GetThreadPreferredUiLanguagesDynamically`, `BuildFallbackPreferredUiLanguageList`, and `GetPreferredUiLanguagesAsUtf8` provide indirect original evidence for application-relative discovery, built-in English, preferred-UI-language auto-selection, persistence, validation, and restart semantics, keeping the Oracle partial. Original DLL checksum, metadata/version, strings, and the complete 107-entry type-5 fallback map are implemented without executing modules. Applying localized template control text throughout the native editor and a reliable retained live language-DLL Wine fixture remain incomplete |
| Configuration | External tools and event subscriptions | P | P | P | P | P | Pass | The safe argument-vector launcher, bounded permission queue, cancellable child worker, and portable opened/saved/level subscriptions pass. Original-compatible user-toolbar tools now expand their direct argument vector and documented working-directory modes, autorun once per new-ROM transition unless globally disabled, and selectively remove pending requests or kill/reap matching children on new-ROM/application-close according to per-button and force-all lifecycle options. The original `LM_ALLOW_MULT_INSTANCES` and global force-all policy now create independently tracked concurrent children while the default suppresses duplicate requests from the same button; every instance retains its own cancellation/UI identity. Windows `LM_NO_CONSOLE_WINDOW` launches directly with `CREATE_NO_WINDOW`. `LM_OPEN_OTHER` uses the permission-gated platform association opener and deliberately retains no opened-application process ownership; Windows routes bounded UTF-16 target/parameters/directory through a safe `ShellExecuteW` wrapper, macOS preserves application arguments through `/usr/bin/open`, and other Unix systems reject unsupported argument-bearing requests. Focused tests cover transition de-duplication, exact queue cancellation, launch-policy selection, concurrent two-child ownership, opener argument boundaries, Windows quoting, and Windows cross-compilation. The original `$BECA`/`$6942` notification transport now packs all seven documented types, publishes launched child PIDs, enumerates all PID-owned top-level Windows without the obsolete visibility filter, and supplies type 0 with a retained caption HWND carrying the ROM path. Native ROM-open, level-change, and close transitions honor their exact per-button and force-all options. Installed level, Map16, and overworld domains coalesce independently and publish types 3–5 only after successful physical ROM persistence; failed saves and clean-baseline reversions emit nothing. Native single-level deletion now redirects every modeled level domain to the authenticated original-area test slot in one revision, is gated to expanded levels behind confirmation, reclaims only unshared tagged payloads, repairs checksum, undoes byte-exactly, and publishes type 6 only after successful physical ROM persistence to buttons with `LM_NOTIFY_ON_DELETE_LEVEL`. The default single-instance path now uses each tracked child PID to restore and focus its first visible top-level Windows window on a repeated button click, matching the authenticated CHM contract; pending tools or children without a window remain harmless no-ops, while both allow-multiple options still launch independently. Windows first-run migration now boundedly reads Lunar Magic's exact `HKCU\\Software\\LunarianConcepts\\LunarMagic\\Settings` profiles (`Emulator*`, `Emulator2*`, and `TileEditor*`) plus recovered `Options`/`Options2` bits, translates Microsoft quote/backslash parsing and `%1` into independent shell-free `{rom}`, `{rom_8dot3}`, or `{graphics}` arguments, skips absent profiles, and never overwrites an explicit native/startup configuration. Canonical `LMTOOLS1` bytes persist every native edit thereafter; malformed preferences preserve the active collection atomically. Ghidra-named profile loaders/savers and `SynchronizeApplicationSettingsRegistry`, the authenticated CHM, six migration/persistence/parser tests, an isolated seeded-Wine registry ABI test, and Windows cross-compilation cover the original and platform boundaries. Non-Windows builds retain portable configurations while correctly omitting Windows-only registry import. |
| Help | Help topics, diagnostics, version/about | P | N/A | P | P | P | Pass | Native Help → About exposes the Rust build version, clean-room identity, Lunar Magic 3.63 compatibility target, license, source link, and a source-URL clipboard action. Help → Compatibility diagnostics captures and copies one bounded, path-free report containing build identity, project/editor state, physical/logical size, copier header, stable and current identity validation, mapper/map mode/cartridge type, stored/computed checksum health, dirty logical range/byte counts, valid RATS allocation counts, revision-profile audit results, classified Layer 2 formats, and authenticated Map16/Lfix3 runtime generations. Runtime probes are explicitly not applicable outside their SMW-US LoROM/ExLoROM family; pristine, headered, SA-1, checksum-corrupt, identity-corrupt, and partial-runtime fixtures cover the reporting boundary. `state::tests::compatibility_report_is_path_free_and_classifies_pristine_runtime_state`, `compatibility_report_distinguishes_headered_and_sa1_variants`, `compatibility_report_surfaces_checksum_and_partial_runtime_corruption`, and `compatibility_report_detects_when_current_bytes_break_opened_identity` provide deterministic evidence. The compatibility report is an explicit native extension rather than a surrogate for an original command. The native Help browser also retains the complete hierarchical table of contents from the authenticated Lunar Magic 3.63 CHM (SHA-256 `6ff2a44ff32902aed11d1969970e2c19a91ef336c29795fed823b78e577d60be`): all 314 section/topic nodes at their original depths, including 281 routed entries and 275 distinct HTML routes. Titles and routes are searchable alongside the Rust workflow guides without redistributing proprietary topic bodies; `help_dialog::tests::retained_original_index_covers_every_routed_363_topic` and `original_topic_search_matches_titles_and_routes` lock the complete inventory and search boundary. A retained live Wine fixture now proves command `$25E5` opens resource `$03F8` as the modal `About Lunar Magic...` window, records its identity, version, build, URL, two auxiliary actions, and OK controls, and proves OK closes only the dialog while the `LMFrame` remains alive. `about_dialog::tests::retained_lunar_magic_about_oracle_matches_the_compatibility_target` binds the fixture to the Rust 3.63 target. Port-8089 recovery now closes the original CHM contents gesture: `OpenLunarMagicHelpFile` at `00440F90` selects a localized or executable-adjacent CHM, removes its zone stream, invokes `004E4870` with HTML Help command/data zero, and retries through `004E4B30`; level and overworld callers pass the owner window rather than a topic route. Rust exposes the same contents action for an installed adjacent regular `Lunar Magic.chm` through one shell-free platform process, reports missing/non-file/process-start failures without closing Help, and never bundles or mutates the proprietary file. `original_help_discovery_accepts_only_an_adjacent_regular_file` and `original_help_launch_uses_one_direct_platform_process_without_a_shell` bind the retained dispatch TSV and publication boundary. The executable's 600-byte resource `$03F8` now binds the 248×160 parent plus all ten child geometries and roles; Ghidra `00415970` binds version/build/website initialization, URL launch/copy, explicit OK, and buttons `$66/$67` to modal resources `$429/$42A`. Rust retains those proportions and semantic controls with localized build identity, source launch/copy, explicit dismissal, and clean-room third-party/legal notices. The retained layout TSV and `retained_lunar_magic_about_oracle_matches_the_compatibility_target` cover the resource and live-control evidence. The retained Help-menu TSV and Ghidra builders `00449DC0`/`0054A420` prove both original Help surfaces contain exactly two commands—Contents `$25E4` plus their About action—and no diagnostics entry; ROM scanning/RATS reporting remains assigned to its actual File/Options workflows. This closes the original Help command inventory without using the Rust-only compatibility report as false parity evidence. |
| Release | Installer, portable packaging, updates, crash recovery | P | P | ~ | - | ~ | Partial | The safe-Rust `lm-package` tool creates deterministic create-new `.tar.gz` bundles containing `lm-native`, `lm-cli`, `lm-libretro`, README, both license texts, and a versioned `LMRELEASE1` manifest with the byte length and SHA-256 digest of every non-manifest payload. It normalizes tar ownership, timestamps, modes, ordering, and gzip time; accepts only bounded regular inputs and portable target/version components; removes a newly created archive if checksum publication fails; and never replaces an existing archive or checksum. Each native-editor process now reserves a unique bounded `LMRECOV1` record and lifetime advisory lock, then writes every new dirty project revision off the UI thread with both the exact last-saved physical baseline and current unsaved ROM. Same-directory temporary-file publication prevents partial records; concurrent editors cannot overwrite or claim one another's live snapshots; startup boundedly queues up to 16 stale sessions and offers each for independent recovery or discard. Recovery opens an unnamed dirty project that requires Save As, while clean saves and normal closes remove only that session's record. `state::tests::crash_recovery_restores_exact_dirty_bytes_level_and_saved_baseline`, `recovery_store::tests::multiple_session_records_are_queued_and_removed_independently`, `discovery_skips_records_owned_by_a_live_session`, and `record_round_trips_and_rejects_corruption_trailing_bytes_and_clean_state` cover the boundary. Editor-local staged forms, installers, code signing/notarization, release publication, updates, hosted bundle proof, and original-behavior evidence remain missing |

The Patches/runtime row additionally routes authenticated SMW-US SA-1 sources through the recovered
SA-1 expanded-settings hook and runtime family instead of rejecting their mapper. The native
`InstallSettings` command preserves its revision gate and installs the owned allocation as one
checksum-valid mutation. `application_installs_sa1_settings_reopens_and_undoes_exactly` proves
command reachability, mapper-stable reopen, one history revision, and byte-exact Undo. Expanded
shared/custom palettes are now descriptor-routed through the relocated SMW body after an actual
LoROM-to-ExLoROM conversion, while checksum repair correctly remains on ExLoROM's authoritative
low first-bank header. `converted_exlorom_installs_edits_reopens_and_undoes_expanded_palettes`
proves native install, installed-layout resolution, palette edit/reopen, checksum validity, and all
three exact Undo boundaries. The authenticated expanded-settings detector now follows the same
mapper transition: it reads the relocated runtime operand, maps its RATS owner with ExLoROM
semantics, authenticates every relocated fixed runtime byte, and returns an ExLoROM table layout.
`converted_exlorom_resolves_and_edits_the_relocated_settings_owner` preserves a non-default level
record through conversion, reopens and edits it, repairs the authoritative checksum, and undoes
the exact converted image. `complete_feature_survives_exlorom_conversion_with_every_owned_byte`
then covers the combined family: all five Layer 3 RATS owners, the expanded-settings owner, and all
fixed runtime writes are byte-identical in the relocated SMW body, the settings descriptor reopens,
the mapper checksum remains valid, and one Undo restores the exact pre-conversion installation.
Alternate identity, direct post-conversion Layer 3 installation, and the remaining SA-1 combined
runtime variants keep the row `Partial`.

The Configuration/Localization row additionally retains the original persistent distinction
between `(AutoDetect)` and `(Default)` English. The localized native Language menu now exposes
automatic selection as a first-class choice; application saves keep `auto-detect`, `builtin-en`,
and an explicit catalog distinct. Automatic selection clears a prior fixed catalog, matches the
bounded installed set against system preferences in order first exactly and then by primary
language, and safely remains in English when no catalog matches. The new localization key is
append-only: complete 183-key catalogs and historical 19-key catalogs upgrade with its English
fallback and re-encode canonically. Direct Ghidra evidence at `004D7360`, `004D7940`, `004DB640`,
and `004DB810` binds the original selection and fallback state machine; `004D7010` and the retained
`localization-auto-detect` fixture bind the non-executing proprietary DLL checksum/resource adapter,
including `$DAC/$DAD/$DAE` string decoding and the exact 272 single-index plus 22 range fixed-buffer
ceilings. Seventeen localization tests, nine preference tests, and eight frontend localization
audits pass. Remaining typed original-string-index mapping, editor-dialog coverage, selectable DLL
catalog conversion, and a retained Wine gesture keep the row `Partial`.

The original-module adapter now advances beyond that earlier decoder-only boundary. Direct
`CreateMainApplicationMenu` call-site evidence maps 31 semantically equivalent typed keys; the
converter normalizes Windows mnemonics/accelerators and About formatting while retaining English
for Rust-only keys. Valid `.dll` modules are now selectable by metadata display name, participate in
the same exact-then-primary auto-detection pass as `.lmlang`, install atomically, and persist as the
bounded canonical typed catalog. Twenty localization, eight loader, and 11 preference tests pass,
including an all-five-resource PE conversion gate. Unmapped original dialog templates and retained
live-module/Wine evidence still keep Configuration/Localization `Partial`.

The dialog-template dispatch map is no longer unknown: exact Ghidra tables bind 107 original IDs
to 107 language-DLL type-5 IDs, and the checked-in pairs reproduce both raw-table SHA-256 values.
The bounded non-executing PE adapter validates module identity and returns each present mapped
template while preserving per-dialog fallback for missing resources. Three synthetic two-type PE
tests bind table extent, first/last extraction, fallback, marker validation, and malformed resource
bounds. Applying template control text to the native Rust dialogs and retaining a live language-DLL
gesture remain incomplete, so the row remains `Partial` and the aggregate stays 59/65.

Both standard and extended Win32 dialog templates now decode portably with bounded UTF-16,
ordinal, alignment, font, control-ID, and creation-data handling. Synthetic rejection tests cover
every truncated prefix, and a local-only gate parses all 107 resources from both original 3.63
architectures. The converted catalog consumes five procedure-bound actions from dialogs `$042B`
and `$03F8` (common OK/Cancel plus three About buttons), while absent/malformed templates retain
English independently. The remaining control inventory still lacks native-form bindings, so this
does not promote the row or aggregate.

Converted catalogs now preserve the complete safely decoded literal dialog inventory in a bounded,
optional `LMDLG001` extension keyed by dialog ID, exact item position, and 32-bit control ID.
Historical catalogs without the extension remain byte-identical, duplicate control IDs survive by
position, and malformed/truncated extensions reject atomically. Native Modify Secondary Entrances
dialog `$03F1` is the first complete consumer: its title plus Clear Slot, Clear All Slots,
Destination Level Number, Screen Number of Entrance, X, and Y captions use their exact original
resource/control identities with independent English fallback. The combined PE-to-catalog gate,
26 focused localization tests, and the native binding test cover persistence and consumption. Other
native forms and a retained live language-DLL Wine gesture remain incomplete, so Localization and
the aggregate remain `Partial` and 59/65.

General Options `$041F` now supplies the complete native undo-history form's title, maximum-undo
label `$66`, OK, and Cancel. Change Compression Options `$0416` supplies the native migration title,
compression label `$65`, original-LZ2 `$294`, LZ3 `$296`, warning `$69`, OK, and Cancel. Focused
partial-catalog tests prove exact resource/control selection and independent fallback. Optimized
LZ2 `$295` is now a distinct selector target and routes authenticated LZ2-Orig/LZ3 sources through
the existing one-revision fast-runtime transaction; a focused test binds that command and revision
without parsing irrelevant allocation fields. Other native forms remain unbound and no live
language-DLL gesture has passed, so both affected rows and the aggregate remain unchanged.

Setup SNES Emulator `$0407` now drives the corresponding native in-app external-tool editor title,
executable-path `$66`, arguments `$68`, OK, and Cancel. The editor adds/removes tools, preserves
independent argv elements, optional working-directory templates, and all three portable event
subscriptions, then publishes the entire draft through the already failure-atomic authoritative
validator. Focused form/inventory tests and existing duplicate-ID/event application tests cover the
boundary. GBA drafts persist under a stable `gba-` ID namespace and select Setup GBA Emulator
`$0408` plus its exact title/path/arguments/OK/Cancel inventory after reopen without changing the
portable schema. Broader process notifications remain incomplete, so Configuration/External tools
and Localization remain `Partial`.

The `$0407/$0408` 8.3-ROM-path control `$67` is now functional and schema-compatible through an
explicit `{rom_8dot3}` argv placeholder. Windows resolves it with a bounded safe-wrapper
`GetShortPathNameW` call before the existing shell-free launch; non-Windows reports unsupported
instead of substituting a false path. Toggle round-trip, placeholder discovery/error behavior, and
Windows cross-compilation pass. Original cross-process notifications and broader lifecycle/registry
behavior still keep Configuration/External tools `Partial`.

The overworld Palette/ExAnimation row additionally has the exact per-map option representation.
Four independently inverted feature bits retain their unrelated low nibble, while original
lightning is decoded from its separate high-bit-first seven-map disable mask. The preview gates
original tiles, level-dot palette cycling, lightning, and submap-local records independently;
`every_overworld_feature_byte_round_trips_with_recovered_inverted_semantics`,
`every_lightning_mask_decodes_in_the_original_high_bit_shift_order`, and
`per_map_options_gate_dot_lightning_and_local_records_independently` cover every stored feature
byte, every lightning mask including its unused low bit, and independent materialization. The
descriptor-relative loader, installed-runtime save backend, and native per-map controls are now
complete. Option-only and mixed nine-payload changes share one checksum-valid application
mutation, while pristine lightning remains independently editable. Alternate lightning routing,
import/export, and ownership navigation are now closed by the destination-attribution gates.
The complete original-ROM byte differential now binds the dedicated runtime installer and its
mutable submap-owner chain. Broader original-editor behavioral evidence and supported
mapper/identity runtime-installation variants remain partial, so the row stays `Partial`.

The Graphics row's older sentence that calls subsequent ExGFX directory synchronization open is
superseded by the 2026-08-09 evidence above: the native directory route now authenticates and
reclaims every referenced RATS owner, republishes present files, removes omitted entries from all
three pointer domains, and preserves the separate sparse route for intentional individual-slot
edits. `directory_sync_reclaims_omitted_owners_and_is_one_undoable_commit`,
`directory_sync_rejects_an_unowned_existing_pointer_before_publication`,
`authentic_sa1_directory_sync_reclaims_replaces_and_removes_omitted_files`, and the extended live
Wine re-export gate cover that boundary. The row remains `Partial` for its other option and
interaction variants.

The level object/sprite rows additionally cover focus-scoped canvas duplication and removal. A click
or drag records whether the active entity is a Layer 1 object, object-backed Layer 2 object, or
sprite. Unmodified Insert duplicates that exact record and Delete removes it through the existing
typed transaction; canonical ordering, selected indexes, and the corresponding form follow the
result. Modified chords remain available to the application instead of being consumed by the
canvas. `canvas_entity_shortcuts_duplicate_and_remove_objects_and_sprites_exactly` proves a
duplicate/remove pair restores pristine level `$105` Layer 1 and sprite streams exactly.

Ghidra `HandleSpriteModeRightButtonDown` (`0048AE80`) and
`DuplicateSelectedSpritesAtCell` (`004CE840`) establish the matching unmodified right-click
clone-and-relocate route for sprites. The native canvas now repositions the clone semantically
while preserving its source and selecting the inserted record. Duplication begins on the physical
secondary press, holds the inserted entity in the original's immediate move-drag state, and applies
the final bounded position when the secondary button is released, even outside the canvas;
`right_click_duplication_repositions_objects_and_sprites_without_removing_sources` covers the
single-selection boundary. Ctrl-modified sprite selection now forms the corresponding native set;
primary drag, right-press clone plus immediate drag, Insert duplication, and Delete removal operate
on the complete set through one checked transaction.

The recovered multi-selection core now has a typed, failure-atomic foundation. Ghidra
`ValidateAndSnapSelectedObjectDrag` (`00438DC0`),
`ReinsertMovedLevelObjectsByTileDelta` (`00438540`), and
`ClampSelectedSpriteGroupMove` (`004CE610`) prove the shared whole-tile displacement and
all-members-valid bounds contract. `ObjectStream::duplicate_ordinary_object_group` and
`relocate_ordinary_object_group` retain sources when cloning, relative spacing, extension bytes,
selection order, trailing controls, and canonical screen transitions while rejecting every
malformed or out-of-bounds group without mutation;
`group_duplication_preserves_sources_relative_delta_and_selection_order` and
`invalid_group_duplication_is_failure_atomic`, together with the parallel relocation tests, cover
the model boundary. Ctrl-modified physical presses now add or remove Layer 1 and object-backed
Layer 2 members from a domain-exclusive selection set. Unmodified primary drag moves the set with
one atomic delta; unmodified right press clones the complete set at one anchor-relative tile delta,
selects the clones, begins the immediate move drag, and applies one atomic release delta.
`ctrl_object_selection_toggles_members_and_keeps_layer_domains_exclusive` and
`right_drag_duplicates_and_moves_a_complete_object_group_atomically` cover that native workflow.
The sprite stream now has the parallel legacy/expanded group transform: it preserves extensions,
tracks caller order across stable canonical sorting, and rebuilds expanded upper-Y controls once.
`expanded_sprite_group_rebuilds_controls_and_preserves_extensions`,
`sprite_group_transactions_commit_once_track_order_and_undo_atomically`, and
`right_drag_duplicates_and_moves_a_complete_sprite_group_atomically` cover that complete native
route. Ghidra `FindNearestValidTileMoveDelta` (`00438C50`) and
`FindNearestValidSpriteMoveOffset` (`004CE430`) recover the remaining correction order: major walks
toward zero inside each minor step toward zero, zero is excluded only during fallback, and every
correction restarts validation across the complete set. Object and sprite group clone/move paths now
share that orientation-bounded search.
`nearest_valid_group_delta_matches_lunar_magic_search_and_restart_order`
and `sprite_group_drag_falls_back_to_the_nearest_shared_in_bounds_delta` bind the exact search and
native transaction boundary.

The sprite row now also covers canonical framing transitions. Semantic native/MWL save paths derive
legacy versus expanded framing from the actual tokens, synchronize header bit `$20`, and reopen
through the selected grammar. The live
`lunar_magic_downgrades_unneeded_expanded_sprite_framing` oracle proves Lunar Magic 3.63 converts
an unnecessary expanded stream to the identical legacy-framed record sequence; focused tests cover
both directions, installed-canvas commit/reopen, and exact undo.

Expanded sprite controls `$FF 80..FD` are no longer an unresolved editing rejection. The executable
parser proves they are ignored two-byte padding with no upper-Y effect, and the live
`lunar_magic_strips_ignored_expanded_sprite_controls` oracle proves Lunar Magic removes `$80` and
`$FD` while preserving a neighboring real upper-Y transition and all records. Raw codecs remain
lossless; semantic edits and exports strip the ignored pairs canonically.

The standard/extended/custom-object rows now have native visual choosers for standard, recovered
command-zero extended, and resolved OSC custom objects in Layer 1 and object-backed Layer 2. The active-tileset
definition set drives selector availability and Map16 preview, including selector `$17`'s
tileset-specific substitution. Selecting an extended entry constructs the canonical three-byte
command-zero record and retains it as the placement template; selecting a standard entry clears
extension provenance and builds a fresh standard placement. Both enter their layer's checked
canvas path. OSC selection retains the command-specific three-to-eight-byte native shape as the
placement template in either layer. Controls `$00–$03` are absent. `extended_catalog_filters_previews_and_places_the_selected_object`
covers filtering, active-tileset preview, Layer 1 selection, and placement; the expanded
`primary_canvas_places_and_drags_object_backed_layer2` additionally saves, reopens, and undoes
catalog-created standard, extended, and four-byte custom Layer 2 objects.

The native object-backed Layer 2 list now uses one staged-state synchronization boundary for its
ordinary controls: list-button insertion selects and reloads the inserted record, accepted raw or
semantic replacements reload canonical fields, and deletion clamps to and reloads a surviving
record or clears the form/template when the stream becomes empty. The application-backed
`vanilla_level_editor::tests::primary_canvas_places_and_drags_object_backed_layer2` workflow covers
those actions together with reordering, typed paste, ROM reopen, and undo.
The primary Layer 1 list now follows that same boundary and constructs list-button insertions from
the extension-preserving placement template rather than rebuilding a three-byte ordinary record.
The upgraded `vanilla_level_editor::tests::osc_custom_object_inserts_commits_reopens_and_retains_display`
workflow proves exact OSC extension retention through native insertion, selection/form reload,
replacement, deletion, undo, ROM commit/reopen, and authenticated custom display resolution.
Every path that selects a real Layer 1 or object-backed Layer 2 record now retains that exact record
as its extension-preserving placement template: initial/undo reload, list and canvas selection,
typed paste, reorder, drag/resize start and completion, and canvas insertion. Only choosing a new
standard catalog command intentionally clears the template. The OSC workflow additionally proves
typed paste, duplicate materialization, and two-way reordering preserve the complete custom record.
The SSC custom-sprite path now has an application-backed mixed-width catalog fixture rather than
only model-level width checks. `vanilla_level_editor::tests::mixed_width_ssc_catalog_sprites_commit_reopen_and_undo_exactly`
proves one sprite number (`$F0`) independently selects four-, five-, six-, and seven-byte records
through extra-bit tables zero through three while retaining current placement, zero-fills every
declared extension, preserves each width and table selector through semantic coordinate editing,
coexists in one grown native stream, reopens byte-exactly, and undoes together to the expanded-ROM
baseline. Additional historical widths, installed runtimes, and Wine dialog coverage remain.

The level-header row now also has strict semantic sprite-property automation. `LMLEDIT1`
`sprite-properties MEMORY BUOYANCY_1 BUOYANCY_2` shares the typed UI setter, rejects memory above
`$12`, preserves serializer-owned expanded framing bit `$20`, changes both recovered buoyancy flags
independently, reopens checksum-valid, and undoes the complete ROM byte-exactly. Raw
`sprite-header` replacement remains available for lossless diagnostic and interchange workflows.

The primary/midway entrance row now has strict automation through `LMENTR1`. Exact main fields,
the original Layer 2 scroll preset, and an authenticated installed midway record share one ordered
failure-atomic controller batch. The scroll edit preserves the main-position low nibble, invalid
`$10+` presets and unavailable midway tables roll back earlier commands, LoROM/ExLoROM layouts bind
to the detected mapper, and the shell test proves checksum-valid reopen plus byte-exact ROM undo.

The secondary-exit row now also has strict `LMSEXED1` automation for semantic set, clear-current,
and clear-all operations. Commands stage in order on the complete detected 8,192-entry table,
validate all six native planes before dispatch, update an authentic installed runtime with a valid
checksum, reopen the exact result, and undo to the original ROM byte-for-byte. A late invalid
destination proves earlier staged clearing cannot escape.

The screen-exit row now also has strict `LMLEDIT1 object screen-exit INDEX SCREEN
DESTINATION_AND_FLAGS` automation through the same canonical setter as the native form. It covers
compact/extended shape changes, required flag `$0400`, advance-bit preservation, checksum-valid
ROM reopen, exact undo, out-of-range screen rejection, and late wrong-record rollback.

Secondary-exit MWL records and installed six-plane tables now use Lunar Magic's complete packed
coordinate byte: screen occupies bits 0–4 and Y occupies bits 5–7. The model consequently accepts
screens `$00–$1F`, rejects Y above `$07`, and no longer aliases late screens into Y. The exhaustive
`every_secondary_screen_and_y_pair_round_trips_in_native_and_mwl_encodings` test covers all 256
legal pairs, while the complete `lm-level` suite verifies both interchange and ROM-table routes.

The standard/extended-object row now also has canvas-grade `LMLEDIT1 object place` and
`object relocate-position` automation. Both share native drag/drop's absolute screen, coordinate
nibbles, and perpendicular-high bit model; regenerate minimal transitions; preserve extensions and
opaque tails; reopen checksum-valid at screens `$1E/$1F`; and undo the complete ROM byte-exactly.
Strict bounds and late-index rollback are covered.
Strict `object fields` automation now combines command, parameter, absolute screen, coordinate
nibbles, and perpendicular-high state in the same typed transaction as portable and installed
aggregate forms. It preserves custom extensions, rejects native-width changes atomically,
regenerates transitions, tracks selection across reordering, reopens checksum-valid, and undoes
the ROM byte-exactly.
Strict `LMLEDIT1 object clear` and `sprite clear` commands now replace both complete entity streams
through the shared failure-atomic edit batch, allowing a level to be rebuilt from an empty object
and sprite layout without raw-file surgery. `parses_atomic_stream_clear_commands`,
`clear_both_entity_streams_is_atomic_reopenable_and_undoable`,
`command_script_edits_builtin_smw_level_without_external_profile`, and
`command_script_drives_interpretation_bound_native_level_document` cover parsing, portable
save/reopen/undo, and a vanilla-ROM built-process clear-and-repopulate workflow.
Object-backed Layer 2 now has the parallel strict `LML2OBJ1` aggregate child. It reuses the whole
typed object grammar, rejects Layer 1 headers/sprites and tilemap-backed levels, carries Layer 2's
separate allocation plan, and has checksum-valid semantic reopen, late-failure rollback, and exact
ROM-undo evidence.
Tilemap-backed Layer 2 has the mutually exclusive strict `LML2TIL1` child for exact native-word
painting and the recovered Lunar Magic remap language, including signed global offsets and
selection-scoped execution. It rejects object storage, malformed/bounds-invalid input, and
descriptor-less bank changes atomically, and carries checksum-valid compressed reopen, late
rollback, and exact ROM-undo evidence.

The sprite row now has canvas-grade `LMLEDIT1 sprite place` and `sprite relocate-position`
automation for both legacy and expanded streams. Installed GUI, portable GUI, automation, and
controller edits share one absolute-position model that preserves record identity/extensions,
stably sorts legacy screens, rebuilds minimum orientation-aware expanded upper-Y controls, enforces
native bounds, reopens checksum-valid, rolls back late failures, and undoes the ROM byte-exactly.
Strict `sprite fields` automation now exposes Y-low, extra bits, screen, X, and sprite number as
typed packed fields. It shares the two GUI workspaces' transaction, preserves custom extensions and
expanded upper-Y, guards revision-selected record width, follows selection across canonical
reordering, and has checksum/reopen plus byte-exact ROM-undo evidence.

The level-render presentation surface now includes default-off `LM_VIEW_BLOCK_CONTENTS` (`$23FA`).
The live canvas resolves the recovered built-in Acts Like and position-dependent mappings, retains
their native composition selectors, and alpha-composites the authentic type-500/ID-502 default
M16 definitions after ordinary Map16 artwork. The extracted 8,192-byte bank includes editor-only
definitions `$219/$21A`; focused tests retain those exact words, the mapping tables, default/toggle
state, and half/three-quarter composition selection. ROM-adjacent custom M16 replacement of this
default bank remains part of the broader sidecar/editor integration row.
The adjacent default-off `LM_VIEW_BLOCK_EXITS` state now marks the recovered vanilla Acts Like
roots and level-mode exception after final-cell painter resolution, with the original opaque
black/red four-pixel outline. Tests cover the exact root set, mode exception, final-write
overwrite semantics, default/toggle state, toolbar routing, and eight stripe positions. Custom
`.dsc` flag-eight markers remain assigned to the broader sidecar/editor integration row.

The Graphics extract/insert row now has exact pristine standard-GFX extraction evidence. The
installed editor exports all 52 files (`GFX00` through `GFX33`), resolves the two startup streams,
and applies Lunar Magic's per-file packed-3bpp expansion table plus its legacy fourth-plane
synthesis for GFX01, GFX08, GFX17, GFX1E, and GFX31. The retained
`retained_lunar_magic_standard_export_matches_every_file` oracle byte-matches every file from a
fresh Lunar Magic 3.63 `-ExportGFX` run; non-pristine profiles retain their declared native table
behavior. Reciprocal first-time directory and `AllGFX.bin` insertion authenticate and install the
recovered 4bpp runtime, expand the ROM to 1 MiB, allocate all 52 files atomically, preserve the
shared-bank GFX32/GFX33 startup contract, repair the checksum, semantically reopen every file, and
undo to the byte-exact original ROM. The automated Wine gate
`lunar_magic_reexports_rust_standard_and_exgfx_across_legacy_migration` has Lunar Magic 3.63 re-export
the Rust-installed ROM with zero differing files. LZ2 encoding consequently restricts dictionary
output to command 4 because the original editor aliases command classes 4 through 7 to forward
copy; this prevents Rust-only self-consistent streams.

Repeated extraction now matches the original fixed-name overwrite behavior. Ghidra identifies
`ExtractAllGFXFiles` at `0047DA40`, `ExportExtendedGraphicsFromRom` at `0047EFF0`, and their shared
`wb` mode string at `005B2D3C`; the retained TSV binds those addresses, exact bytes, paths, names,
and handler flags. Rust replaces existing separate GFX/ExGFX outputs and `AllGFX.bin`, but stages
the complete result before atomically publishing it, retains regular-file permissions, and rejects
symlink/non-file targets without partial output. The raw Save As action now follows the approved
file-dialog replacement route rather than treating the selected existing file as a create-only
collision.

## Current critical path

The fastest route to a broadly usable editor is:

1. Finish the ordinary level-editing workflows and their native import/export dialogs.
2. Finish Map16 bitmap import and GFX/ExGFX native workflows.
3. Finish overworld transfer and custom-resource workflows.
4. Recover and implement the remaining runtime-patch families.
5. Broaden modified-ROM, SA-1, and ecosystem fixtures.
6. Complete configuration, help, packaging, and release gates.

The current Map16 bitmap evidence also includes a non-aligned 17×16 clipboard product in both
blank-option states. Rust matches the original palette, graphics, and complete 65,536-definition
workspace after correcting edge normalization: partial synthetic cells retain index zero, while
wholly synthetic 8×8 cells are materialized after source palette reduction through the vanilla
Map16 back-area selection (row 0, entry `$D`). A separate `$8FFF` boundary capture proves exact
prefix-preserving allocation exhaustion and warning text. Transparent-source blank reuse is fully
crossed and persistence-bound inside Rust, and its independent option semantics are bound to the
original `004EE470`/`004EEE40`/`004EF2D0` branch chain. A live-original cross-process product would
strengthen that evidence, but the affected rows remain Partial for the other explicitly listed
format and interaction gaps rather than for an unproven blank-reuse decision.

Whenever a row changes, its evidence must name the relevant Rust tests and, where applicable, the
Wine fixture and Ghidra address. A passing aggregate test suite is necessary but cannot promote a
row whose workflow-specific evidence is absent.

The localization auto-detection gate additionally enforces the original 64-preference comparison
ceiling on every platform. A matching 64th normalized preference is accepted, while a matching
65th preference is ignored; this prevents unbounded environment-language lists from widening the
recovered `AutoDetectAndLoadLanguageModule` contract.

The original-language-DLL compatibility boundary now has a portable, non-executing validator for
the fully recovered `$004D7010` checksum transform and a bounded PE32/PE32+ resource reader for the
`$01F4:$0DB7` magic plus `$01F4:$0DB6` display/version/locale/code-page metadata. Six focused tests
bind exact transform arithmetic, trailer placement, BOM/CRLF handling, both PE optional-header
forms, integer resource traversal, missing/out-of-bounds resources, and every truncated prefix
through the final required payload byte. Native startup now separately inventories up to 64
checksum-valid `.dll` regular files from the exact executable-adjacent `sysLMLanguage` directory,
skips unreadable/oversized/invalid siblings, and deterministically retains their complete metadata
without treating them as selectable canonical catalogs. Two discovery tests cover extension/type
filtering, invalid and sparse-oversized files, metadata ordering, and the 64-module ceiling.
At this prerequisite milestone, conversion of original localized dialog/string resources into the
typed catalog remained open; the later milestones below supersede that decoder-only boundary.

Original DLL string payload decoding is now recovered and portable as well: validated resources
`$01F4:$0DAC/$0DAD/$0DAE` supply a chained-obfuscated raw-DEFLATE pool plus parallel 32-bit
offset/length tables. Rust requires a complete stream, bounds inflated output to 32 MiB, caps the
effective table at the original 5,869 entries, checks every offset/length/NUL boundary, and
publishes only valid UTF-8 strings. Three focused tests cover exact decoding, count truncation,
invalid-entry clearing, malformed tables, incomplete compression, invalid UTF-8, and a reduced
inflate ceiling. At that milestone typed key-index mapping and localized dialog-template conversion
remained open; the later typed-catalog and 107-entry dispatch-map milestones supersede those gaps.
