# Lunar Magic 3.63 feature-parity ledger

This ledger is the product-level completion boundary for the Rust reimplementation. It complements
`REIMPLEMENTATION_TEST_MATRIX.md`, whose rows primarily describe format and subsystem evidence.
Lunar Magic feature parity is not complete until every in-scope workflow below is `Pass`.

## Evidence and status rules

The original Lunar Magic 3.63 executable is the enumeration and behavior authority. The current
baseline contains 270 PE resources, including 107 dialog resources. Ghidra on port 8089 provides
the named implementation surface; Wine provides observable UI, file, and ROM-transition behavior.

Each workflow has five independent gates:

- **Model**: lossless decode/encode of every owned and opaque field.
- **Tx**: checked mutation, checksum repair, unchanged-range preservation, reopen, undo, and redo.
- **GUI**: the complete user workflow is reachable and usable in `lm-native`.
- **Oracle**: a retained Wine fixture or automated behavioral comparison covers success and
  rejection behavior.
- **Variants**: the supported ROM/format variants are explicitly enumerated and tested.

Gate values are `P` (proved), `~` (partial or indirect evidence), and `-` (missing). A row is
`Pass` only when all required gates are `P`. `N/A` is allowed only when the original workflow has
no corresponding gate, with the reason recorded in evidence.

Broad format support does not promote a row. For example, decoding MWL files does not prove the
interactive level import workflow, and a CLI transaction does not prove its native dialog.

## Workflow ledger

| Area | Original Lunar Magic workflow | Model | Tx | GUI | Oracle | Variants | Status | Primary evidence / next gap |
| --- | --- | :---: | :---: | :---: | :---: | :---: | --- | --- |
| Lifecycle | Open ROM, recent ROM, close, quit, dirty confirmation | P | P | P | ~ | ~ | Partial | `lm-app` lifecycle and native menus pass; automate Wine warning/cancel equivalence and modified-ROM cases |
| Lifecycle | Save and Save As with atomic replacement | P | P | P | ~ | ~ | Partial | Exact application snapshot acknowledgement passes; expand Wine failure/collision matrix |
| Lifecycle | Full-ROM restore points and associated-file restore | P | P | P | ~ | - | Partial | Exact `.lrp` header, linked `DIRL` records, record extents, stored/decoded checksums, raw-DEFLATE payloads, compact fill/raw commands, logical-ROM CRC-32, and bounded reconstruction pass; an LM 3.63 two-record archive restores the 2,097,664-byte target ROM byte-exactly from `smwOrig.smc`. All thirteen Ghidra-confirmed sidecar slots decode with incremental inheritance, per-record compression, and the pre-3.21 large-file offset rule. Native creation supports initial/appended full checkpoints, deltas, successful/failed reversion markers, consecutive-reversion replacement, reciprocal link/header updates, complete timestamp snapshots, and compressed/uncompressed ROM/sidecar data. Reconstruction follows the non-linear reversion graph. Native automatic-policy controls expose interval and daily full checkpoints while preserving continuity-break priority, and persist those choices across launches. The restore dialog atomically publishes the selected ROM, sidecars, and successful reversion marker, or records the native failed form. Open projects can create a new full archive or append manual full, delta, and automatic points. Native round-trip fixtures cover nonempty sidecars and timestamp-selected incremental sidecar storage. Add authentic Lunar Magic nonempty-sidecar fixtures |
| Lifecycle | Undo/redo with configured history limit | P | P | P | ~ | ~ | Partial | Shared project history passes; compare per-editor grouping and configured limit with Wine |
| ROM | Detect identity, mapper, copier header, checksum | P | P | P | P | ~ | Partial | Headered pristine fixture is now deterministic; broaden SA-1 and modified-ROM corpus |
| ROM | Expand ROM and mapper conversion rules | P | P | P | ~ | ~ | Partial | Native expansion dialog passes; recover all original size/ExLoROM/ZSNES compatibility choices |
| ROM | Add/remove copier header | P | P | P | ~ | P | Partial | Headered/headerless unit and process suite passes; add Wine dialog transition fixture |
| ROM | Create IPS patch | P | P | P | ~ | P | Partial | Native non-blocking original/modified/output workflow follows Ghidra `CreateIpsPatch` at `0041F0B0`, normalizes copier headers, rejects aliases, and publishes atomically; automate Wine dialogs |
| ROM | Apply IPS patch | P | P | P | ~ | P | Partial | Native preview/transaction passes; add Wine error and changed-range comparison |
| ROM | Scan/reclaim owned RATS blocks | P | P | P | N/A | ~ | Partial | Rust safety workflow intentionally requires ownership proof; broaden mapper variants |
| Level | Navigate all 512 slots and preserve viewport history | P | P | P | P | P | Pass | 512/512 rendering ledger plus application navigation tests |
| Level | Edit level header, mode, music, time, scroll, sprite header | P | P | P | ~ | ~ | Partial | Forms and ROM transactions pass. The opt-in `level_header_wine::lunar_magic_exports_every_rust_legacy_level_header_field` gate changes all seven modeled legacy header bitfields plus the sprite header in one checksum-repaired Rust transaction, reopens the exact native values, and requires Lunar Magic 3.63 to export the same five-byte legacy header and complete sprite stream. Music, time, scroll, rejection behavior, and broader ROM variants still need field-by-field Wine coverage |
| Level | Edit primary/midway entrances | P | P | P | ~ | ~ | Partial | Native form and separate-midway runtime exist; incomplete Wine dialog/save matrix |
| Level | Edit screen exits | P | P | P | ~ | ~ | Partial | Typed fields and canvas route exist; compare all original exit dialog modes |
| Level | Edit secondary exits and clear operations | P | P | P | ~ | ~ | Partial | The complete 8,192-entry backend and native GUI expose staged edit, clear-current, and confirmation-gated clear-all operations. Clear-current requires the selected index to be loaded and changes exactly that entry; clear-all replaces the complete staged table with native zero records, reloads the selected form, and rejects stale confirmation delivery. Neither operation touches the ROM before the existing revision-checked atomic commit, and dirty close remains guarded. Compact pristine installation uses the detected source fixed planes as transactional preconditions, so clearing or importing changes within those planes now installs and semantically reopens instead of rejecting the intended replacement. `rom_secondary_exit_editor::tests::clear_entry_requires_loaded_selection_and_changes_only_that_entry`, `rom_secondary_exit_editor::tests::clear_all_stages_one_complete_zero_table_and_reloads_selection`, and `secondary_exit_install::tests::compact_install_can_replace_pristine_fixed_planes` cover the routes; add Wine dialog fixtures |
| Level | Insert/edit/delete/move standard and extended objects | P | P | P | ~ | ~ | Partial | Authenticated object canvas works; complete interactive command-by-command Wine coverage |
| Level | Insert/edit/delete/move standard and custom sprites | P | P | P | ~ | ~ | Partial | Standard dispatch/rendering verified; broaden custom/expanded ROM fixtures and dialog parity |
| Level | Layer 2 object-backed editing | P | P | P | P | ~ | Partial | Shared history/canvas tests pass. The SMW-US profile classifies all four recovered Layer 2 runtime generations: exact `LM $0103` selects the current descriptor layout, while hook opcodes `$A9`/`$4B`/`$5C` identify legacy formats `$100`/`$101`/`$102`. Unknown `LM` versions reject instead of being written through the wrong layout. All three legacy formats now migrate transactionally through the application and native built-in-runtime UI: the complete Layer 1 table is retained as a precondition, all 512 Layer 2 pointers and descriptors follow the recovered migration loops (including the shared `$100`/`$101` descriptor-flag normalization), the exact 64-byte `$103` hook is installed, checksum/history commit once, late changes reject atomically, and undo restores the source. The `$100` hook is authenticated in full from its recovered hard-coded `LDA #$05 / PHA / PLB` data-bank sequence and shared `$101` tail; the `$102` result matches the authentic Lunar Magic 3.01→3.63 oracle byte-for-byte; and the `$101` hook is independently identical in Lunar Magic 1.31–1.65 binaries, an official 2001 Demo World patch, and a period community patch. The opt-in `layer2_wine::lunar_magic_reexports_rust_object_backed_layer2_edit` gate discovers a real object-backed level in the installed fixture, changes an ordinary object's coordinate through the typed Rust model, saves/reopens it with a repaired checksum, and requires Lunar Magic 3.63 to export the byte-exact expected Layer 2 payload. Runtime migration routes are covered by `layer2_runtime_install::tests::format_100_hook_and_descriptor_flags_migrate_transactionally`, `layer2_runtime_install::tests::format_101_hook_and_descriptor_flags_migrate_transactionally`, `layer2_runtime_install::tests::external_lunar_magic_301_migration_matches_363_tables_and_hook_exactly`, and `built_in_runtime_installer::tests::layer2_runtime_selection_migrates_authentic_format_102_and_undoes_exactly`; retain an external `$100` ROM oracle and automate complete original-editor gestures |
| Level | Layer 2 background tile editor, rectangle tools, undo | P | P | P | P | ~ | Partial | Ghidra-proved two-plane mapping and all focused tests pass; automate complete tool gestures |
| Level | Layer 3 settings, graphics, tilemap, remap commands | P | P | P | P | ~ | Partial | Wine import/re-export evidence exists; broaden installed runtime versions |
| Level | Per-level palette editing and import/export | P | P | P | ~ | ~ | Partial | The installed native-level-assets editor boundedly imports and create-new exports its complete staged palette as portable `.lmpal`, exact 257-word raw, version-2 TPL, or RGB24 without blocking the UI. Raw/TPL/RGB imports automatically read an optional same-basename `.palmask`; raw uses the authenticated 257-word operation, while TPL/RGB use the retained installed `[0],[2..256]` to supported `[0..255]` mapping and preserve installed backdrop word 1. Selected row-zero entries receive the recovered clearing behavior. Detected RGB expansion is retained for reciprocal export, and full exports recoverably remove a stale mask sidecar. Every import routes the resulting complete palette through the aggregate controller's immutable ownership map, so fixed and ExAnimation-owned changes reject atomically while source-level provenance permits intentional cross-level copies. Loading gates aggregate edits, commit, and close; export snapshots staged colors and file persistence gates commit and close. `native_level_assets_controller::tests::complete_palette_replacement_is_slot_agnostic_shape_checked_and_atomic`, `rom_level_assets_editor::palette_transfer::tests::native_level_palette_helpers_round_trip_exact_words_and_provenance`, `rom_level_assets_editor::palette_transfer::tests::supported_transfer_uses_exact_installed_mapping_and_selected_row_zero_clearing`, and `rom_level_assets_editor::palette_transfer::tests::authentic_import_helpers_are_shape_checked_and_failure_atomic` cover the native route. Original row prompts and the Wine ownership/animation-reservation matrix remain incomplete |
| Level | Per-level and global ExAnimation editing | P | P | P | P | ~ | Partial | Semantic Wine MWL fixture exists; broaden every trigger/type/legacy runtime |
| Level | Change bypassed GFX/ExGFX and animation options | P | P | P | P | ~ | Partial | Ghidra-proved expanded-header mapping now has a lossless typed model and controls for six FG/BG plus four SP 12-bit files in both the portable and installed native-level-assets editors. Installed edits use the checked undoable aggregate transaction and preserve unknown bits. A profile-backed resolver decodes all ten selected files at native 2/3/4bpp depths and materializes fixed VRAM slots. With bypass disabled or expanded settings absent, the installed preview instead resolves SMW-US revision-0's active four-file object and four-file sprite tileset tables and pads the two unused FG/BG slots; unsupported profile families reject rather than borrowing those fixed tables. Installed tables with entries `$32/$33` also provide relocated player/animation and animated-display sources, while pristine tables use the recovered fixed startup pointers; a retained equivalence gate covers both routes. The live installed-assets workspace rasterizes tilemap Layer 2 plus recovered-family Layer 1 and object-backed Layer 2 streams from either graphics source together with profile Map16 definitions and the staged palette, at mode-specific editor dimensions; object streams supply sparse per-object final-cell writes, omit initialized blanks, and retain cross-object same-cell overwrites in painter order. Each placement carries opaque, exact destination-average, or source-only half-color composition: tileset-4 Map16 `$027`–`$02A` selects Lunar Magic's channel-floor average for object and tilemap sources, while recovered modes `$0C`/`$0D` half-color Layer 2 background pixels independently of the destination. Its typed per-layer palette routing reproduces tileset-3 object-backed Layer 2's low-row-plus-four rule without changing tilemap Layer 2 or Layer 1. Every accepted aggregate edit regenerates staged state, and the shared exact viewport adapter supplies a bounded draggable 512×448 camera at 50–400% rational zoom with center-anchored combo changes, pointer-anchored Ctrl/Command-wheel steps, a world-aligned optional Map16 grid, and click-selected Map16 cells with a shared marching overlay. The selected cell inspector preserves all staged Layer 2/Layer 1 placements in painter order, exposes words, composition, and bounded acts-like chains with typed failures, and decodes Map16 visual/source quadrants plus tile/encoded-palette/effective-CGRAM/priority/composed-flip fields. It also lists overlapping authenticated sprite parts with full provenance plus decoded tile/page/palette/priority/flip fields. Its shared 60 ms clock drives the recovered eight-phase GFX32/GFX33 tile interpreter and eight-color Dragon Coin palette cycle independently according to the two staged per-level switches; selection-only animation is isolated from both asset switches. Retained-ROM evidence proves adjacent tile phases differ, and focused tests prove palette mutation is confined to CGRAM `$64`. Authenticated standard-sprite previews are composited from the four selected ordinary SP slots, the separate GFX33 animated display page, and staged sprite palette rows. The four inverted animation-feature bits now have named lossless semantics, MWL checkbox/script routing, exact installed-ROM migration, runtime-relative table location, strict profile/allocation routing, installed checkbox/aggregate-save integration with checksum/reopen/undo coverage, and a live Lunar Magic export that recovers Rust-written byte `$5B` exactly. Global/level ExAnimation rendering remains provider-gated until transfer types are recovered; variants and broader Wine dialog/write fixtures remain |
| Level | Custom object library (`.mw0`/`.mw0t`/`.osc`) | P | P | P | ~ | ~ | Partial | Paired document/editor passes; automate Lunar Magic reload and placement equivalence |
| Level | Custom sprite library (`.mw2`/`.mwt`/`.ssc`) | P | P | P | ~ | ~ | Partial | Size-table-bound editor passes; automate all SSC display/palette/source modes |
| Level | Import/export one binary MWL level | P | P | P | P | ~ | Partial | Complete controller, shell, native level-assets actions, and live Rust export → LM import/re-export oracle pass; broaden ROM/runtime variants |
| Level | Import/export legacy multi-file levels | P | P | P | ~ | ~ | Partial | Native two-stage bounded import and rollback-safe grouped export match the exact live level-105 `.mwl`/`.mw0`/`.mw1`/`.mw2` fixture and Ghidra `00477940`/`004796c0`; add custom-palette, historical-version, and Wine rejection fixtures |
| Level | Insert multiple levels from a directory | P | P | P | P | ~ | Partial | Shell and native progress dialog auto-target visible MWLs, skip hidden files, continue after failures, commit independently, and support Escape cancellation; broader variants remain |
| Level | Export all levels to a directory | P | P | P | P | ~ | Partial | Shell and non-blocking native actions implement all/modified modes, exact `%03X` naming, grouped publication, Ghidra predicate, Wine-backed selection fixture, and cooperative Escape/button cancellation between levels with a final pre-publication cancellation gate; variants remain |
| Level | Export one/all level images as PNG/BMP | P | P | ~ | P | ~ | Partial | Renderer/CLI pass. The installed native-assets editor exports the current staged level as create-new PNG or bottom-up padded 24-bit BMP using the full pre-viewport software canvas and exact native bounds. Its non-blocking all-level PNG/BMP actions use fixed `Level %03X` names, per-level progress, Escape/button cancellation, streaming disk staging, and all-or-rollback publication; broader original prompt/naming variants remain |
| Level | Level usage analysis | P | N/A | P | P | ~ | Partial | Native background scan/progress/cancel and exact `LevelAnalysis.txt` framing pass; all four usage domains match Lunar Magic's authenticated 512-level working-ROM report, including zero Map16 count deltas; broaden modified-ROM variants (read-only workflow, so Tx is N/A) |
| Level | Restrict level access | P | N/A | P | P | ~ | Partial | Exact authenticated LM 3.63 ROM mutation, randomized protection material, native warning/title dialog, checksum preservation, and atomic rejection/undo pass; original restore-point/optional-IPS/direct-close workflow and non-LoROM variants remain |
| Level | LMSW/emulator level testing integration | ~ | - | - | - | - | Missing | Ghidra `LoadCurrentLevelIntoLmsw`, `LoadRomImageIntoLmsw`; release-safe replacement required |
| Map16 | Browse/edit subtiles, attributes, Acts Like, undo | P | P | P | ~ | ~ | Partial | Native editor loads and atomically commits all 256 foreground/background pages. Both the profile-backed and pristine/native installed-ROM controllers now route replacement, subtile, and Acts-Like edits through the same bounded complete-graph validator: dangling targets, multi-node cycles, duplicate replacements, malformed sets, and exhausted caller traversal limits reject failure-atomically, while background replacements canonicalize their non-semantic Acts-Like field to zero. Its installed-ROM workspace has a bounded 100-snapshot staged undo/redo history around manual fields, typed clipboard paste, legacy page-pair import, and complete structured import. Each accepted mutation records one exact complete-set predecessor, no-ops record nothing, undo/redo restore through the same validated controller, monotonic staged revisions invalidate pending clipboard delivery, and divergent edits clear redo; ROM commit remains one application-level undo transaction. Typed single-tile clipboard paste across the installed-ROM, standalone-page, and complete-set editors captures the exact requested tile/address and controller revision before asking the platform clipboard; delivery targets that immutable address even if selection moves, rejects a stale portable revision, ignores unsolicited paste events, and clears pending targets when workspaces open or close. `smw_map16_controller::tests::native_edits_reject_acts_like_cycles_atomically_and_normalize_background_behavior`, `smw_map16_controller::tests::native_edits_enforce_the_caller_resolution_limit`, `rom_map16_editor::tests::staged_map16_history_restores_exact_sets_and_invalidates_divergent_redo`, `rom_map16_editor::tests::staged_map16_history_is_bounded_to_one_hundred_snapshots`, `map16_editor::tests::clipboard_delivery_uses_requested_tile_and_rejects_a_stale_revision`, `map16_set_editor::clipboard::tests::clipboard_delivery_uses_requested_address_and_rejects_a_stale_revision`, and `rom_map16_editor::tests::rom_clipboard_delivery_uses_the_requested_map16_address` cover these routes; broaden Wine interaction and modified-set fixtures |
| Map16 | Import/export current page and complete sets | P | P | ~ | ~ | ~ | Partial | File models/CLI pass; the native ROM editor now boundedly imports and atomically create-new exports Lunar Magic's legacy current-page `Map16Page.bin` (`0x200` Acts-Like bytes) plus `Map16PageG.bin` (`0x800` definition bytes) pair for editable foreground pages `$02–$7F`. Import captures the selected page and ROM revision, replaces all 256 definitions through one complete-set controller edit, and rejects protected `$00–$01` or background targets; paired publication is all-or-nothing and never overwrites. Active page/complete file work gates edits, bitmap import, ROM commit, and close. The same editor has bounded complete `.map16` import and create-new export across all 256 pages, binds background import completion to the initiating ROM revision, preserves imported auxiliary/editor sections, and byte-exactly re-exports a genuine LM 3.63 fixture. `rom_map16_editor::complete_file::tests::complete_import_is_bound_to_the_revision_that_started_loading`, `rom_map16_editor::legacy_page::tests::legacy_pair_round_trips_exact_plane_shapes_and_order`, `rom_map16_editor::legacy_page::tests::page_import_is_complete_targeted_and_rejects_protected_or_background_pages`, `rom_map16_editor::legacy_page::tests::companion_path_matches_lunar_magic_g_suffix`, and `persistence_worker::tests::create_pair_is_all_or_nothing_and_never_replaces` cover the added routes; modern selected-range `.map16` prompts and the complete Wine interaction matrix remain incomplete |
| Map16 | Import bitmap with quantization, deduplication, previews | P | P | ~ | ~ | ~ | Partial | Native PNG and bounded background system-clipboard workflows load real level graphics/palette context, show synchronized pan/zoom previews, expose optimization controls and both recovered reduction choices, and model all 128 free/reusable/reserved palette states. Each accepted background request captures its ROM revision, level, first Map16 tile, two optional graphics slots, and palette row before loading; completion rejects stale revisions, and active loading or preview gates conflicting Map16 edits, transfers, bitmap requests, and ROM commit. Loading blocks close, while an open preview requires discard confirmation. The weighted set/subset allocator assigns one row per 8×8 tile and commits palette/GFX/Map16/runtime changes atomically. Median Cut matches the recovered moment/variance/average/SNES-rounding semantics, generated entries receive Lunar Magic's integer HSL row ordering before pixel remapping, and the low-color/Popularity paths match an exact Wine-observed four-color fixture. `rom_map16_editor::bitmap_import::tests::bitmap_request_captures_every_typed_target_before_loading` and `rom_map16_editor::bitmap_import::tests::bitmap_completion_is_bound_to_the_revision_that_started_loading` cover the asynchronous boundary; broader variants remain |
| Map16 | Clipboard bitmap and rectangle conversion | P | P | ~ | - | ~ | Partial | Clipboard models exist; disposable-process Wine automation now captures the original bitmap dialog plus pre/post palette and graphics buffers; compare broader option combinations and automate rectangle conversion |
| Graphics | 8×8 tile viewer/editor with selection, flips, copy/paste | P | P | P | ~ | ~ | Partial | Portable, pristine-layout, and profile-backed installed graphics editors expose selected-tile horizontal and vertical flips through their existing typed controller mutation paths. Portable flips are individually revisioned and undoable; installed flips retain fixed/ExAnimation ownership and stale/file-worker guards before joining the staged ROM commit. Typed single-tile copy/paste now spans all three surfaces through buttons and the authenticated tile-sheet gestures: Ctrl+left copies and selects the pointed tile, unmodified right-click pastes the active edited tile without changing its selection, and Ctrl+right requests a system-clipboard tile and selects the target only after successful validation. Installed paste retains the ownership, stale, and file-worker guards. Each surface displays only the active 256-tile page as the native seamless 16×16 array of 16-pixel cells. F7 toggles its initially hidden grid; Ctrl+Alt+F7 switches the retained white/black grid color and reports the exact native status. Up/Down retain page navigation for every modifier form except editable Shift without Ctrl/Alt; Left/Right wrap whenever Shift is held and the tile is editable. Page Up steps palettes with any modifiers, while Page Down does so except for the reserved Ctrl+Shift diagnostic unlock. The default selection renders the exact sixteen-color RGBQUAD table recovered from the executable. Unmodified F9 invokes each surface's existing save/commit path; pristine and installed ROM editors retain expansion, dirty-state, stale-revision, active-worker, and manifest-loader gates. The recovered `WM_CHAR` dispatcher routes lowercase and Shift-uppercase D/M/R/X/Y to apply color map, edit color maps, rotate 90° clockwise, flip horizontally, and flip vertically across all three surfaces; Ctrl/Alt chords stay outside it, and mutations reuse the same controller and ownership gates. The recovered sixteen-by-sixteen color-map model, identity reset, source/mapped previews, transactional OK/Cancel dialog, and selected-tile application now span all three surfaces through those same guards. They also share event-driven status for tile/address and palette hover, tile/foreground/background selection, copy/paste results, rendered/default palettes, viewed pages, and page boundaries. Left/right palette clicks select FG/BG, left/right pixel gestures paint them, and Ctrl+left/right sample them; native defaults FG 1 and BG 0. Installed sampling remains available on read-only tiles without bypassing mutation ownership. Installed Ctrl+Shift hover additionally reports exact OrigAnim, level ExAnimation, and global ExAnimation slots when supplied by canonical v2 ownership evidence; legacy v1 generic-record evidence remains readable. Transient hover outlines and the fixed 256×256 logical selected-tile canvas share exact render/hit-test geometry; toolkit DPI scaling supplies the native presentation scaling without an invented user zoom control. Broader Wine interaction/write fixtures remain |
| Graphics | Extract/insert GFX and ExGFX | P | P | ~ | ~ | ~ | Partial | CLI/project operations pass. The installed graphics editor non-blockingly extracts its staged slot as create-new raw 4bpp `GFX%02X.bin`/`ExGFX%02X.bin` and boundedly inserts an exact-size raw file through the same ownership-aware staged transaction. Its standard-GFX directory actions use fixed `GFX%02X.bin` names, bounded background I/O, per-file progress, and Escape/button cancellation. Extraction decodes the profile-declared standard range from one immutable ROM image and publishes the complete set all-or-rollback. Insertion requires the complete exact-shape set, precompresses every file, then allocates, repoints, semantically reopens, checksum-repairs, and commits the ROM as one revision or leaves it unchanged. Equivalent create-new `AllGFX.bin` extraction concatenates those raw slots in table order; bounded joined insertion derives exact split offsets from the current ROM and enters the same atomic transaction. Canonical profiles represent contiguous or three-plane graphics pointers, validate and audit their physical components, and protect every plane during allocation. On the exact pristine SMW layout this enables separate native directory extraction and atomic insertion of the recovered nonsequential special pair (`GFX33`, then `GFX32`) under their public filenames. Audited expanded tables require every standard `$000..$033` pointer and treat later zero pointers as unused auxiliary/ExGFX sentinels. Native ExGFX extraction enumerates every nonzero `$080..$FFF` entry and preserves raw 2/3/4bpp bytes under canonical `ExGFX%02X.bin` names; insertion strictly enumerates canonical directory files, supports both replacement and new native-depth slots, and commits the sparse set atomically without touching omitted pointers. Broader original prompts and modified-runtime fixtures remain incomplete |
| Graphics | Change compression and migrate dependent tables | P | P | P | P | ~ | Partial | Transactional dialog and recovered runtimes pass; broaden runtime/version variants |
| Graphics | External graphics editor launch and reload | P | P | P | - | ~ | Partial | The installed graphics editor exports the current staged slot into a private create-new workspace under its canonical `GFX%02X.bin`/`ExGFX%02X.bin` name. Persisted `LMTOOLS1` entries that reference `{graphics}` in a direct argument appear in a graphics-editor selector and expand that staged path together with ordinary ROM/project/level context only after the private file exists; using the staged file as a working directory is rejected before staging. A direct executable/application chooser remains available. The approval window shows the exact executable or macOS `.app` bundle and every direct argument, launches without a shell on a worker, and waits non-blockingly; macOS bundles route through `/usr/bin/open -W -n -a ... --args` so the newly launched instance participates in the same reload boundary without collapsing argument strings. Only a successful exact-size regular file reloads into the staged controller. Pixel edits, commits, and close/shutdown are gated during the round trip; missing or malformed templates, stale application revisions, failed processes, symlink/non-file replacements, truncation, growth, disconnected workers, and cleanup failures cannot replace staged state. Cancellation, completion, errors, expansion failures, and dropped prompts remove the private workspace. Exact original template syntax and Wine-observed reload prompts remain incomplete |
| Palette | Edit level/shared/overworld palettes | P | P | P | ~ | ~ | Partial | Multiple native editors pass. The ownership-qualified ROM palette, installed level-assets palette, portable/installed overworld palettes, and native shared/custom palette all use the typed clipboard for exact single-color and 16-color-row copy/paste, reproducing the documented Ctrl+Alt left/right gestures plus explicit row controls. Rows align to the selected color; incomplete tails reject. Paste is request-targeted, ROM/shared workspaces gate stale or active-import delivery, aggregate and overworld rows use their ownership-atomic controller transactions, and shared/custom rows decode and replace their exact backend once without disturbing auxiliary bytes. `native_clipboard::tests::palette_row_requires_and_round_trips_exactly_sixteen_colors`, `rom_palette_editor::tests::palette_row_paste_targets_the_complete_selected_row_and_rejects_partial_tail`, `native_level_assets_panels::palette::tests::aggregate_row_paste_is_aligned_and_complete`, `overworld_editor_palette::tests::overworld_row_paste_is_aligned_and_complete`, `rom_shared_palette_editor::tests::typed_row_paste_replaces_one_aligned_backend_row_atomically`, `rom_shared_palette_editor::workspace::tests::edits_preserve_backend_shape_and_unselected_bytes`, and `palette_controller::tests::protected_late_edit_rolls_back_and_stale_commit_is_rejected` cover these routes. Complete Wine interaction matrix remains |
| Palette | Import/export full palette, row, shared palette formats | P | P | ~ | ~ | ~ | Partial | Installed per-level full palette import/export now covers portable `.lmpal` plus exact raw, TPL v2, RGB24, and automatic optional `.palmask` handling with bounded background I/O, exact installed/supported mapping, atomic ownership enforcement, staged-state export, RGB-convention retention, stale-sidecar removal, create-new publication, and revision/busy guards. The installed shared/custom editor boundedly imports and create-new exports exact native `.smwpal` files in both the `$7E2`-byte legacy and `$810`-byte expanded layouts, including all 16 expanded auxiliary bytes; supported legacy-to-expanded installation is allowed and unsafe downgrade rejects atomically. The ownership-qualified ROM palette editor boundedly imports and create-new exports Lunar Magic's exact 257-word raw palette, version-2 TPL, and RGB24 palette. Every import automatically discovers the exact lossless 257-byte same-basename `.palmask` selector proven by the bundled LM 3.63 help and executable strings, preserving unselected words; only a missing sidecar is ignored, while malformed, non-regular, oversized, or unreadable siblings reject the whole load. TPL/RGB routing uses retained LM 3.63 evidence for the exact installed `[0],[2..256]` to supported `[0..255]` mapping, so supported selector indices address that same order while installed backdrop word 1 remains unreachable and retained. Raw import performs its recovered selected-row-zero clearing; TPL/RGB imports clear only selected supported row-zero indices 16 through 240. Masked RGB expansion detection considers only selected entries, and reciprocal export retains the detected high-bit or replicated-bit channel convention. The native mask mode starts fully enabled for each opened workspace, marks disabled colors with `X`, toggles one entry by click or its bounded 16-color row by Alt-click, exposes enable-all/disable-all, and retains imported noncanonical selector bytes until changed. Any disabled entry makes raw/TPL/RGB export atomically create the palette and same-basename `.palmask`; an all-enabled mask creates only the palette while recoverably removing a stale regular sidecar as required by LM 2.42+, and collision/failure restores that sidecar and publishes no palette. Every format submits only actual differences through immutable ownership so a protected mutation rejects the entire batch. Import loading is revision-bound and gates editing; all active file work gates commit and close, while export persists immutable palette and mask snapshots. `document_loader::tests::optional_read_omits_only_a_missing_sibling`, `persistence_worker::tests::create_pair_is_all_or_nothing_and_never_replaces`, `persistence_worker::tests::create_removing_deletes_only_a_stale_regular_sibling`, `palette_controller::tests::raw_import_is_masked_row_zero_canonical_and_ownership_atomic`, `palette_controller::tests::supported_palette_mapping_matches_retained_tpl_oracle_and_is_ownership_atomic`, `palette_controller::tests::supported_palette_mask_uses_file_indices_and_preserves_unselected_rows`, `rom_palette_editor::tests::palette_mask_click_and_alt_row_preserve_unrelated_and_partial_rows`, `rom_palette_editor::transfer::tests::raw_transfer_helper_preserves_exact_words_and_noncanonical_mask_bytes`, `rom_palette_editor::transfer::tests::tpl_and_rgb_helpers_round_trip_supported_order_and_rgb_convention`, `rom_palette_editor::transfer::tests::supported_mask_is_retained_and_limits_rgb_expansion_evidence`, `rom_palette_editor::transfer::tests::import_requests_use_automatic_same_name_palmask_sibling`, `rom_palette_editor::transfer::tests::export_publishes_palmask_only_when_a_color_is_disabled`, `rom_level_assets_editor::palette_transfer::tests::supported_transfer_uses_exact_installed_mapping_and_selected_row_zero_clearing`, `rom_level_assets_editor::palette_transfer::tests::authentic_import_helpers_are_shape_checked_and_failure_atomic`, `rom_shared_palette_editor::workspace::tests::complete_file_replacement_allows_install_upgrade_but_rejects_downgrade_atomically`, `rom_shared_palette_editor::transfer::tests::native_shared_palette_helper_round_trips_both_exact_backends`, and `rom_shared_palette_editor::tests::complete_file_upgrade_dispatches_and_reopens_exact_expanded_backend` cover these native routes. Portable formats and CLI coverage remain broad; the complete original prompt matrix remains incomplete |
| Overworld | Edit Layer 1 tiles, levels, paths, events | P | P | P | P | ~ | Partial | Complete model and transfer oracle exist; interactive tool/variant matrix incomplete |
| Overworld | Edit Layer 2 tiles and event tiles | P | P | P | P | ~ | Partial | Transfer evidence exists; automate selection/move/restore gesture parity |
| Overworld | Edit paths and directional links | P | P | P | ~ | ~ | Partial | Standalone and ROM editors pass; compare one-way/reciprocal UI behavior |
| Overworld | Edit exit/warp links | P | P | P | ~ | ~ | Partial | Native workflow passes; broaden original dialog and modified-ROM fixtures |
| Overworld | Edit level settings, names, messages, player starts | P | P | P | P | ~ | Partial | Individual native workflows pass; broaden installed runtime versions |
| Overworld | Edit sprites and custom overworld sprite sidecars | P | P | P | ~ | ~ | Partial | Models/appearance editor exist; `.ovssc`/`.ovpath*` UI parity incomplete |
| Overworld | Edit event-number map, main/special reveals, tilemaps | P | P | P | P | ~ | Partial | Wine transfer fixture covers physical/semantic changes; broaden interactive cases |
| Overworld | Palette and ExAnimation editing | P | P | P | ~ | ~ | Partial | Models/native panels exist; original timers, triggers, and import dialogs incomplete |
| Overworld | Import/export complete overworld | P | P | ~ | P | ~ | Partial | The installed-ROM complete-overworld editor now exposes bounded non-blocking import and create-new export for the nine-domain `.lmow` aggregate. Import requires the active profile shape, animation limit/modes, compact encoding, and palette ownership before one atomic staged replacement; source-slot provenance does not prevent intentional cross-slot copies. Export snapshots the complete staged aggregate and cannot overwrite. Active file work gates commit and close, and import rejects a ROM revision change during loading. `overworld_controller::tests::complete_file_replacement_is_atomic_validated_and_slot_agnostic` and `rom_overworld_editor::transfer::tests::native_transfer_helpers_round_trip_all_nine_domains` cover the native route; the retained nine-domain transfer oracle remains passing. Original file/prompt behavior and broader variants remain incomplete |
| Title | Edit/import/export title-screen tilemap | P | P | P | P | ~ | Partial | Pristine/expanded transaction and Wine transfer evidence pass; broaden variants |
| Title | Record/import/export/play title movements | P | P | P | ~ | ~ | Partial | ZSNES/Snes9x/native formats pass; original emulator capture workflow incomplete |
| Credits | Edit/import/export credits tilemap | P | P | P | P | ~ | Partial | Legacy/expanded rows and Wine transfer pass; broaden runtime variants |
| Patches | Install expanded settings, Layer 3, palette runtimes | P | P | P | P | ~ | Partial | Three built-in families are recovered; original compatibility/migration families remain |
| Patches | Sprite 19 ASM fix and Lfix3 families | P | P | ~ | P | - | Partial | The native revision-bound built-in runtime dialog exposes both the complete recovered SMW-US-v1 Lfix3 core and Lunar Magic's user-requested sprite `$19` ASM fix as first-class transactions. The Sprite 19 path was recovered from command `$26AC` and a matched pristine-ROM Lunar Magic 3.63 Wine oracle: it identity-checks and installs the exact six-byte `$00E762` hook, fixed `$20`-byte `$01BCA0` helper, and three-byte `$0020A0` branch removal, repairs the checksum, authenticates the result, commits one revision, rejects duplicate or corrupt forms, and undoes byte-exactly. If Lunar Magic already installed the authenticated shared helper, only the final branch removal is written. The Lfix3 path allocates the `$510`-byte runtime, applies 107 typed relocations and all helpers/hooks/tables, and strictly authenticates immutable code while allowing mutable per-level tables. Its recovered generation probe authenticates generation 1 from the exact `$02D7CE` JSL and complete `$02DC50` helper, generation 2 from its `$240` RATS runtime and immutable network (including the legitimately retained generation-1 hook), and generation 3 from the complete current `$0111` runtime. Both legacy generations now migrate transactionally. Generation 1 requires every later fixed write and new table destination to retain its pristine precondition, converts the live packed plane into the recovered two-plane form, initializes the third plane, installs the current runtime, authenticates the result, and undoes exactly. Generation 2 reclaims its authenticated owner in staging, relocates the current runtime into the reusable run, preserves all three live tables byte-for-byte, and keeps late failure atomic. Native/application installation routes both migrations. `lfix3_install::tests::generation_1_migration_converts_tables_authenticates_fixed_sites_and_undoes_exactly`, `lfix3_install::tests::generation_2_migration_reclaims_runtime_preserves_tables_and_undoes_exactly`, and `relocatable_patch::tests::replacement_late_plan_failure_does_not_escape_staged_reclamation` cover these boundaries. Modified-runtime variants and broader original interaction remain missing |
| Patches | Map16 staged hooks and compatibility upgrades | ~ | P | ~ | P | - | Partial | The revision-bound native built-in runtime dialog exposes the authenticated pristine SMW-US-v1 Map16 runtime transformation recovered from an unchanged complete Map16 import under Lunar Magic 3.63. One application command applies every identity-checked fixed-byte range, relocates the exact `$8000`-byte auxiliary table into the recovered 512-KiB-to-1-MiB window, publishes its typed low-bank operand, repairs the checksum, advances one project revision, semantically reopens the installed secondary Map16 storage, and undoes byte-exactly. Current-runtime detection no longer trusts the single `$22` marker: it authenticates every recovered fixed replacement byte outside the checksum and typed relocation, resolves the relocated low-bank operand, requires exact RATS ownership and length, and compares the complete auxiliary payload. Corrupt or marker-only forms reject, while an authenticated current install disables duplicate native installation without changing history. The generation probe now additionally authenticates Lunar Magic 3.01's retained stage-three network from the complete `$037600` hook base, `LM $0111` marker, and untouched `$0377A0` stage-four destination. Its transactional migration writes the recovered `LM $0112` marker and exact 20-byte stage-four helper while leaving live Map16 tables and allocations untouched; immutable no-op preconditions reject late hook changes, checksum repair remains atomic, one undo restores the exact legacy ROM, and the native dialog identifies the route as a migration. An external LM 3.01-to-3.63 before/after oracle matches every runtime byte; only Lunar Magic's editor-version stamp and Rust's explicit checksum repair are excluded. `map16_runtime_install::tests::embedded_patch_decodes_and_installs_the_relocated_wine_shape`, `map16_runtime_install::tests::current_detector_rejects_marker_only_fixed_and_payload_modifications`, `map16_runtime_install::tests::stage_three_runtime_is_authenticated_migrated_and_undoes_exactly`, `map16_runtime_install::tests::stage_three_migration_rejects_staged_hooks_changed_after_planning`, `map16_runtime_install::tests::external_stage_three_oracle_matches_lunar_magic_upgrade_bytes`, `built_in_runtime_installer::workspace::tests::commands_are_bound_to_the_open_revision_and_selected_family`, `built_in_runtime_installer::tests::map16_runtime_selection_installs_auxiliary_hooks_and_undoes_exactly`, and `built_in_runtime_installer::tests::map16_runtime_selection_migrates_stage_three_and_undoes_exactly` cover the recovered and native boundaries. Earlier `$0100`/`$0101` stages, mapper variants, and broader original interaction remain missing |
| Configuration | Keyboard shortcuts | P | P | P | - | ~ | Partial | The native Tools → Keyboard Shortcuts editor stages the active typed configuration, supports every action and portable key family, rejects malformed/duplicate gestures atomically, suppresses active shortcuts while editing, applies or clears the complete binding set, and persists the canonical validated configuration across launches. `shortcut_editor::tests` and `application::preference_tests` cover parsing, action coverage, validation, ordering, and persistence. Original shortcut-dialog behavior and a Wine oracle remain missing |
| Configuration | Main/user toolbar customization | P | P | ~ | - | ~ | Partial | Tools → Customize Toolbar now stages the active lossless layout, preserves stable IDs and independent localization keys, exposes all twelve actions and separators, supports reordering/removal, validates atomically, restores the built-in toolbar explicitly, and persists canonical `LMTBAR01` layouts across launches. Configured toolbars render with complete English fallback text when no localization catalog is installed. `toolbar_editor::tests`, `frontend_ui::tests::english_fallback_covers_every_typed_localization_key`, and `application::preference_tests` cover the native path. Original bitmap/icon and user-toolbar/menu editor behavior plus a Wine oracle remain incomplete |
| Configuration | Localization | P | P | ~ | - | ~ | Partial | Tools → Language now reports the active locale, non-blockingly loads a standalone bounded `.lmlang`/`LMLOC001` catalog, validates and switches it atomically, persists exact canonical Unicode catalog bytes across launches, and explicitly restores built-in English. `localization::tests::published_encoded_limit_accepts_the_largest_valid_catalog`, `configuration_loader::tests::decodes_standalone_localization_catalog`, `application::preference_tests`, and `state::tests::frontend_configuration_replacement_is_validated_and_atomic` cover bounds, decoding, persistence, and reset. The typed catalog currently covers title/status/toolbar text; complete menu/dialog coverage, original language discovery/switch behavior, and a Wine oracle remain incomplete |
| Configuration | External tools and event subscriptions | P | P | P | ~ | ~ | Partial | Safe argument-vector launcher passes; compare original menus, events, and registry migration |
| Help | Help topics, diagnostics, version/about | ~ | N/A | ~ | - | - | Partial | Native Help → About exposes the Rust build version, clean-room identity, Lunar Magic 3.63 compatibility target, license, source link, and a source-URL clipboard action. Help → Build diagnostics presents and copies a bounded non-sensitive report containing build identity plus project dirty state, active editor, profile/save state, undo/redo availability, and current level; it excludes paths and project content. Original CHM topic launching, deeper ROM/runtime diagnostics, About layout equivalence, and Wine evidence remain missing |
| Release | Installer, portable packaging, updates, crash recovery | - | - | - | - | - | Missing | Required for mature drop-in replacement; not represented by format tests |

## Current critical path

The fastest route to a broadly usable editor is:

1. Finish the ordinary level-editing workflows and their native import/export dialogs.
2. Finish Map16 bitmap import and GFX/ExGFX native workflows.
3. Finish overworld transfer and custom-resource workflows.
4. Recover and implement the remaining runtime-patch families.
5. Broaden modified-ROM, SA-1, and ecosystem fixtures.
6. Complete configuration, help, packaging, and release gates.

Whenever a row changes, its evidence must name the relevant Rust tests and, where applicable, the
Wine fixture and Ghidra address. A passing aggregate test suite is necessary but cannot promote a
row whose workflow-specific evidence is absent.
